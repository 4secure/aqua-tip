export default function ResetPasswordPage() {
  return (
    <div className="min-h-screen bg-primary flex items-center justify-center">
      <p className="text-text-secondary font-mono">Reset Password — placeholder (Plan 03)</p>
    </div>
  );
}
