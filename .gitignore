# Dependencies
node_modules/

# Build output
dist/

# Environment files
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Vite
.vite/

# Playwright MCP
.playwright-mcp/

# Local only
.claude/
CLAUDE.md

# Stray root npm artifact (no package.json at root — frontend/ is the only npm workspace)
/package-lock.json

# Scratch SQL scripts (run manually against the prod DB; never commit — may contain PII)
backend/tmp_*.sql
