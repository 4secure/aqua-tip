---
phase: 06-postgresql-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: false
requirements:
  - DB-03
  - DB-04

must_haves:
  truths:
    - "All migrations run cleanly on a fresh PostgreSQL database"
    - "All 44+ Pest tests pass (on SQLite, confirming no migration regressions)"
    - "PostgreSQL database is accessible and application connects successfully"
  artifacts:
    - path: "backend/database/migrations/*"
      provides: "Migration files that run on PostgreSQL"
  key_links:
    - from: "backend/.env"
      to: "PostgreSQL server"
      via: "pgsql driver connection"
      pattern: "DB_CONNECTION=pgsql"
    - from: "backend/database/migrations/*"
      to: "PostgreSQL aqua_tip database"
      via: "php artisan migrate:fresh"
---

<objective>
Create the PostgreSQL database, run all migrations, execute the full test suite, and verify local development works end-to-end.

Purpose: Validate that the MySQL-to-PostgreSQL migration is complete and the local dev environment functions correctly.
Output: Working PostgreSQL database with all tables, passing test suite, verified local dev.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-postgresql-migration/06-RESEARCH.md
@.planning/phases/06-postgresql-migration/06-01-SUMMARY.md

<interfaces>
<!-- Plan 01 outputs: .env now has DB_CONNECTION=pgsql, DB_PORT=5432, DB_USERNAME=postgres -->
<!-- PostgreSQL 17.2 installed at C:/laragon/bin/postgresql/postgresql/ -->
<!-- createdb CLI: C:/laragon/bin/postgresql/postgresql/bin/createdb.exe -->
<!-- psql CLI: C:/laragon/bin/postgresql/postgresql/bin/psql.exe -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostgreSQL database and run migrations</name>
  <files></files>
  <action>
1. Verify PostgreSQL is running. Use the Laragon PostgreSQL binaries:
   ```bash
   "C:/laragon/bin/postgresql/postgresql/bin/pg_isready.exe" -h 127.0.0.1 -p 5432
   ```
   If PostgreSQL is not running, inform the user they need to enable PostgreSQL in Laragon and restart it. Do NOT proceed until PostgreSQL is confirmed running.

2. Create the `aqua_tip` database (if it does not already exist):
   ```bash
   "C:/laragon/bin/postgresql/postgresql/bin/createdb.exe" -U postgres -h 127.0.0.1 aqua_tip
   ```
   If database already exists, the command will error -- that is fine, continue.

3. Run fresh migrations against PostgreSQL:
   ```bash
   cd backend && php artisan migrate:fresh --force
   ```
   This drops all tables and re-runs all 9 migrations. All should succeed with the ->after() calls removed.

4. Verify database connection:
   ```bash
   cd backend && php artisan db:show
   ```
   Confirm output shows driver=pgsql, database=aqua_tip.

If migration fails with authentication error, try password "postgres" by setting DB_PASSWORD=postgres in .env. Laragon's PostgreSQL default credentials vary by installation.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan migrate:status | head -20</automated>
  </verify>
  <done>
  - PostgreSQL aqua_tip database exists
  - All 9 migrations show "Ran" status
  - `php artisan db:show` confirms pgsql driver and aqua_tip database
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full Pest test suite</name>
  <files></files>
  <action>
Run the full Pest test suite to confirm no regressions from the migration changes:
```bash
cd backend && php artisan test
```

Tests run on SQLite in-memory (per phpunit.xml config) which validates that the migration file changes (->after() removal) did not break anything. The tests use Eloquent/query builder only, so they are database-agnostic.

Expected: All 44+ tests pass. If any test fails, diagnose whether the failure is related to the migration changes or a pre-existing issue. Migration-related failures should be fixed. Pre-existing failures should be noted but not block completion.

Additionally, run one test cycle against PostgreSQL directly to satisfy DB-03 ("tests pass against PostgreSQL"):
```bash
cd backend && DB_CONNECTION=pgsql DB_DATABASE=aqua_tip php artisan test
```

Note: On Windows/Git Bash, environment variable override syntax may need adjustment. If the inline env var approach does not work, temporarily update phpunit.xml to use pgsql, run tests, then revert phpunit.xml.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test</automated>
  </verify>
  <done>
  - All 44+ Pest tests pass on SQLite (default)
  - All 44+ Pest tests pass on PostgreSQL (at least one confirmed run)
  - No test modifications required (or minimal PostgreSQL-compatible fixes documented)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify local dev works end-to-end with PostgreSQL</name>
  <files></files>
  <action>
Present the user with verification steps for the complete MySQL-to-PostgreSQL migration. What was built:
- .env and .env.example updated to pgsql config
- config/database.php cleaned (removed dead Mysql import)
- 3 migration files had ->after() calls removed (6 total)
- PostgreSQL database created with all tables
- All tests passing on both SQLite and PostgreSQL

Verification steps for user:
1. Open Laragon and confirm PostgreSQL service is running (green indicator)
2. Run `cd backend && php artisan serve` to start the dev server
3. Open http://localhost:8000 in browser -- confirm the application loads
4. Test a database-dependent endpoint (e.g., registration or login flow)
5. Optionally check pgAdmin or psql to see the aqua_tip tables:
   ```
   "C:/laragon/bin/postgresql/postgresql/bin/psql.exe" -U postgres -d aqua_tip -c "\dt"
   ```
  </action>
  <verify>User confirms local development works with PostgreSQL backend</verify>
  <done>User has approved that the application works end-to-end on PostgreSQL locally</done>
</task>

</tasks>

<verification>
- `php artisan migrate:status` shows all migrations as "Ran"
- `php artisan db:show` shows pgsql driver, aqua_tip database
- `php artisan test` passes all 44+ tests
- Application responds on localhost:8000 with PostgreSQL backend
</verification>

<success_criteria>
- PostgreSQL database aqua_tip exists with all tables from 9 migrations
- All 44+ Pest tests pass without modification
- Local development server starts and serves requests backed by PostgreSQL
- User has confirmed end-to-end local dev works (checkpoint approved)
</success_criteria>

<output>
After completion, create `.planning/phases/06-postgresql-migration/06-02-SUMMARY.md`
</output>
