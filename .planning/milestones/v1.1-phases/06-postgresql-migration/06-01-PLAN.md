---
phase: 06-postgresql-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - backend/.env.example
  - backend/config/database.php
  - backend/database/migrations/2026_03_12_154943_add_oauth_columns_to_users_table.php
  - backend/database/migrations/2026_03_13_000003_add_trial_ends_at_to_users_table.php
  - backend/database/migrations/2026_03_13_105454_add_phone_and_onboarding_to_users_table.php
autonomous: true
requirements:
  - DB-01
  - DB-02

must_haves:
  truths:
    - "DB_CONNECTION is set to pgsql in .env and .env.example"
    - "No MySQL-specific ->after() calls remain in any migration file"
    - "config/database.php has no dead MySQL import"
  artifacts:
    - path: "backend/.env"
      provides: "PostgreSQL connection config"
      contains: "DB_CONNECTION=pgsql"
    - path: "backend/.env.example"
      provides: "PostgreSQL defaults for other developers"
      contains: "DB_CONNECTION=pgsql"
    - path: "backend/config/database.php"
      provides: "Clean database config without dead MySQL import"
  key_links:
    - from: "backend/.env"
      to: "backend/config/database.php"
      via: "env() calls reading DB_CONNECTION, DB_PORT, DB_USERNAME"
      pattern: "DB_CONNECTION=pgsql"
---

<objective>
Switch Laravel from MySQL to PostgreSQL configuration and remove MySQL-specific syntax from migrations.

Purpose: Prepare the codebase for PostgreSQL -- all config files point to pgsql and all migrations are cross-database compatible.
Output: Updated .env, .env.example, config/database.php, and 3 migration files with ->after() removed.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-postgresql-migration/06-RESEARCH.md

<interfaces>
<!-- From backend/config/database.php: pgsql connection block already exists (lines 87-100) -->
<!-- From backend/.env: Currently DB_CONNECTION=mysql, DB_PORT=3306, DB_USERNAME=root -->
<!-- 3 migration files use ->after() which is MySQL-only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database configuration for PostgreSQL</name>
  <files>backend/.env, backend/.env.example, backend/config/database.php</files>
  <action>
1. Edit `backend/.env`:
   - Change `DB_CONNECTION=mysql` to `DB_CONNECTION=pgsql`
   - Change `DB_PORT=3306` to `DB_PORT=5432`
   - Change `DB_USERNAME=root` to `DB_USERNAME=postgres`
   - Leave `DB_PASSWORD=` as empty (Laragon default for PostgreSQL)
   - Leave `DB_DATABASE=aqua_tip` unchanged

2. Edit `backend/.env.example`:
   - Change `DB_CONNECTION=mysql` to `DB_CONNECTION=pgsql`
   - Change `DB_PORT=3306` to `DB_PORT=5432`
   - Change `DB_USERNAME=aqua_tip` to `DB_USERNAME=postgres`
   - Leave `DB_PASSWORD=` as empty

3. Edit `backend/config/database.php`:
   - Remove `use Pdo\Mysql;` import on line 4 (dead code since we no longer use the mysql driver)
   - Do NOT remove the mysql/mariadb connection blocks -- they are harmless and part of Laravel's default config
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && grep "DB_CONNECTION=pgsql" .env && grep "DB_PORT=5432" .env && grep "DB_USERNAME=postgres" .env && grep "DB_CONNECTION=pgsql" .env.example && ! grep "use Pdo\\\\Mysql" config/database.php && echo "PASS"</automated>
  </verify>
  <done>
  - .env has DB_CONNECTION=pgsql, DB_PORT=5432, DB_USERNAME=postgres
  - .env.example has DB_CONNECTION=pgsql, DB_PORT=5432, DB_USERNAME=postgres
  - config/database.php no longer imports Pdo\Mysql
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove MySQL-specific ->after() from migrations</name>
  <files>backend/database/migrations/2026_03_12_154943_add_oauth_columns_to_users_table.php, backend/database/migrations/2026_03_13_000003_add_trial_ends_at_to_users_table.php, backend/database/migrations/2026_03_13_105454_add_phone_and_onboarding_to_users_table.php</files>
  <action>
Remove all `->after('column_name')` method calls from the following migrations. These are MySQL-only column ordering modifiers that PostgreSQL silently ignores. Removing them makes migrations explicitly cross-database compatible.

1. `2026_03_12_154943_add_oauth_columns_to_users_table.php` -- remove 3 instances:
   - `->after('remember_token')` from oauth_provider column
   - `->after('oauth_provider')` from oauth_id column
   - `->after('oauth_id')` from avatar_url column

2. `2026_03_13_000003_add_trial_ends_at_to_users_table.php` -- remove 1 instance:
   - `->after('...')` from trial_ends_at column (check exact column name in file)

3. `2026_03_13_105454_add_phone_and_onboarding_to_users_table.php` -- remove 2 instances:
   - `->after('...')` from phone column
   - `->after('...')` from onboarding_completed_at column (check exact column names in file)

Do NOT change anything else in these files. Keep all column definitions, types, nullable(), default() etc. intact. Only strip the ->after() chain calls.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && grep -r "->after(" database/migrations/ && echo "FAIL: ->after() still found" || echo "PASS: no ->after() calls remain"</automated>
  </verify>
  <done>
  - Zero `->after()` calls in any migration file under backend/database/migrations/
  - All other migration logic unchanged
  </done>
</task>

</tasks>

<verification>
- `grep -r "->after(" backend/database/migrations/` returns no results
- `grep "DB_CONNECTION=pgsql" backend/.env` succeeds
- `grep -c "use Pdo\\Mysql" backend/config/database.php` returns 0
- Existing tests still pass on SQLite: `cd backend && php artisan test`
</verification>

<success_criteria>
- .env and .env.example both configured for PostgreSQL (pgsql, port 5432, user postgres)
- config/database.php has no dead Pdo\Mysql import
- All 3 migration files have ->after() calls removed (6 total removals)
- No other code changes -- migrations are otherwise identical
</success_criteria>

<output>
After completion, create `.planning/phases/06-postgresql-migration/06-01-SUMMARY.md`
</output>
