---
phase: 04.1-layout-redesign-sidebar-topbar-and-applayout-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - frontend/src/components/layout/AppLayout.jsx
  - frontend/src/components/layout/Sidebar.jsx
  - frontend/src/components/layout/Topbar.jsx
  - frontend/src/components/layout/SearchModal.jsx
  - frontend/src/styles/main.css
autonomous: false
requirements:
  - LAYOUT-SIDEBAR-COLLAPSE
  - LAYOUT-SIDEBAR-HOVER
  - LAYOUT-SIDEBAR-GLASS
  - LAYOUT-SIDEBAR-LOGO
  - LAYOUT-SIDEBAR-SETTINGS-SUBMENU
  - LAYOUT-AUTH-NAV-STATES
  - LAYOUT-MOBILE-DRAWER
  - LAYOUT-TOPBAR-REMOVE-SEARCH
  - LAYOUT-TOPBAR-AVATAR-DROPDOWN
  - LAYOUT-TOPBAR-GUEST-BUTTONS
  - LAYOUT-TOPBAR-BREADCRUMB
  - LAYOUT-DYNAMIC-OFFSET

must_haves:
  truths:
    - "Sidebar collapses to ~64px icon-only rail when toggle is clicked"
    - "Hovering over collapsed sidebar temporarily expands it as overlay with labels"
    - "Sidebar collapse preference persists across page reloads via localStorage"
    - "Unauthenticated users see all nav items disabled/grayed with lock icon except IP Search"
    - "Clicking a locked nav item redirects to /login"
    - "Settings menu is hidden for unauthenticated users"
    - "Settings has collapsible submenu with Profile Management item"
    - "Sidebar uses glassmorphism (semi-transparent + backdrop-blur) not solid bg-surface"
    - "Logo shows logo.png, adapts between full and icon-only on collapse"
    - "On mobile, sidebar is an overlay drawer opened via hamburger in topbar"
    - "Topbar has no search trigger or Cmd+K shortcut"
    - "Authenticated topbar shows PRO badge, notification bell, avatar with dropdown (Manage Profile + Logout)"
    - "Unauthenticated topbar shows Log In (ghost) + Sign Up (gradient) buttons"
    - "Topbar left offset and main content margin adjust dynamically with sidebar collapse"
  artifacts:
    - path: "frontend/src/components/layout/AppLayout.jsx"
      provides: "Layout shell with collapse state, mobile drawer state, no SearchModal"
      min_lines: 30
    - path: "frontend/src/components/layout/Sidebar.jsx"
      provides: "Collapsible sidebar with glassmorphism, auth-aware nav, hover-expand, mobile drawer"
      min_lines: 100
    - path: "frontend/src/components/layout/Topbar.jsx"
      provides: "Topbar with dynamic offset, avatar dropdown, mobile hamburger, no search"
      min_lines: 50
    - path: "frontend/src/styles/main.css"
      provides: "Dynamic main-content margin with transition"
  key_links:
    - from: "frontend/src/components/layout/AppLayout.jsx"
      to: "frontend/src/hooks/useSidebarCollapse.js"
      via: "hook import and usage"
      pattern: "useSidebarCollapse"
    - from: "frontend/src/components/layout/Sidebar.jsx"
      to: "frontend/src/data/mock-data.js"
      via: "NAV_ITEMS import"
      pattern: "import.*NAV_ITEMS"
    - from: "frontend/src/components/layout/Sidebar.jsx"
      to: "frontend/src/contexts/AuthContext.jsx"
      via: "useAuth for locked/active nav state"
      pattern: "useAuth"
    - from: "frontend/src/components/layout/AppLayout.jsx"
      to: "frontend/src/components/layout/Sidebar.jsx"
      via: "props: collapsed, toggle, hovered, mobileOpen, setMobileOpen"
      pattern: "collapsed.*toggle"
    - from: "frontend/src/components/layout/AppLayout.jsx"
      to: "frontend/src/components/layout/Topbar.jsx"
      via: "props: collapsed, onHamburgerClick"
      pattern: "collapsed.*onHamburgerClick"
---

<objective>
Rewrite the layout shell components: Sidebar, Topbar, and AppLayout.

Purpose: Implement the full visual and interactive redesign -- collapsible sidebar with glassmorphism and auth-aware nav states, simplified topbar with avatar dropdown, mobile responsive drawer, and dynamic content offsets. This is the core of the layout redesign.

Output: Fully functional layout shell matching all user decisions from CONTEXT.md.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-layout-redesign-sidebar-topbar-and-applayout-overhaul/04.1-CONTEXT.md
@.planning/phases/04.1-layout-redesign-sidebar-topbar-and-applayout-overhaul/04.1-RESEARCH.md
@.planning/phases/04.1-layout-redesign-sidebar-topbar-and-applayout-overhaul/04.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/hooks/useSidebarCollapse.js (created in Plan 01):
```jsx
export function useSidebarCollapse() {
  // Returns { collapsed: boolean, toggle: () => void }
  // Persists to localStorage key 'sidebar-collapsed'
}
```

From frontend/src/data/mock-data.js (updated in Plan 01):
```jsx
export const NAV_ITEMS = Object.freeze([
  { label: 'Dashboard', icon: 'dashboard', href: '/dashboard', public: false },
  { label: 'IP Search', icon: 'search', href: '/ip-search', public: true },
  { label: 'Threat Map', icon: 'globe', href: '/threat-map', public: false },
  { label: 'Dark Web', icon: 'globe', href: '/dark-web', public: false },
  { label: 'Threat Actors', icon: 'users', href: '/threat-actors', public: false },
  { label: 'Threat News', icon: 'rss', href: '/threat-news', public: false },
]);
```

From frontend/src/contexts/AuthContext.jsx:
```jsx
export function useAuth() → { isAuthenticated, user, userInitials, logout, loading, emailVerified, onboardingCompleted }
```

From frontend/src/data/icons.jsx:
```jsx
export function Icon({ name }) // renders icon by name string
```

Existing Lucide icons already imported in project: LogOut, Settings
New Lucide icons needed: Lock, Menu, X, ChevronDown, ChevronRight, ChevronLeft, Users, Newspaper

From frontend/src/components/ui/GradientButton.jsx:
```jsx
export function GradientButton({ children, size, variant, ...props })
```

From frontend/src/styles/main.css:
```css
.main-content { @apply flex-1 ml-[260px] pt-[60px] p-6; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AppLayout and Sidebar</name>
  <files>
    frontend/src/components/layout/AppLayout.jsx,
    frontend/src/components/layout/Sidebar.jsx,
    frontend/src/components/layout/SearchModal.jsx
  </files>
  <action>
1. **Delete `SearchModal.jsx`** -- remove the file entirely. Global search (Cmd+K) is eliminated per user decision.

2. **Rewrite `AppLayout.jsx`**:
   - Remove: SearchModal import, searchOpen state, openSearch/closeSearch callbacks, the `useKeyboardShortcut('k', ...)` binding
   - Keep: NotificationDrawer import and notifOpen state, `useKeyboardShortcut('Escape', ...)` for closing notif drawer
   - Add: `import { useSidebarCollapse } from '../../hooks/useSidebarCollapse'`
   - Add: `const { collapsed, toggle } = useSidebarCollapse()`
   - Add: `const [mobileOpen, setMobileOpen] = useState(false)` for mobile drawer
   - Pass to Sidebar: `collapsed`, `toggle`, `mobileOpen`, `setMobileOpen`
   - Pass to Topbar: `collapsed`, `onHamburgerClick={() => setMobileOpen(true)}`, `onNotifClick={toggleNotif}`
   - Main content div: Replace static `className="main-content"` with dynamic classes:
     - Desktop expanded: `ml-[260px]`
     - Desktop collapsed: `ml-16` (64px)
     - Mobile (lg breakpoint): `ml-0`
     - Always: `flex-1 pt-[60px] p-6 transition-all duration-300`
     - Use template literal: `` `flex-1 pt-[60px] p-6 transition-all duration-300 ${collapsed ? 'lg:ml-16' : 'lg:ml-[260px]'} ml-0` ``
   - Remove SearchModal from JSX entirely

3. **Rewrite `Sidebar.jsx`** (full rewrite, ~150 lines):
   - **Props:** `collapsed`, `toggle`, `mobileOpen`, `setMobileOpen`
   - **Imports:** NAV_ITEMS from mock-data, Icon from icons, useAuth from AuthContext, useNavigate/NavLink from react-router-dom, Lucide icons: Lock, Settings, LogOut, ChevronDown, ChevronRight, ChevronLeft, X
   - **State:**
     - `const [hovered, setHovered] = useState(false)` -- for hover-expand on collapsed sidebar
     - `const [settingsOpen, setSettingsOpen] = useState(false)` -- for Settings submenu
     - `const { isAuthenticated, user, userInitials, logout } = useAuth()`
   - **Hover-expand logic:**
     - `onMouseEnter={() => collapsed && setHovered(true)}`
     - `onMouseLeave={() => setHovered(false)}`
     - Add 150ms delay before expanding: use `setTimeout` in onMouseEnter, clear in onMouseLeave to prevent jitter
     - `const showLabels = !collapsed || hovered` -- determines if text labels are visible
   - **Aside element (sidebar container):**
     - Fixed position, left-0, top-0, h-screen, z-40, flex flex-col
     - Width: `collapsed && !hovered ? 'w-16' : 'w-[260px]'`
     - Glassmorphism style (inline): `background: 'rgba(15, 17, 23, 0.8)'`, `backdropFilter: 'blur(20px)'`, `WebkitBackdropFilter: 'blur(20px)'`, `borderRight: '1px solid rgba(30, 32, 48, 0.8)'`
     - When hovered while collapsed: add `shadow-2xl` for overlay effect
     - Transition: `transition-all duration-300 ease-in-out`
     - **Mobile behavior:** On screens < lg (1024px), sidebar is hidden unless `mobileOpen` is true. Use classes: `max-lg:fixed max-lg:z-50` and `max-lg:${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`. On desktop (lg+): always visible, no translate.
   - **Mobile backdrop:** When `mobileOpen` is true, render a backdrop div: `fixed inset-0 bg-black/50 z-40 lg:hidden` with onClick={()=>setMobileOpen(false)}
   - **Mobile close button:** When mobileOpen, show X icon button in top-right of sidebar
   - **Logo section (top, h-[60px]):**
     - Use `<img src="/logo.png" alt="AquaSecure" className="w-8 h-8 rounded-lg object-contain" />`
     - When `showLabels`: show "AquaSecure" (font-display font-bold) and "Threat Intel Platform" (text-[10px] text-text-muted uppercase)
     - When collapsed and not hovered: show only the logo image
   - **Nav section:**
     - Map over NAV_ITEMS (flat array from Plan 01)
     - For each item, render based on auth state:
       - If `item.public || isAuthenticated`: render as `<NavLink>` with active style `bg-violet/10 text-violet-light border border-violet/20` (keep existing pattern)
       - If `!isAuthenticated && !item.public`: render as `<button>` that calls `navigate('/login')`. Style: `text-text-muted/50 opacity-40` with a Lock icon (w-3.5 h-3.5) on the right side when showLabels
     - When `!showLabels` (collapsed, not hovered): show only the icon (centered in the 64px width), no label text, no lock icon
     - When `showLabels`: show icon + label + (lock icon if disabled)
   - **Settings section (below nav, only if `isAuthenticated`):**
     - Hidden entirely for unauthenticated users per decision
     - Collapsible submenu: button with Settings icon + "Settings" label + ChevronDown/ChevronRight
     - Click toggles `settingsOpen` state
     - When open: show nested item "Profile Management" linking to `/settings`
     - Style same as other nav items but indented (pl-12 when expanded)
   - **Collapse toggle button (bottom of sidebar):**
     - Positioned at bottom of nav area, above any footer
     - ChevronLeft when expanded, ChevronRight when collapsed
     - `onClick={toggle}`
     - Only visible on desktop (lg+), hide on mobile: `max-lg:hidden`
     - Small, subtle: `p-2 rounded-lg hover:bg-surface-2 text-text-muted`
   - **Remove:** Old avatar dropdown footer (moved to Topbar per decision). No user info in sidebar footer.
  </action>
  <verify>
    <automated>cd frontend && npx vite build --logLevel error 2>&1 | tail -5</automated>
  </verify>
  <done>
    - SearchModal.jsx deleted
    - AppLayout manages collapse state via useSidebarCollapse hook and mobile drawer state
    - AppLayout passes props to Sidebar and Topbar, no SearchModal in JSX
    - Sidebar renders flat nav items from NAV_ITEMS
    - Sidebar collapses to 64px rail, hover-expands as overlay
    - Sidebar uses glassmorphism styling
    - Sidebar shows locked items (grayed + lock icon) for guests, except IP Search
    - Settings hidden for guests, collapsible submenu for authenticated users
    - Logo uses logo.png, adapts to collapse state
    - Mobile drawer with backdrop overlay
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Topbar and update main.css</name>
  <files>
    frontend/src/components/layout/Topbar.jsx,
    frontend/src/styles/main.css
  </files>
  <action>
1. **Rewrite `Topbar.jsx`**:
   - **Props:** `collapsed`, `onHamburgerClick`, `onNotifClick` (remove `onSearchClick`)
   - **Imports:** useLocation, Link, useNavigate from react-router-dom; NOTIFICATIONS from mock-data; Icon from icons; useAuth from AuthContext; GradientButton from ui; Lucide: Menu
   - **State:** `const [avatarDropdown, setAvatarDropdown] = useState(false)` with click-outside ref
   - **Update PAGE_NAMES:** Remove old entries (/cti, /cti-report, /feeds, /vuln-scanner, /domain-report, /cve-detail, /components). Add new entries: `/ip-search: 'IP Search'`, `/dark-web: 'Dark Web'`, `/threat-actors: 'Threat Actors'`, `/threat-news: 'Threat News'`. Keep /dashboard, /threat-map, /settings.
   - **Header element:**
     - Dynamic left offset: `${collapsed ? 'lg:left-16' : 'lg:left-[260px]'} left-0`
     - Keep: fixed, top-0, right-0, h-[60px], bg-surface/80, backdrop-blur-xl, border-b border-border, z-30
     - Add: `transition-all duration-300` for smooth offset change
   - **Left side (breadcrumb area):**
     - **Hamburger button (mobile only):** `<button className="lg:hidden p-2 -ml-2 mr-2 text-text-muted hover:text-text-primary" onClick={onHamburgerClick}><Menu className="w-5 h-5" /></button>`
     - Keep breadcrumb: "AquaSecure / {pageName}" exactly as current
   - **Right side:**
     - **REMOVE entirely:** The search trigger button (the one with Cmd+K kbd). Gone.
     - **Authenticated state:**
       - PRO badge (keep as-is)
       - Notification bell (keep as-is, uses onNotifClick)
       - **Avatar with dropdown** (NEW -- replaces static avatar):
         - Clickable avatar (same gradient circle or user.avatar_url image)
         - Click toggles `avatarDropdown` state
         - Dropdown: absolute positioned, right-0, top-full, mt-2, w-48, bg-surface-2, border border-border, rounded-lg, shadow-lg, z-50
         - Items: "Manage Profile" (navigates to /settings, closes dropdown) and "Logout" (calls logout(), navigates to /, closes dropdown)
         - Click-outside to close: useRef + useEffect pattern (same as existing Sidebar pattern)
     - **Unauthenticated state:** Keep existing Log In (ghost) + Sign Up (gradient) buttons -- already correct

2. **Update `frontend/src/styles/main.css`**:
   - Change `.main-content` from `@apply flex-1 ml-[260px] pt-[60px] p-6;` to `@apply flex-1 pt-[60px] p-6 transition-all duration-300;`
   - Remove the static `ml-[260px]` -- margin is now applied dynamically via className in AppLayout.jsx
  </action>
  <verify>
    <automated>cd frontend && npx vite build --logLevel error 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Topbar has no search trigger, no Cmd+K reference
    - Topbar left offset is dynamic based on collapsed prop
    - Hamburger button visible on mobile (lg:hidden)
    - Avatar has dropdown with "Manage Profile" and "Logout"
    - PAGE_NAMES updated (old routes removed, new routes added)
    - Unauthenticated state shows Log In + Sign Up buttons
    - main.css .main-content has no static ml-[260px], uses transition
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete layout redesign</name>
  <files>none</files>
  <action>
Run `cd frontend && npm run dev` and verify all layout states visually. This is a human verification checkpoint -- no code changes, only visual confirmation of the complete layout redesign across desktop (unauthenticated, authenticated, collapsed, expanded) and mobile (drawer open/close) states.

Verification scenarios:

**Desktop - Unauthenticated:**
1. Navigate to http://localhost:5173/ -- landing page loads, CTAs link to /ip-search
2. Click "Threat Lookup" CTA -- arrives at /ip-search with full AppLayout (sidebar + topbar)
3. Sidebar shows 6 nav items: Dashboard, IP Search, Threat Map, Dark Web, Threat Actors, Threat News
4. IP Search is the only active/clickable item; all others are grayed with a lock icon
5. Click a locked item (e.g., Dashboard) -- redirects to /login
6. Settings menu is NOT visible in sidebar
7. Topbar shows "Log In" + "Sign Up" buttons (no search trigger, no avatar)
8. No Cmd+K shortcut triggers anything

**Desktop - Sidebar Collapse:**
9. Click the collapse toggle at bottom of sidebar -- sidebar shrinks to ~64px icon-only rail
10. Main content and topbar shift left to fill space
11. Hover over collapsed sidebar -- temporarily expands as overlay showing labels
12. Move mouse away -- sidebar re-collapses
13. Refresh page -- sidebar stays collapsed (localStorage persistence)

**Mobile (resize browser to less than 1024px):**
14. Sidebar is hidden
15. Hamburger icon appears in topbar
16. Click hamburger -- sidebar slides in as overlay with dark backdrop
17. Click backdrop or X button -- sidebar closes

**Desktop - Authenticated (log in first):**
18. All nav items are active/clickable (no lock icons)
19. Settings appears with collapsible submenu -- click to expand, shows "Profile Management"
20. Topbar shows PRO badge, notification bell, avatar
21. Click avatar -- dropdown with "Manage Profile" and "Logout"
22. Click "Manage Profile" -- navigates to /settings
23. Click "Logout" -- logs out, redirects home
  </action>
  <verify>
    <automated>cd frontend && npx vite build --logLevel error 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually verified all layout states and approved the redesign</done>
</task>

</tasks>

<verification>
- Frontend builds without errors (`npm run build`)
- SearchModal.jsx is deleted
- No references to /ioc-search remain in the codebase
- Sidebar renders 6 flat nav items (no group headers)
- Collapse state persists in localStorage
- Guest users can access /ip-search without redirect
- All locked nav items redirect to /login when clicked by guest
</verification>

<success_criteria>
- Sidebar collapses to 64px rail, hover-expands, persists state
- Glassmorphism sidebar styling (semi-transparent + backdrop-blur)
- Auth-aware nav: locked items for guests, Settings hidden for guests
- Topbar: no search, dynamic offset, avatar dropdown, mobile hamburger
- Mobile: sidebar as overlay drawer with backdrop
- Logo uses logo.png, adapts to collapse
- IP Search publicly accessible with full AppLayout
- All visual states verified by user
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-layout-redesign-sidebar-topbar-and-applayout-overhaul/04.1-02-SUMMARY.md`
</output>
