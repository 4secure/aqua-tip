---
phase: 27-outfit-font-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/main.css
  - frontend/tailwind.config.js
autonomous: true
requirements:
  - TYPO-01
  - TYPO-03
  - TYPO-04

must_haves:
  truths:
    - "Google Fonts import loads Outfit (100-900) and JetBrains Mono (400/500/600) only"
    - "Tailwind font-sans resolves to Outfit, font-mono resolves to JetBrains Mono"
    - "No font-display, font-heading, or font-body tokens exist in Tailwind config"
    - "Base CSS body uses font-sans, h1/h2/h3 use font-sans with font-semibold"
  artifacts:
    - path: "frontend/tailwind.config.js"
      provides: "Font token definitions (sans + mono only)"
      contains: "sans: ['Outfit'"
    - path: "frontend/src/styles/main.css"
      provides: "Google Fonts import and base layer font rules"
      contains: "family=Outfit:wght@100..900"
  key_links:
    - from: "frontend/src/styles/main.css"
      to: "Google Fonts CDN"
      via: "@import URL"
      pattern: "fonts.googleapis.com.*Outfit"
    - from: "frontend/src/styles/main.css"
      to: "frontend/tailwind.config.js"
      via: "@apply font-sans"
      pattern: "font-sans"
---

<objective>
Update font configuration foundation: Google Fonts import, Tailwind config, and base CSS rules to establish Outfit as the primary font family.

Purpose: This must happen BEFORE class replacements (Plan 02) so that `font-sans` resolves to Outfit when old token classes are replaced.
Output: Working font config where any element using `font-sans` renders in Outfit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-outfit-font-migration/27-CONTEXT.md
@.planning/phases/27-outfit-font-migration/27-RESEARCH.md

@frontend/tailwind.config.js
@frontend/src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Google Fonts import and Tailwind font config</name>
  <files>frontend/src/styles/main.css, frontend/tailwind.config.js</files>
  <read_first>
    - frontend/src/styles/main.css (current @import URL on line 1, base layer rules lines 31-42)
    - frontend/tailwind.config.js (current fontFamily block lines 45-50)
  </read_first>
  <action>
**Step 1: Update Google Fonts @import in main.css (line 1)**

Replace the entire first line:
```
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&family=Space+Grotesk:wght@400;500;600;700&family=Syne:wght@600;700;800&display=swap');
```
With:
```
@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Outfit:wght@100..900&display=swap');
```
Per D-01 (Outfit 100-900), D-02 (remove Syne, Space Grotesk, Inter), D-03 (keep JetBrains Mono unchanged).

**Step 2: Update fontFamily in tailwind.config.js (lines 45-50)**

Replace the entire `fontFamily` block:
```javascript
fontFamily: {
  display: ['Syne', 'sans-serif'],
  heading: ['Space Grotesk', 'sans-serif'],
  mono: ['JetBrains Mono', 'monospace'],
  body: ['Inter', 'sans-serif'],
},
```
With:
```javascript
fontFamily: {
  sans: ['Outfit', 'sans-serif'],
  mono: ['JetBrains Mono', 'monospace'],
},
```
Per D-04 (consolidate to 2 tokens: font-sans + font-mono), D-05 (remove font-display, font-heading, font-body).

**Step 3: Update base layer rules in main.css (lines 31-38)**

Replace the body rule (line 31-33):
```css
body {
  @apply bg-primary text-text-primary font-body antialiased;
  min-height: 100vh;
}
```
With:
```css
body {
  @apply bg-primary text-text-primary font-sans antialiased;
  min-height: 100vh;
}
```

Replace the heading rule (lines 36-38):
```css
h1, h2, h3 {
  @apply font-heading;
}
```
With:
```css
h1, h2, h3 {
  @apply font-sans font-semibold;
}
```
Per D-08 (headings 600-700) and D-09 (base CSS h1-h3 use font-sans with font-weight).

The code/mono rule (lines 40-42) stays UNCHANGED per D-03.

**Step 4: Update .section-title in main.css components layer (line 55)**

Replace:
```css
.section-title {
  @apply text-lg font-heading font-semibold text-text-primary mb-4;
}
```
With:
```css
.section-title {
  @apply text-lg font-sans font-semibold text-text-primary mb-4;
}
```
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - main.css line 1 contains `family=Outfit:wght@100..900`
    - main.css line 1 contains `family=JetBrains+Mono:wght@400;500;600`
    - main.css line 1 does NOT contain `Inter` or `Syne` or `Space+Grotesk`
    - tailwind.config.js fontFamily contains `sans: ['Outfit', 'sans-serif']`
    - tailwind.config.js fontFamily contains `mono: ['JetBrains Mono', 'monospace']`
    - tailwind.config.js fontFamily does NOT contain `display:` or `heading:` or `body:`
    - main.css body rule contains `font-sans` (not `font-body`)
    - main.css h1/h2/h3 rule contains `font-sans font-semibold` (not `font-heading`)
    - main.css .section-title contains `font-sans` (not `font-heading`)
    - main.css code/.mono rule still contains `font-mono` (unchanged)
    - `npm run build` succeeds with exit code 0
  </acceptance_criteria>
  <done>Google Fonts loads only Outfit + JetBrains Mono. Tailwind config has exactly 2 font tokens (sans, mono). Base CSS rules use font-sans for body and headings, font-mono for code. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd C:/laragon/www/aqua-tip/frontend && npm run build` exits 0
2. Grep confirms no old font tokens in config: `grep -E "font-display|font-heading|font-body" frontend/tailwind.config.js` returns 0 results
3. Grep confirms no old fonts in import: `grep -E "Syne|Space.Grotesk|'Inter'" frontend/src/styles/main.css` returns 0 results (excluding comments)
</verification>

<success_criteria>
- Build succeeds
- Google Fonts import loads exactly Outfit + JetBrains Mono
- Tailwind config defines exactly font-sans (Outfit) and font-mono (JetBrains Mono)
- Base layer correctly applies font-sans to body and headings
</success_criteria>

<output>
After completion, create `.planning/phases/27-outfit-font-migration/27-01-SUMMARY.md`
</output>
