---
phase: 27-outfit-font-migration
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - frontend/src/pages/ThreatSearchPage.jsx
  - frontend/src/pages/ThreatActorsPage.jsx
  - frontend/src/pages/DashboardPage.jsx
  - frontend/src/pages/ThreatNewsPage.jsx
  - frontend/src/pages/FeedsPage.jsx
  - frontend/src/pages/ComponentsPage.jsx
  - frontend/src/pages/CtiReportPage.jsx
  - frontend/src/pages/VulnScannerPage.jsx
  - frontend/src/pages/LandingPage.jsx
  - frontend/src/pages/SettingsPage.jsx
  - frontend/src/pages/CveDetailPage.jsx
  - frontend/src/pages/DarkWebPage.jsx
  - frontend/src/pages/DomainReportPage.jsx
  - frontend/src/pages/ResetPasswordPage.jsx
  - frontend/src/pages/EulaPage.jsx
  - frontend/src/pages/PrivacyPolicyPage.jsx
  - frontend/src/pages/ForgotPasswordPage.jsx
  - frontend/src/pages/PricingPage.jsx
  - frontend/src/pages/RegisterPage.jsx
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/GetStartedPage.jsx
  - frontend/src/pages/VerifyEmailPage.jsx
  - frontend/src/pages/CtiSearchPage.jsx
  - frontend/src/components/landing/LandingScroll.jsx
  - frontend/src/components/pricing/PlanCard.jsx
  - frontend/src/components/pricing/PlanConfirmModal.jsx
  - frontend/src/components/threat-map/ThreatMapCounters.jsx
  - frontend/src/components/ui/GradientButton.jsx
  - frontend/src/components/ui/Globe.jsx
  - frontend/src/components/layout/NotificationDrawer.jsx
  - frontend/src/components/layout/Sidebar.jsx
  - frontend/src/components/layout/Topbar.jsx
  - frontend/src/styles/components.css
  - CLAUDE.md
autonomous: false
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04

must_haves:
  truths:
    - "No JSX or CSS file in frontend/src/ contains font-display, font-heading, or font-body classes"
    - "No JSX or CSS file in frontend/src/ contains hardcoded Syne or Space Grotesk font-family strings"
    - "All headings render in Outfit (visually verified)"
    - "All body/UI text renders in Outfit (visually verified)"
    - "Code blocks and data tables still render in JetBrains Mono"
    - "CLAUDE.md Fonts section documents Outfit + JetBrains Mono (not old fonts)"
  artifacts:
    - path: "frontend/src/styles/components.css"
      provides: "Fixed hardcoded font-family"
      contains: "'Outfit'"
    - path: "frontend/src/pages/DashboardPage.jsx"
      provides: "Fixed Chart.js and Leaflet font references"
      contains: "'Outfit'"
    - path: "frontend/src/pages/ComponentsPage.jsx"
      provides: "Updated font showcase text content"
      contains: "Outfit"
    - path: "CLAUDE.md"
      provides: "Updated font documentation"
      contains: "Outfit"
  key_links:
    - from: "all JSX files"
      to: "frontend/tailwind.config.js"
      via: "font-sans class"
      pattern: "font-sans"
---

<objective>
Replace all old font class names (font-display, font-heading, font-body) with font-sans across 33 files, fix 3 hardcoded font-family strings, update ComponentsPage showcase text, and update CLAUDE.md documentation.

Purpose: Complete the migration started in Plan 01. After this plan, zero references to old fonts remain anywhere in the codebase.
Output: Fully migrated codebase with Outfit as the sole display/body font.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-outfit-font-migration/27-CONTEXT.md
@.planning/phases/27-outfit-font-migration/27-RESEARCH.md
@.planning/phases/27-outfit-font-migration/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk replace font classes and fix hardcoded font-family strings</name>
  <files>
    All 33 JSX files listed in files_modified above,
    frontend/src/styles/components.css
  </files>
  <read_first>
    - frontend/src/styles/components.css (line 32 has hardcoded 'Space Grotesk')
    - frontend/src/pages/DashboardPage.jsx (line 126 Chart.js font, line 452 Leaflet popup font)
    - frontend/src/pages/ComponentsPage.jsx (lines 213-214 font showcase text content)
  </read_first>
  <action>
**Step 1: Bulk class replacement across all JSX and CSS files in frontend/src/**

Use find-and-replace (sed or similar) to perform these 3 replacements across ALL `.jsx`, `.js`, and `.css` files in `frontend/src/`:

1. `font-display` -> `font-sans` (in Tailwind class contexts)
2. `font-heading` -> `font-sans` (in Tailwind class contexts)
3. `font-body` -> `font-sans` (in Tailwind class contexts)

Per D-06: Replace all 135 occurrences across 33 files. These are Tailwind utility classes used in `className` strings.

Important: Do NOT replace `font-mono` -- it must remain as-is per D-03.

**Step 2: Fix hardcoded font-family in components.css**

In `frontend/src/styles/components.css`, find the `.score-ring .score-value` rule (around line 32). Replace:
```css
font-family: 'Space Grotesk', sans-serif;
```
With:
```css
font-family: 'Outfit', sans-serif;
```

**Step 3: Fix hardcoded font-family in DashboardPage.jsx**

In `frontend/src/pages/DashboardPage.jsx`:

(a) Find the Chart.js axis tick configuration (around line 126). Replace:
```javascript
family: 'Space Grotesk'
```
With:
```javascript
family: 'Outfit'
```

(b) Find the Leaflet popup inline style (around line 452). Replace:
```javascript
font-family:'Space Grotesk'
```
With:
```javascript
font-family:'Outfit'
```

**Step 4: Update ComponentsPage.jsx font showcase text**

In `frontend/src/pages/ComponentsPage.jsx`, find the font showcase section (around lines 213-214). Update the demo text strings:

Replace text containing `"Syne Display"` with `"Outfit Display"` (keep the rest of the demo text like "AquaSecure Threat Intel").

Replace text containing `"Space Grotesk Heading"` with `"Outfit Heading"` (keep the rest like "Dashboard Overview").

Also update any font-family labels in the showcase that mention "Syne", "Space Grotesk", or "Inter" to reference "Outfit" instead. Keep JetBrains Mono references unchanged.

**Step 5: Final validation grep**

After all replacements, run these commands to confirm no old font references remain:
```bash
grep -rn "font-display\|font-heading\|font-body" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"
grep -rn "Syne\|Space Grotesk" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"
grep -rn "'Inter'" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"
```
All three commands must return 0 results.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npm run build && echo "BUILD OK" && grep -rn "font-display\|font-heading\|font-body" src/ --include="*.jsx" --include="*.js" --include="*.css" | wc -l | grep -q "^0$" && echo "NO OLD CLASSES" && grep -rn "Syne\|Space Grotesk" src/ --include="*.jsx" --include="*.js" --include="*.css" | wc -l | grep -q "^0$" && echo "NO OLD FONTS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "font-display" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
    - `grep -rn "font-heading" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
    - `grep -rn "font-body" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
    - `grep -rn "Syne" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
    - `grep -rn "Space Grotesk" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
    - `grep -rn "'Inter'" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
    - components.css contains `font-family: 'Outfit', sans-serif`
    - DashboardPage.jsx contains `family: 'Outfit'` (Chart.js tick font)
    - DashboardPage.jsx contains `font-family:'Outfit'` (Leaflet popup)
    - ComponentsPage.jsx contains `Outfit` in font showcase text (no Syne or Space Grotesk labels)
    - `npm run build` succeeds with exit code 0
  </acceptance_criteria>
  <done>Zero references to font-display, font-heading, font-body, Syne, Space Grotesk, or Inter remain in frontend/src/. All 135+ class replacements done. All 3 hardcoded font-family strings updated. ComponentsPage showcase text updated. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md font documentation</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Fonts section under Design System, around lines showing font-display, font-heading, font-mono, font-body)
  </read_first>
  <action>
**Update the Fonts section in CLAUDE.md Design System.**

Find the current fonts documentation block (lists `font-display (Syne)`, `font-heading (Space Grotesk)`, `font-mono (JetBrains Mono)`, `font-body (Inter)`).

Replace with exactly:
```
**Fonts** (loaded via CSS @import in `main.css`):
- `font-sans` (Outfit) - all headings, body text, nav, buttons, UI elements
- `font-mono` (JetBrains Mono) - code blocks, data displays, monospace text
```

Also find and update any other references in CLAUDE.md that mention the old fonts. Specifically:
- If the Tech Stack section lists font names, update to show Outfit instead of Syne/Space Grotesk/Inter
- The Google Fonts mention should reflect loading via `main.css` CSS @import (not `index.html`)
  </action>
  <verify>
    <automated>grep -c "font-sans.*Outfit" C:/laragon/www/aqua-tip/CLAUDE.md | grep -q "[1-9]" && echo "OUTFIT DOCUMENTED" && grep -c "Syne\|Space Grotesk\|font-display\|font-heading\|font-body" C:/laragon/www/aqua-tip/CLAUDE.md | grep -q "^0$" && echo "OLD FONTS REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `font-sans` and `Outfit` in the Fonts section
    - CLAUDE.md contains `font-mono` and `JetBrains Mono` in the Fonts section
    - CLAUDE.md does NOT contain `font-display` as a font token reference
    - CLAUDE.md does NOT contain `font-heading` as a font token reference
    - CLAUDE.md does NOT contain `font-body` as a font token reference
    - CLAUDE.md does NOT contain `Syne` as a font name
    - CLAUDE.md does NOT contain `Space Grotesk` as a font name
    - CLAUDE.md does NOT contain `Inter` as a font name (in font context)
  </acceptance_criteria>
  <done>CLAUDE.md Fonts section documents exactly 2 font tokens: font-sans (Outfit) and font-mono (JetBrains Mono). No references to old fonts remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Outfit font rendering</name>
  <files>N/A (visual verification only)</files>
  <action>
Start the dev server and visually verify Outfit font rendering across all pages.

What was built: Complete Outfit font migration -- Google Fonts import updated, Tailwind config consolidated to 2 tokens, 135+ class replacements across 33 files, 3 hardcoded font-family fixes, ComponentsPage showcase updated, CLAUDE.md documentation updated.

How to verify:
1. Run `cd C:/laragon/www/aqua-tip/frontend && npm run dev`
2. Open http://localhost:5173 (Landing Page) -- verify all headings and body text render in Outfit (geometric sans-serif, distinct from old fonts)
3. Navigate to /dashboard -- verify chart axis labels and card text are in Outfit, not Space Grotesk
4. Navigate to /threat-search -- verify search UI, results text all in Outfit
5. Navigate to /components -- verify font showcase section shows "Outfit" labels (not Syne/Space Grotesk)
6. On any page, inspect an element with code/monospace content -- verify it renders in JetBrains Mono
7. Spot-check 2-3 other pages (Settings, Feeds, CTI Search) for consistent Outfit rendering

Resume signal: Type "approved" or describe any pages/elements where the wrong font appears.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npm run dev -- --host 2>&1 | head -5</automated>
  </verify>
  <done>User visually confirms all pages render headings and body text in Outfit, code/data in JetBrains Mono, with no remnants of Syne, Space Grotesk, or Inter.</done>
</task>

</tasks>

<verification>
1. `cd C:/laragon/www/aqua-tip/frontend && npm run build` succeeds
2. `grep -rn "font-display\|font-heading\|font-body" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
3. `grep -rn "Syne\|Space Grotesk" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
4. `grep -rn "'Inter'" frontend/src/ --include="*.jsx" --include="*.js" --include="*.css"` returns 0 results
5. Visual verification across landing page, dashboard, threat search, and components page
</verification>

<success_criteria>
- Zero old font class names (font-display, font-heading, font-body) in any source file
- Zero hardcoded references to Syne, Space Grotesk, or Inter in any source file
- All pages visually render headings in Outfit semibold/bold, body in Outfit regular
- JetBrains Mono still used for code/data displays
- CLAUDE.md accurately documents the new font system
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/27-outfit-font-migration/27-02-SUMMARY.md`
</output>
