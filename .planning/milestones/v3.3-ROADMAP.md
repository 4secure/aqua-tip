# v3.3 Threat Map Dashboard — Milestone Archive

**Status:** ✅ SHIPPED
**Date Shipped:** 2026-04-06
**Phases:** 37-40 (4 phases, 5 plans)
**Requirements:** 12/12 satisfied
**Git Range:** `3e7eeef` → `8596523`
**Audit:** PASSED (see `v3.3-MILESTONE-AUDIT.md`)

## Milestone Goal

Replace the dashboard with an enhanced threat map at `/dashboard` — existing map features preserved, new overlay panels for stats and indicators with toggle + peek-on-hover.

## Stats

- **Phases:** 4
- **Plans:** 5
- **Timeline:** 2 days (2026-04-05 → 2026-04-06)
- **Commits:** 54
- **Files modified:** 61 (+7,066 / -3,217 lines)
- **Build:** ✅ Vite production build passes (21.82s, 0 errors)

## Key Accomplishments

1. **Route swap** — Threat map relocated to `/dashboard` with `/threat-map` redirect for backward compatibility; sidebar simplified to a single Dashboard entry.
2. **Shared dashboard config module** — Extracted `STAT_CARD_CONFIG`, `TYPE_BADGE_COLORS`, and `formatRelativeTime` into `frontend/src/data/dashboard-config.js` for cross-component reuse.
3. **Overlay panels with glassmorphism** — `LeftOverlayPanel` (7 threat database stat cards + existing map widgets) and `RightOverlayPanel` (indicators table + live feed) with event isolation for Leaflet compatibility and `PanelToggle` for unified collapse/expand.
4. **Peek-on-hover interaction** — 10px glassmorphism edge slivers when collapsed with 150ms entry / 250ms exit delays, unified hover-zone wrappers to prevent flicker, and independent left/right reveal.
5. **localStorage persistence** — Toggle state persists across refreshes under `aqua-tip:panels-collapsed` key with try/catch fallback.
6. **Dead code cleanup** — `DashboardPage.jsx` deleted (and 9 other orphaned files); `STAT_COLOR_MAP` orphan export removed; `ThreatSearchPage` now consumes shared config rather than local duplicates; grep audit confirms zero stale references.

## Phase Details

### Phase 37: Map Route Foundation
**Goal:** Users access the threat map as their main dashboard at `/dashboard` with all existing map features intact
**Requirements:** LAYOUT-01, LAYOUT-02, LAYOUT-03
**Plans:** 1/1 complete
- [x] 37-01-PLAN.md — Route swap, redirect, and sidebar nav update
**Completed:** 2026-04-05

### Phase 38: Overlay Panel Components
**Goal:** Users see threat database stats and recent indicators floating over the map with a toggle to collapse both panels
**Requirements:** PANEL-01, PANEL-02, PANEL-03, TOGGLE-01
**Plans:** 2/2 complete
- [x] 38-01-PLAN.md — Extract shared dashboard config to reusable module
- [x] 38-02-PLAN.md — Build overlay panels, toggle button, and wire into ThreatMapPage
**Completed:** 2026-04-05

### Phase 39: Peek-on-Hover Behavior
**Goal:** Users can peek at collapsed panels by hovering edge slivers, with state persisted across refreshes
**Requirements:** TOGGLE-02, TOGGLE-03, TOGGLE-04
**Plans:** 1/1 complete
- [x] 39-01-PLAN.md — localStorage persistence, peek slivers, and hover-reveal
**Completed:** 2026-04-06

### Phase 40: Cleanup & Verification
**Goal:** Dead code from the old dashboard is removed and navigation reflects the new structure
**Requirements:** CLEAN-01, CLEAN-02
**Plans:** 1/1 complete
- [x] 40-01-PLAN.md — Delete dead code, clean stale references, grep audit + build verify
**Completed:** 2026-04-06

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Route swap without deleting DashboardPage in Phase 37 | Keep revert-safe; Phase 40 handles deletion | ✓ Good |
| Shared `dashboard-config.js` in `frontend/src/data/` | Enable cross-component reuse of stat config + badge colors | ✓ Good |
| Overlay panel pattern: motion.div + AnimatePresence + 5-handler event isolation | Leaflet-compatible absolute-positioned panels | ✓ Good |
| Unified hover-zone wrapper for sliver+panel | Prevents flicker from hover gap transition | ✓ Good |
| localStorage-backed useState with try/catch | Handle storage unavailability gracefully | ✓ Good |
| `ThreatSearchPage` consumes shared config (consolidation) | Remove duplicate definitions of `formatRelativeTime` and `TYPE_BADGE_COLORS` | ✓ Good |
| Deletion-then-audit pattern in Phase 40 | Delete orphans first, grep audit confirms zero stale refs | ✓ Good |

## Tech Debt (Non-Blocking)

1. **PANEL-01 wording mismatch (ACCEPTED):** REQUIREMENTS.md says "left overlay panel" for stat cards; implementation places them in `RightOverlayPanel.jsx` "Threat Database" section. PM confirmed current implementation is canonical and wording preserved.
2. **VALIDATION.md draft status:** Phases 37, 38, 39 have `nyquist_compliant: false` because no test framework exists in the project (per CLAUDE.md). Informational only.

## Integration Verification

- Wired exports: 7 connected, 0 orphaned (after cleanup), 0 missing
- API consumers: 4/4 dashboard routes have callers
- Auth: `/dashboard` wrapped in ProtectedRoute; `/threat-map` redirect inherits protection
- E2E flows: 7/7 complete
- Vite build: ✅ 0 errors

See `v3.3-MILESTONE-AUDIT.md` for full audit details.
