---
phase: 23-creditresolver-plan-aware-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/Services/CreditResolver.php
  - backend/app/Http/Middleware/DeductCredit.php
  - backend/app/Http/Controllers/Credit/CreditStatusController.php
  - backend/app/Models/User.php
  - backend/database/migrations/2026_03_21_000003_add_pending_plan_columns_to_users.php
  - backend/tests/Feature/CreditResolverTest.php
autonomous: true
requirements: [PLAN-03, PLAN-04, PLAN-07, TRIAL-01, TRIAL-02, TRIAL-03]

must_haves:
  truths:
    - "Credit limits are derived from user's plan, not hardcoded"
    - "Trial users (no plan, trial active) get 10 credits/day"
    - "Trial expired users with no plan are auto-assigned Free plan on next credit check"
    - "Guest credit limit stays at 1/day"
    - "DeductCredit middleware delegates to CreditResolver (no internal resolveCredit/lazyReset)"
    - "CreditStatusController delegates to CreditResolver (no internal resolveCredit/lazyReset)"
    - "Lazy reset syncs credit limit from plan on new day"
    - "Pending downgrade is applied during lazy reset when plan_change_at <= today"
  artifacts:
    - path: "backend/app/Services/CreditResolver.php"
      provides: "Shared credit resolution, lazy reset, plan-aware limit derivation"
      exports: ["resolve", "lazyReset", "resolveLimit"]
    - path: "backend/database/migrations/2026_03_21_000003_add_pending_plan_columns_to_users.php"
      provides: "pending_plan_id and plan_change_at columns on users table"
    - path: "backend/tests/Feature/CreditResolverTest.php"
      provides: "Tests for plan-aware credit resolution, trial expiry, guest limits"
  key_links:
    - from: "backend/app/Http/Middleware/DeductCredit.php"
      to: "backend/app/Services/CreditResolver.php"
      via: "constructor injection"
      pattern: "__construct.*CreditResolver"
    - from: "backend/app/Http/Controllers/Credit/CreditStatusController.php"
      to: "backend/app/Services/CreditResolver.php"
      via: "constructor injection"
      pattern: "__construct.*CreditResolver"
    - from: "backend/app/Services/CreditResolver.php"
      to: "backend/app/Models/Plan.php"
      via: "plan relationship for limit derivation"
      pattern: "plan->daily_credit_limit"
---

<objective>
Extract duplicated credit resolution logic from DeductCredit middleware and CreditStatusController into a shared CreditResolver service, make credit limits plan-aware (derived from user's plan rather than hardcoded), enforce trial expiry with auto-downgrade to Free tier, and add pending_plan columns to the users table.

Purpose: Eliminate hardcoded credit limits, establish single source of truth for credit resolution, and enable plan-based credit gating that downstream plan selection API depends on.
Output: CreditResolver service class, rewired consumers, pending_plan migration, comprehensive tests.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-creditresolver-plan-aware-backend/23-CONTEXT.md
@.planning/phases/23-creditresolver-plan-aware-backend/23-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/Models/Plan.php:
```php
class Plan extends Model
{
    protected $fillable = ['slug', 'name', 'daily_credit_limit', 'price_cents', 'features', 'description', 'is_popular', 'sort_order', 'is_active'];
    // Casts: features->array, is_popular->boolean, is_active->boolean, daily_credit_limit->integer, price_cents->integer, sort_order->integer
    public function users(): HasMany;
}
```

From backend/app/Models/User.php:
```php
class User extends Authenticatable implements MustVerifyEmail
{
    protected $fillable = ['name', 'email', 'password', 'oauth_provider', 'oauth_id', 'avatar_url', 'email_verified_at', 'phone', 'onboarding_completed_at', 'trial_ends_at', 'plan_id', 'timezone', 'organization', 'role'];
    // Casts: email_verified_at->datetime, onboarding_completed_at->datetime, trial_ends_at->datetime, password->hashed
    public function plan(): BelongsTo;
    public function credit(): HasOne;
}
```

From backend/app/Models/Credit.php:
```php
class Credit extends Model
{
    protected $fillable = ['user_id', 'ip_address', 'remaining', 'limit', 'last_reset_at'];
    // Casts: last_reset_at->datetime, remaining->integer, limit->integer
    public function user(): BelongsTo;
}
```

From backend/database/seeders/PlanSeeder.php:
```
Free:  slug='free',  daily_credit_limit=3,   sort_order=1
Basic: slug='basic', daily_credit_limit=15,  sort_order=2
Pro:   slug='pro',   daily_credit_limit=50,  sort_order=3
Enterprise: slug='enterprise', daily_credit_limit=200, sort_order=4
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreditResolver service, migration, and model updates</name>
  <files>
    backend/app/Services/CreditResolver.php,
    backend/database/migrations/2026_03_21_000003_add_pending_plan_columns_to_users.php,
    backend/app/Models/User.php,
    backend/tests/Feature/CreditResolverTest.php
  </files>
  <read_first>
    backend/app/Http/Middleware/DeductCredit.php,
    backend/app/Http/Controllers/Credit/CreditStatusController.php,
    backend/app/Models/User.php,
    backend/app/Models/Plan.php,
    backend/app/Models/Credit.php,
    backend/database/seeders/PlanSeeder.php,
    backend/database/migrations/2026_03_21_000002_add_plan_columns_to_users.php
  </read_first>
  <behavior>
    - Test: resolveLimit returns 1 for null user (guest)
    - Test: resolveLimit returns 10 for user with no plan and active trial (trial_ends_at in future)
    - Test: resolveLimit returns plan daily_credit_limit for user with plan_id set (e.g., Pro user gets 50)
    - Test: resolveLimit returns Free plan limit (3) for user with no plan and expired trial (fallback)
    - Test: resolve creates Credit record with plan-derived limit for authenticated user with plan
    - Test: resolve creates Credit record with 1/1 for guest (IP-based)
    - Test: lazyReset does nothing if last_reset_at is today
    - Test: lazyReset resets remaining and limit to plan-derived value when last_reset_at is yesterday
    - Test: lazyReset calls checkTrialExpiry -- user with expired trial and no plan gets Free plan assigned
    - Test: lazyReset calls applyPendingDowngrade -- user with pending_plan_id and plan_change_at <= today gets plan_id updated
    - Test: applyPendingDowngrade does nothing if plan_change_at is in the future
  </behavior>
  <action>
    **Step 1: Write tests first (RED)**

    Create `backend/tests/Feature/CreditResolverTest.php` using Pest syntax. Run PlanSeeder in beforeEach. Tests:

    ```php
    use App\Models\Credit;
    use App\Models\Plan;
    use App\Models\User;
    use App\Services\CreditResolver;
    use Database\Seeders\PlanSeeder;

    beforeEach(function () {
        $this->seed(PlanSeeder::class);
        $this->resolver = app(CreditResolver::class);
    });
    ```

    Test cases per behavior list above. Use `User::factory()->create(['plan_id' => Plan::where('slug','pro')->first()->id])` for plan users, `User::factory()->create(['plan_id' => null, 'trial_ends_at' => now()->addDays(15)])` for trial users, `User::factory()->create(['plan_id' => null, 'trial_ends_at' => now()->subDay()])` for expired trial.

    For pending downgrade test: create user with `pending_plan_id` set to Free plan ID and `plan_change_at` set to yesterday. After lazyReset, assert `plan_id` equals Free plan ID and `pending_plan_id` is null.

    **Step 2: Create migration (GREEN prep)**

    Create `backend/database/migrations/2026_03_21_000003_add_pending_plan_columns_to_users.php`:
    - `pending_plan_id` — nullable foreignId, constrained to `plans`, nullOnDelete, placed after `plan_id`
    - `plan_change_at` — nullable timestamp, placed after `pending_plan_id`
    - `down()` drops both columns

    **Step 3: Update User model**

    Add to `backend/app/Models/User.php`:
    - Add `'pending_plan_id'` and `'plan_change_at'` to `$fillable` array
    - Add `'plan_change_at' => 'datetime'` to `casts()` return array
    - Add `pendingPlan()` relationship: `return $this->belongsTo(Plan::class, 'pending_plan_id');`

    **Step 4: Create CreditResolver service (GREEN)**

    Create `backend/app/Services/CreditResolver.php` with:
    - `TRIAL_DAILY_LIMIT = 10` constant
    - `GUEST_DAILY_LIMIT = 1` constant
    - `resolve(Request $request): Credit` — copy from DeductCredit::resolveCredit(), but use `$this->resolveLimit($user)` instead of hardcoded 10 for authenticated user initial remaining/limit
    - `lazyReset(Credit $credit, ?User $user): void` — check `last_reset_at < todayStart`. If new day: call `applyPendingDowngrade($user)`, call `checkTrialExpiry($user)`, then derive limit via `resolveLimit($user)` and update credit with new remaining/limit/last_reset_at
    - `resolveLimit(?User $user): int` — null user returns GUEST_DAILY_LIMIT (1). User with plan_id returns `$user->plan->daily_credit_limit` (loadMissing first). User with no plan + active trial returns TRIAL_DAILY_LIMIT (10). Fallback: query Free plan limit or hardcode 3
    - `private applyPendingDowngrade(User $user): void` — if `pending_plan_id !== null && plan_change_at !== null && plan_change_at->startOfDay()->lte(now('UTC')->startOfDay())`, update plan_id to pending_plan_id, clear pending_plan_id and plan_change_at, call `$user->unsetRelation('plan')`
    - `private checkTrialExpiry(User $user): void` — if `plan_id === null && trial_ends_at !== null && trial_ends_at->isPast()`, find Free plan by slug, update user plan_id, call `$user->unsetRelation('plan')`

    Exact code structure from 23-RESEARCH.md "CreditResolver Service Structure" section.

    **Step 5: Run migration**

    `cd backend && php artisan migrate`

    **Step 6: Run tests (GREEN)**

    `cd backend && php artisan test --filter=CreditResolverTest`
  </action>
  <verify>
    <automated>cd backend && php artisan test --filter=CreditResolverTest</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/Services/CreditResolver.php exists and contains `class CreditResolver`
    - CreditResolver.php contains `const TRIAL_DAILY_LIMIT = 10`
    - CreditResolver.php contains `const GUEST_DAILY_LIMIT = 1`
    - CreditResolver.php contains `public function resolve(Request $request): Credit`
    - CreditResolver.php contains `public function lazyReset(Credit $credit, ?User $user): void`
    - CreditResolver.php contains `public function resolveLimit(?User $user): int`
    - CreditResolver.php contains `private function applyPendingDowngrade(User $user): void`
    - CreditResolver.php contains `private function checkTrialExpiry(User $user): void`
    - CreditResolver.php contains `$user->plan->daily_credit_limit` (plan-derived limit)
    - backend/database/migrations/2026_03_21_000003_add_pending_plan_columns_to_users.php exists
    - Migration contains `pending_plan_id` and `plan_change_at`
    - Migration contains `nullOnDelete`
    - backend/app/Models/User.php contains `'pending_plan_id'` in fillable
    - backend/app/Models/User.php contains `'plan_change_at' => 'datetime'` in casts
    - backend/app/Models/User.php contains `function pendingPlan()` method
    - backend/tests/Feature/CreditResolverTest.php exists with 10+ test cases
    - `cd backend && php artisan test --filter=CreditResolverTest` exits 0
  </acceptance_criteria>
  <done>CreditResolver service created with plan-aware limit resolution, trial expiry auto-downgrade, pending downgrade application. Migration adds pending_plan columns. All CreditResolver tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire DeductCredit and CreditStatusController to delegate to CreditResolver</name>
  <files>
    backend/app/Http/Middleware/DeductCredit.php,
    backend/app/Http/Controllers/Credit/CreditStatusController.php
  </files>
  <read_first>
    backend/app/Http/Middleware/DeductCredit.php,
    backend/app/Http/Controllers/Credit/CreditStatusController.php,
    backend/app/Services/CreditResolver.php,
    backend/tests/Feature/AuthCreditLimitTest.php,
    backend/tests/Feature/GuestCreditLimitTest.php
  </read_first>
  <action>
    **DeductCredit.php rewire:**

    1. Add constructor injection: `public function __construct(private CreditResolver $creditResolver) {}`
    2. Add use statement: `use App\Services\CreditResolver;`
    3. In `handle()` method, replace `$credit = $this->resolveCredit($request);` with `$credit = $this->creditResolver->resolve($request);`
    4. Replace `$this->lazyReset($credit);` with `$this->creditResolver->lazyReset($credit, $request->user());`
    5. Delete the private `resolveCredit()` method entirely (lines 44-79 of current file)
    6. Delete the private `lazyReset()` method entirely (lines 81-91 of current file)
    7. Keep the atomic `DB::table('credits')->where(...)->update(...)` deduction logic UNCHANGED — CreditResolver does NOT handle deduction

    **CreditStatusController.php rewire:**

    1. Add constructor injection: `public function __construct(private CreditResolver $creditResolver) {}`
    2. Add use statement: `use App\Services\CreditResolver;`
    3. In `__invoke()` method, replace `$credit = $this->resolveCredit($request);` with `$credit = $this->creditResolver->resolve($request);`
    4. Replace `$this->lazyReset($credit);` with `$this->creditResolver->lazyReset($credit, $request->user());`
    5. Delete the private `resolveCredit()` method entirely
    6. Delete the private `lazyReset()` method entirely
    7. Keep the JSON response unchanged

    **Verify existing tests still pass** — the behavior is identical, only the code structure changed. Trial users still get 10/day, guests still get 1/day.

    Run full test suite: `cd backend && php artisan test`
  </action>
  <verify>
    <automated>cd backend && php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - DeductCredit.php contains `use App\Services\CreditResolver`
    - DeductCredit.php contains `__construct(private CreditResolver $creditResolver)`
    - DeductCredit.php contains `$this->creditResolver->resolve($request)`
    - DeductCredit.php contains `$this->creditResolver->lazyReset($credit, $request->user())`
    - DeductCredit.php does NOT contain `private function resolveCredit`
    - DeductCredit.php does NOT contain `private function lazyReset`
    - DeductCredit.php still contains `DB::table('credits')` (atomic deduction preserved)
    - CreditStatusController.php contains `use App\Services\CreditResolver`
    - CreditStatusController.php contains `__construct(private CreditResolver $creditResolver)`
    - CreditStatusController.php contains `$this->creditResolver->resolve($request)`
    - CreditStatusController.php does NOT contain `private function resolveCredit`
    - CreditStatusController.php does NOT contain `private function lazyReset`
    - `cd backend && php artisan test` exits 0 (all 111+ existing tests pass)
  </acceptance_criteria>
  <done>Both DeductCredit middleware and CreditStatusController delegate to CreditResolver. No duplicated credit logic remains. All existing tests pass without modification.</done>
</task>

</tasks>

<verification>
1. `cd backend && php artisan test --filter=CreditResolverTest` — all CreditResolver unit tests green
2. `cd backend && php artisan test` — full suite passes (111+ existing tests + new tests)
3. `grep -r "private function resolveCredit" backend/app/` returns no matches (duplication eliminated)
4. `grep -r "private function lazyReset" backend/app/` returns no matches (duplication eliminated)
5. `grep -c "CreditResolver" backend/app/Http/Middleware/DeductCredit.php` returns 2+ (import + usage)
6. `grep -c "CreditResolver" backend/app/Http/Controllers/Credit/CreditStatusController.php` returns 2+ (import + usage)
</verification>

<success_criteria>
- CreditResolver is the single source of truth for credit resolution and limit derivation
- No hardcoded credit limits (10, 1) remain outside CreditResolver
- Trial expiry auto-assigns Free plan during lazy reset
- Pending downgrade is applied during lazy reset when due
- Both consumers (DeductCredit, CreditStatusController) delegate to CreditResolver
- All existing tests pass without modification
- New CreditResolver tests cover plan-aware limits, trial expiry, guest limits, pending downgrade
</success_criteria>

<output>
After completion, create `.planning/phases/23-creditresolver-plan-aware-backend/23-01-SUMMARY.md`
</output>
