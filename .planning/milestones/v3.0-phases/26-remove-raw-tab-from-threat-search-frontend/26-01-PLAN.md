---
phase: 26-remove-raw-tab-from-threat-search-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/ThreatSearchPage.jsx
autonomous: true
requirements:
  - CLEANUP-01

must_haves:
  truths:
    - "Raw tab no longer appears in the threat search results tab bar"
    - "All other tabs (Summary, Relations, External References, Indicators, Sightings, Notes) still render correctly"
    - "No dead code (RawTab component, raw tab entry, raw tab rendering, unused Code icon import) remains in ThreatSearchPage.jsx"
  artifacts:
    - path: "frontend/src/pages/ThreatSearchPage.jsx"
      provides: "Threat search page without raw tab"
      does_not_contain: "RawTab"
  key_links:
    - from: "tabs useMemo"
      to: "tab rendering conditionals"
      via: "activeTab matching"
      pattern: "activeTab === '(summary|relations|external_refs|indicators|sightings|notes)'"
---

<objective>
Remove the "Raw" JSON debug tab from the Threat Search results page.

Purpose: The raw tab exposes unformatted API response data that end users don't need. Developers can still inspect raw responses via browser DevTools.
Output: Cleaned ThreatSearchPage.jsx with no trace of RawTab.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/pages/ThreatSearchPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove RawTab component, tab entry, and rendering conditional</name>
  <files>frontend/src/pages/ThreatSearchPage.jsx</files>
  <read_first>frontend/src/pages/ThreatSearchPage.jsx</read_first>
  <action>
Make the following deletions in frontend/src/pages/ThreatSearchPage.jsx (per D-01):

1. **Delete the RawTab component** (lines 404-412):
   Remove the entire function block:
   ```
   function RawTab({ result }) {
     return (
       <div className="glass-card p-5">
         <pre className="bg-primary rounded-lg p-4 text-xs font-mono text-text-secondary overflow-auto max-h-[500px] border border-border">
           {JSON.stringify(result.raw || result, null, 2)}
         </pre>
       </div>
     );
   }
   ```

2. **Delete the raw tab entry** from the `tabs` useMemo (line 554):
   Remove this line:
   ```
   t.push({ key: 'raw', label: 'Raw', icon: Code });
   ```

3. **Delete the raw tab rendering conditional** (line 846):
   Remove this line:
   ```
   {activeTab === 'raw' && <RawTab result={result} />}
   ```

4. **Remove the `Code` icon from the lucide-react import** (per D-02):
   On line 5, change:
   ```
   Network, FileText, Eye, StickyNote, Code, ExternalLink, Globe,
   ```
   to:
   ```
   Network, FileText, Eye, StickyNote, ExternalLink, Globe,
   ```
   The `Code` icon is only used by the raw tab entry (line 554). The lowercase `code` on lines 171-172 is a function parameter for country code flag conversion, not the lucide icon.

5. **Clean up any awkward spacing** left by the removals — ensure no double blank lines or orphaned comments remain around the deletion sites.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npx grep-cli "RawTab" src/pages/ThreatSearchPage.jsx; echo "EXIT:$?" && grep -c "Code" src/pages/ThreatSearchPage.jsx | grep -q "^2$" && echo "Code icon removed (only lowercase code refs remain)" || echo "WARN: Check Code import"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "RawTab" frontend/src/pages/ThreatSearchPage.jsx` returns 0 (no matches)
- `grep "key: 'raw'" frontend/src/pages/ThreatSearchPage.jsx` returns no matches
- `grep "activeTab === 'raw'" frontend/src/pages/ThreatSearchPage.jsx` returns no matches
- `grep "Code," frontend/src/pages/ThreatSearchPage.jsx` returns no matches (the icon import is gone)
- `grep "code\." frontend/src/pages/ThreatSearchPage.jsx` still returns matches (lowercase code param for country flags is preserved)
- `grep "activeTab === 'summary'" frontend/src/pages/ThreatSearchPage.jsx` returns a match (other tabs untouched)
- `grep "activeTab === 'notes'" frontend/src/pages/ThreatSearchPage.jsx` returns a match (other tabs untouched)
  </acceptance_criteria>
  <done>RawTab component, its tab entry, its rendering conditional, and the unused Code icon import are all removed. All other tabs render as before. No dead code remains.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds with raw tab removed</name>
  <files>frontend/src/pages/ThreatSearchPage.jsx</files>
  <read_first>frontend/src/pages/ThreatSearchPage.jsx</read_first>
  <action>
Run the Vite production build to confirm no import errors, no missing references, and no broken JSX after the removal.

```bash
cd frontend && npm run build
```

If the build fails, read the error output and fix the issue (most likely cause: a leftover reference to `RawTab` or `Code` that was missed).
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `cd frontend && npm run build` exits with code 0
- Build output contains "built in" (Vite success message)
- No warnings mentioning "RawTab" or unused "Code" import in build output
  </acceptance_criteria>
  <done>Production build succeeds cleanly with no errors or warnings related to the removal.</done>
</task>

</tasks>

<verification>
1. `grep -c "RawTab" frontend/src/pages/ThreatSearchPage.jsx` returns 0
2. `grep -c "key: 'raw'" frontend/src/pages/ThreatSearchPage.jsx` returns 0
3. `grep -c "activeTab === 'raw'" frontend/src/pages/ThreatSearchPage.jsx` returns 0
4. `grep "Code," frontend/src/pages/ThreatSearchPage.jsx` returns no matches
5. `cd frontend && npm run build` exits 0
6. Other tabs (summary, relations, external_refs, indicators, sightings, notes) still present in source
</verification>

<success_criteria>
- Raw tab is completely removed from ThreatSearchPage.jsx (component, tab entry, render conditional, icon import)
- All other tabs function as before
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/26-remove-raw-tab-from-threat-search-frontend/26-01-SUMMARY.md`
</output>
