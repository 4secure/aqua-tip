---
phase: 38-overlay-panel-components
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - frontend/src/components/threat-map/LeftOverlayPanel.jsx
  - frontend/src/components/threat-map/RightOverlayPanel.jsx
  - frontend/src/components/threat-map/PanelToggle.jsx
  - frontend/src/pages/ThreatMapPage.jsx
autonomous: true
requirements:
  - PANEL-01
  - PANEL-02
  - PANEL-03
  - TOGGLE-01

must_haves:
  truths:
    - "User sees 7 threat database stat cards stacked vertically in a left overlay panel on top of the map"
    - "User sees existing map widgets (Counters, Countries, Donut) below the stat cards in the same left panel"
    - "User sees a scrollable recent indicators table in a right overlay panel on top of the map"
    - "User sees ThreatMapFeed below the indicators table in the same right panel"
    - "Both overlay panels have glassmorphism styling (glass-card-static class)"
    - "User can click a single toggle button to collapse both panels simultaneously"
    - "Clicks and scrolls inside panels do not propagate to the map underneath"
  artifacts:
    - path: "frontend/src/components/threat-map/LeftOverlayPanel.jsx"
      provides: "Left overlay panel with stat cards and existing map widgets"
      exports: ["default"]
      min_lines: 60
    - path: "frontend/src/components/threat-map/RightOverlayPanel.jsx"
      provides: "Right overlay panel with indicators table and feed"
      exports: ["default"]
      min_lines: 60
    - path: "frontend/src/components/threat-map/PanelToggle.jsx"
      provides: "Toggle button for collapsing/expanding both panels"
      exports: ["default"]
      min_lines: 15
    - path: "frontend/src/pages/ThreatMapPage.jsx"
      provides: "Orchestrates panels, toggle state, and map"
      contains: "panelsCollapsed"
  key_links:
    - from: "frontend/src/pages/ThreatMapPage.jsx"
      to: "frontend/src/components/threat-map/LeftOverlayPanel.jsx"
      via: "import and render with collapsed prop"
      pattern: "LeftOverlayPanel.*collapsed"
    - from: "frontend/src/pages/ThreatMapPage.jsx"
      to: "frontend/src/components/threat-map/RightOverlayPanel.jsx"
      via: "import and render with collapsed prop"
      pattern: "RightOverlayPanel.*collapsed"
    - from: "frontend/src/pages/ThreatMapPage.jsx"
      to: "frontend/src/components/threat-map/PanelToggle.jsx"
      via: "import and render with onToggle prop"
      pattern: "PanelToggle.*onToggle"
    - from: "frontend/src/components/threat-map/LeftOverlayPanel.jsx"
      to: "frontend/src/data/dashboard-config.js"
      via: "import STAT_CARD_CONFIG, STAT_COLOR_MAP"
      pattern: "import.*STAT_CARD_CONFIG.*from.*dashboard-config"
    - from: "frontend/src/components/threat-map/RightOverlayPanel.jsx"
      to: "frontend/src/data/dashboard-config.js"
      via: "import TYPE_BADGE_COLORS, formatRelativeTime"
      pattern: "import.*TYPE_BADGE_COLORS.*from.*dashboard-config"
    - from: "frontend/src/components/threat-map/LeftOverlayPanel.jsx"
      to: "/api/dashboard/counts"
      via: "apiClient.get in useEffect"
      pattern: "apiClient.*dashboard/counts"
    - from: "frontend/src/components/threat-map/RightOverlayPanel.jsx"
      to: "/api/dashboard/indicators"
      via: "apiClient.get in useEffect"
      pattern: "apiClient.*dashboard/indicators"
---

<objective>
Build three new components (LeftOverlayPanel, RightOverlayPanel, PanelToggle) and wire them into ThreatMapPage to create floating glassmorphism overlay panels over the threat map at /dashboard.

Purpose: Delivers the core Phase 38 user experience -- stat cards, indicators, and a toggle to collapse/expand panels over the map. Addresses PANEL-01, PANEL-02, PANEL-03, TOGGLE-01.
Output: 3 new components in `components/threat-map/`, updated ThreatMapPage.jsx with panel orchestration.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-overlay-panel-components/38-CONTEXT.md
@.planning/phases/38-overlay-panel-components/38-RESEARCH.md
@.planning/phases/38-overlay-panel-components/38-UI-SPEC.md
@.planning/phases/38-overlay-panel-components/38-01-SUMMARY.md

<interfaces>
<!-- From dashboard-config.js (created in Plan 01): -->
export const STAT_CARD_CONFIG = [
  { entity_type: 'IPv4-Addr', label: 'IP Addresses', color: 'red' },
  { entity_type: 'Domain-Name', label: 'Domains', color: 'violet' },
  { entity_type: 'Hostname', label: 'Hostnames', color: 'cyan' },
  { entity_type: 'X509-Certificate', label: 'Certificates', color: 'amber' },
  { entity_type: 'Email-Addr', label: 'Email', color: 'amber' },
  { entity_type: 'Cryptocurrency-Wallet', label: 'Crypto Wallet', color: 'green' },
  { entity_type: 'Url', label: 'URL', color: 'violet' },
];

export const STAT_COLOR_MAP = {
  red: { bg: 'bg-red/10', text: 'text-red', border: 'border-red/20' },
  violet: { bg: 'bg-violet/10', text: 'text-violet', border: 'border-violet/20' },
  cyan: { bg: 'bg-cyan/10', text: 'text-cyan', border: 'border-cyan/20' },
  amber: { bg: 'bg-amber/10', text: 'text-amber', border: 'border-amber/20' },
  green: { bg: 'bg-green/10', text: 'text-green', border: 'border-green/20' },
};

export const TYPE_BADGE_COLORS = { /* 8 entity types with bg/text hex colors */ };

export function formatRelativeTime(dateStr) { /* returns relative time string */ }

<!-- From ThreatMapPage.jsx (current, to be modified): -->
// Props available from useThreatStream():
const { events, counters, countryCounts, typeCounts, connected } = useThreatStream();

// Existing map widgets (DO NOT MODIFY their internals):
// - ThreatMapCounters: props { counters, connected }
// - ThreatMapCountries: props { countryCounts }
// - ThreatMapDonut: props { typeCounts }
// - ThreatMapFeed: props { events, onEventClick }
// - ThreatMapStatus: props { connected } (stays independent, NOT in any panel)

<!-- From apiClient (frontend/src/api/client.js): -->
import { apiClient } from '../api/client';
// apiClient.get('/api/dashboard/counts') -> { data: { counts: { 'IPv4-Addr': 123, ... } } }
// apiClient.get('/api/dashboard/indicators') -> { data: { indicators: [...] } }

<!-- glassmorphism.css class: -->
.glass-card-static {
  background: rgba(15, 17, 23, 0.7);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(30, 32, 48, 0.8);
  border-radius: 1rem;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LeftOverlayPanel, RightOverlayPanel, and PanelToggle components</name>
  <files>
    frontend/src/components/threat-map/LeftOverlayPanel.jsx,
    frontend/src/components/threat-map/RightOverlayPanel.jsx,
    frontend/src/components/threat-map/PanelToggle.jsx
  </files>
  <read_first>
    - frontend/src/data/dashboard-config.js (shared config from Plan 01 -- import paths and export names)
    - frontend/src/pages/DashboardPage.jsx (see how stat cards and indicators are currently rendered -- lines 60-200 approx)
    - frontend/src/components/threat-map/ThreatMapFeed.jsx (existing widget embedded in right panel -- props interface)
    - frontend/src/components/threat-map/ThreatMapCounters.jsx (existing widget embedded in left panel -- props interface)
    - frontend/src/components/threat-map/ThreatMapCountries.jsx (existing widget -- props interface)
    - frontend/src/components/threat-map/ThreatMapDonut.jsx (existing widget -- props interface)
    - frontend/src/styles/glassmorphism.css (glass-card-static class definition)
    - frontend/src/api/client.js (apiClient import path)
    - .planning/phases/38-overlay-panel-components/38-UI-SPEC.md (panel layout contract, interaction contract, typography, spacing)
  </read_first>
  <action>
Create three new component files:

**1. `frontend/src/components/threat-map/LeftOverlayPanel.jsx`**

Props: `{ collapsed, counters, connected, countryCounts, typeCounts }`

Structure per D-01, D-02, D-04, D-05, UI-SPEC Panel Layout Contract:
- Wrap entire panel in `AnimatePresence` from `framer-motion`. When `collapsed` is true, panel unmounts with exit animation.
- Outermost element: `motion.div` with:
  - `initial={{ x: -20, opacity: 0 }}`, `animate={{ x: 0, opacity: 1 }}`, `exit={{ x: -20, opacity: 0 }}`
  - `transition={{ type: 'spring', stiffness: 300, damping: 30 }}`
  - className: `absolute top-4 left-4 z-[1000] w-[340px] max-h-[calc(100vh-120px)] overflow-y-auto space-y-4`
  - Event isolation handlers on the motion.div:
    ```
    onPointerDown={(e) => e.stopPropagation()}
    onWheel={(e) => e.stopPropagation()}
    onClick={(e) => e.stopPropagation()}
    onDoubleClick={(e) => e.stopPropagation()}
    onTouchStart={(e) => e.stopPropagation()}
    ```

- **Stat cards section** (per D-04, D-05, PANEL-01):
  - Wrapped in `<div className="glass-card-static p-3">`
  - Heading: `<h3 className="text-sm font-semibold text-text-secondary mb-2">Threat Database</h3>`
  - Fetch stat counts: `useState` for `counts` (object), `countsLoading` (bool, init true), `countsError` (string|null). In `useEffect` on mount, call `apiClient.get('/api/dashboard/counts')`, set `counts` from response `res.data.counts` (or `res.data` depending on API shape -- check DashboardPage for exact path). Set `countsLoading = false` on success or error.
  - Map `STAT_CARD_CONFIG` to render 7 `StatRow` inline components. Each StatRow:
    - Container: `<div className="flex items-center justify-between py-2 px-3 border-b border-border/30 last:border-0">`
    - Left side: `<div className="flex items-center gap-2">` containing:
      - Color dot: `<div className={`w-2 h-2 rounded-full bg-${config.color}`} />` (use the color name directly as Tailwind class: `bg-red`, `bg-violet`, `bg-cyan`, `bg-amber`, `bg-green`)
      - Label: `<span className="text-xs text-text-secondary">{config.label}</span>`
    - Right side: if `countsLoading`, show `<div className="h-4 w-12 bg-surface-2 rounded animate-pulse" />`; if `countsError`, show `<span className="text-sm font-mono text-text-muted">---</span>`; else show `<span className="text-sm font-mono font-semibold text-text-primary">{(counts[config.entity_type] || 0).toLocaleString()}</span>`
    - Height: approximately 40px per row (py-2 = 8px top + 8px bottom + content ~24px = ~40px) per D-04.

- **Existing widgets below stat cards** (per D-01):
  - `<ThreatMapCounters counters={counters} connected={connected} />`
  - `<ThreatMapCountries countryCounts={countryCounts} />`
  - `<ThreatMapDonut typeCounts={typeCounts} />`
  - Each rendered as a direct child in the `space-y-4` container (they already have their own glass-card-static styling).

Import `{ useState, useEffect }` from 'react', `{ AnimatePresence, motion }` from 'framer-motion', `{ apiClient }` from '../../api/client', `{ STAT_CARD_CONFIG }` from '../../data/dashboard-config', and the three existing widget components.

**2. `frontend/src/components/threat-map/RightOverlayPanel.jsx`**

Props: `{ collapsed, events, onEventClick }`

Structure per D-03, D-06, D-07, PANEL-02, UI-SPEC:
- Wrap in `AnimatePresence`. When `collapsed` is true, panel unmounts.
- Outermost: `motion.div` with:
  - `initial={{ x: 20, opacity: 0 }}`, `animate={{ x: 0, opacity: 1 }}`, `exit={{ x: 20, opacity: 0 }}`
  - `transition={{ type: 'spring', stiffness: 300, damping: 30 }}`
  - className: `absolute top-4 right-4 z-[1000] w-[380px] max-h-[calc(100vh-120px)] overflow-y-auto space-y-4`
  - Same 5 event isolation handlers as LeftOverlayPanel

- **Indicators section** (per D-06, D-07, PANEL-02):
  - Wrapped in `<div className="glass-card-static p-3">`
  - Heading: `<h3 className="text-sm font-semibold text-text-secondary mb-2">Recent Indicators</h3>`
  - Fetch indicators: `useState` for `indicators` (array), `indicatorsLoading` (bool, init true), `indicatorsError` (string|null). In `useEffect` on mount, call `apiClient.get('/api/dashboard/indicators')`, set `indicators` from response (check DashboardPage for exact response path).
  - Loading state: 5 skeleton rows with `<div className="h-6 bg-surface-2 rounded animate-pulse" style={{ width: '${w}%' }} />` where widths cycle through [60, 80, 50, 70, 65].
  - Error state: `<p className="text-xs text-text-muted text-center py-4">Failed to load indicators</p>`
  - Empty state: `<p className="text-xs text-text-muted text-center py-4">No indicators found</p>`
  - Success state: map indicators to rows. Each row:
    - Container: `<div className="flex items-center justify-between gap-2 py-1.5">`
    - Left: `<div className="flex items-center gap-2 min-w-0">`
      - Badge: `<span className="inline-block px-2 py-0.5 rounded text-[10px] font-mono font-semibold shrink-0" style={{ backgroundColor: badge.bg, color: badge.text }}>{ind.entity_type}</span>` where `badge = TYPE_BADGE_COLORS[ind.entity_type] || { bg: '#7A44E425', text: '#7A44E4' }`
      - Value: `<span className="font-mono text-xs text-text-primary truncate">{ind.value}</span>`
    - Right: `<span className="text-[10px] text-text-muted whitespace-nowrap shrink-0">{formatRelativeTime(ind.created_at)}</span>`

- **ThreatMapFeed below indicators** (per D-03):
  - `<ThreatMapFeed events={events} onEventClick={onEventClick} />`
  - Rendered as direct child in `space-y-4` container.

Import `{ useState, useEffect }` from 'react', `{ AnimatePresence, motion }` from 'framer-motion', `{ apiClient }` from '../../api/client', `{ TYPE_BADGE_COLORS, formatRelativeTime }` from '../../data/dashboard-config', and `ThreatMapFeed`.

**3. `frontend/src/components/threat-map/PanelToggle.jsx`**

Props: `{ collapsed, onToggle }`

Per D-08, D-09, TOGGLE-01, UI-SPEC Toggle Button:
- Import `{ PanelLeftClose, PanelLeftOpen }` from 'lucide-react'
- Render a `<button>` with:
  - `onClick={(e) => { e.stopPropagation(); onToggle(); }}`
  - className: `absolute bottom-4 left-1/2 -translate-x-1/2 z-[1000] glass-card-static px-3 py-2 rounded-full cursor-pointer hover:border-violet/30 transition-colors`
  - Event isolation: `onPointerDown`, `onWheel`, `onDoubleClick`, `onTouchStart` all with `e.stopPropagation()`
  - Content: `{collapsed ? <PanelLeftOpen className="w-4 h-4 text-text-secondary" /> : <PanelLeftClose className="w-4 h-4 text-text-secondary" />}`
- Export as default.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/threat-map/LeftOverlayPanel.jsx` exists, exports default function
    - LeftOverlayPanel.jsx contains `AnimatePresence` and `motion.div`
    - LeftOverlayPanel.jsx contains `Threat Database` heading text
    - LeftOverlayPanel.jsx contains `apiClient.get` call to `/api/dashboard/counts`
    - LeftOverlayPanel.jsx contains `STAT_CARD_CONFIG` import from `../../data/dashboard-config`
    - LeftOverlayPanel.jsx contains `stopPropagation` (at least 5 occurrences for event isolation)
    - LeftOverlayPanel.jsx contains `ThreatMapCounters`, `ThreatMapCountries`, `ThreatMapDonut` renders
    - LeftOverlayPanel.jsx contains `w-[340px]` and `max-h-[calc(100vh-120px)]`
    - `frontend/src/components/threat-map/RightOverlayPanel.jsx` exists, exports default function
    - RightOverlayPanel.jsx contains `AnimatePresence` and `motion.div`
    - RightOverlayPanel.jsx contains `Recent Indicators` heading text
    - RightOverlayPanel.jsx contains `apiClient.get` call to `/api/dashboard/indicators`
    - RightOverlayPanel.jsx contains `TYPE_BADGE_COLORS` and `formatRelativeTime` imports from `../../data/dashboard-config`
    - RightOverlayPanel.jsx contains `ThreatMapFeed` render
    - RightOverlayPanel.jsx contains `w-[380px]`
    - RightOverlayPanel.jsx contains `Failed to load indicators` error text
    - `frontend/src/components/threat-map/PanelToggle.jsx` exists, exports default function
    - PanelToggle.jsx contains `PanelLeftClose` and `PanelLeftOpen` from lucide-react
    - PanelToggle.jsx contains `glass-card-static` and `rounded-full`
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>All three overlay components created with proper styling, data fetching, event isolation, and animation</done>
</task>

<task type="auto">
  <name>Task 2: Wire overlay panels into ThreatMapPage</name>
  <files>frontend/src/pages/ThreatMapPage.jsx</files>
  <read_first>
    - frontend/src/pages/ThreatMapPage.jsx (current state -- understand existing layout at lines 72-91)
    - frontend/src/components/threat-map/LeftOverlayPanel.jsx (just created -- verify props interface)
    - frontend/src/components/threat-map/RightOverlayPanel.jsx (just created -- verify props interface)
    - frontend/src/components/threat-map/PanelToggle.jsx (just created -- verify props interface)
  </read_first>
  <action>
Modify `frontend/src/pages/ThreatMapPage.jsx`:

1. **Add imports** (keep existing imports, add these):
   ```js
   import { useState } from 'react';  // add useState to existing { useRef, useEffect, useCallback } import
   import LeftOverlayPanel from '../components/threat-map/LeftOverlayPanel';
   import RightOverlayPanel from '../components/threat-map/RightOverlayPanel';
   import PanelToggle from '../components/threat-map/PanelToggle';
   ```

2. **Add toggle state** inside `ThreatMapPage` function, after the `useThreatStream()` line:
   ```js
   const [panelsCollapsed, setPanelsCollapsed] = useState(false);
   ```

3. **Replace the existing left sidebar and bottom-right feed sections** (lines 78-88 in current file):

   DELETE these blocks:
   ```jsx
   {/* Left sidebar overlay */}
   <div className="absolute top-4 left-4 z-[1000] w-[340px] space-y-4">
     <ThreatMapCounters counters={counters} connected={connected} />
     <ThreatMapCountries countryCounts={countryCounts} />
     <ThreatMapDonut typeCounts={typeCounts} />
   </div>

   {/* Bottom-right feed */}
   <div className="absolute bottom-4 right-4 z-[1000] w-[380px]">
     <ThreatMapFeed events={events} onEventClick={handleEventClick} />
   </div>
   ```

   REPLACE with:
   ```jsx
   <LeftOverlayPanel
     collapsed={panelsCollapsed}
     counters={counters}
     connected={connected}
     countryCounts={countryCounts}
     typeCounts={typeCounts}
   />

   <RightOverlayPanel
     collapsed={panelsCollapsed}
     events={events}
     onEventClick={handleEventClick}
   />

   <PanelToggle
     collapsed={panelsCollapsed}
     onToggle={() => setPanelsCollapsed((prev) => !prev)}
   />
   ```

4. **Remove unused direct imports** of ThreatMapCounters, ThreatMapCountries, ThreatMapDonut, ThreatMapFeed from the top of the file (they are now imported inside the panel components). Keep ThreatMapStatus import -- it stays independent.

5. **Keep everything else unchanged**: `addPulseMarker`, `addHighlightPulse`, `useLeaflet`, `useThreatStream`, `handleEventClick`, the map container div, and `ThreatMapStatus`.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ThreatMapPage.jsx contains `import LeftOverlayPanel from '../components/threat-map/LeftOverlayPanel'`
    - ThreatMapPage.jsx contains `import RightOverlayPanel from '../components/threat-map/RightOverlayPanel'`
    - ThreatMapPage.jsx contains `import PanelToggle from '../components/threat-map/PanelToggle'`
    - ThreatMapPage.jsx contains `useState(false)` for panelsCollapsed
    - ThreatMapPage.jsx contains `<LeftOverlayPanel` with `collapsed={panelsCollapsed}`
    - ThreatMapPage.jsx contains `<RightOverlayPanel` with `collapsed={panelsCollapsed}`
    - ThreatMapPage.jsx contains `<PanelToggle` with `onToggle=`
    - ThreatMapPage.jsx does NOT contain direct `<ThreatMapCounters` render (moved to LeftOverlayPanel)
    - ThreatMapPage.jsx does NOT contain direct `<ThreatMapFeed` render (moved to RightOverlayPanel)
    - ThreatMapPage.jsx STILL contains `<ThreatMapStatus` (stays independent)
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>
  <done>ThreatMapPage orchestrates both overlay panels and toggle with single panelsCollapsed state, build passes, all existing map functionality preserved</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- LeftOverlayPanel renders stat cards with "Threat Database" heading + existing widgets below
- RightOverlayPanel renders indicators table with "Recent Indicators" heading + ThreatMapFeed below
- PanelToggle renders at bottom-center with glassmorphism pill styling
- ThreatMapPage passes panelsCollapsed to both panels and toggle
- All panels use AnimatePresence for collapse/expand animation
- All panels block event propagation (5 handlers each)
- PANEL-01: 7 stat cards visible in left panel
- PANEL-02: Indicators table in scrollable right panel
- PANEL-03: glass-card-static styling on panel sections
- TOGGLE-01: Single toggle collapses/expands both panels
</verification>

<success_criteria>
- All 3 new components created and wired into ThreatMapPage
- Toggle button collapses and expands both panels simultaneously
- Panels float over the map with glassmorphism styling
- Event propagation isolated -- panel interactions don't affect map
- Build succeeds with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-overlay-panel-components/38-02-SUMMARY.md`
</output>
