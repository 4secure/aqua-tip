---
phase: 38-overlay-panel-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/data/dashboard-config.js
  - frontend/src/pages/DashboardPage.jsx
autonomous: true
requirements:
  - PANEL-01
  - PANEL-02

must_haves:
  truths:
    - "Shared config module exports STAT_CARD_CONFIG, STAT_COLOR_MAP, TYPE_BADGE_COLORS, formatRelativeTime"
    - "DashboardPage imports these constants from the shared module instead of defining them locally"
    - "DashboardPage renders identically after refactor (no visual change)"
  artifacts:
    - path: "frontend/src/data/dashboard-config.js"
      provides: "Shared stat card config, color maps, and utility functions"
      exports: ["STAT_CARD_CONFIG", "STAT_COLOR_MAP", "TYPE_BADGE_COLORS", "formatRelativeTime"]
    - path: "frontend/src/pages/DashboardPage.jsx"
      provides: "Updated imports from shared module"
      contains: "from '../data/dashboard-config'"
  key_links:
    - from: "frontend/src/data/dashboard-config.js"
      to: "frontend/src/pages/DashboardPage.jsx"
      via: "named imports"
      pattern: "import.*STAT_CARD_CONFIG.*from.*dashboard-config"
---

<objective>
Extract shared data configuration (stat card config, color maps, formatRelativeTime) from DashboardPage.jsx into a dedicated shared module so both DashboardPage and the new overlay panel components can import them.

Purpose: Avoid duplicating config constants across DashboardPage and overlay panels (per Pitfall 3 from RESEARCH.md). Creates the shared data foundation that Plan 02 depends on.
Output: `frontend/src/data/dashboard-config.js` with all shared exports; DashboardPage updated to import from it.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-overlay-panel-components/38-CONTEXT.md
@.planning/phases/38-overlay-panel-components/38-UI-SPEC.md

<interfaces>
<!-- From DashboardPage.jsx lines 12-57: constants and functions to extract -->
const STAT_CARD_CONFIG = [
  { entity_type: 'IPv4-Addr', label: 'IP Addresses', color: 'red' },
  { entity_type: 'Domain-Name', label: 'Domains', color: 'violet' },
  { entity_type: 'Hostname', label: 'Hostnames', color: 'cyan' },
  { entity_type: 'X509-Certificate', label: 'Certificates', color: 'amber' },
  { entity_type: 'Email-Addr', label: 'Email', color: 'amber' },
  { entity_type: 'Cryptocurrency-Wallet', label: 'Crypto Wallet', color: 'green' },
  { entity_type: 'Url', label: 'URL', color: 'violet' },
];

const STAT_COLOR_MAP = {
  red: { bg: 'bg-red/10', text: 'text-red', border: 'border-red/20' },
  violet: { bg: 'bg-violet/10', text: 'text-violet', border: 'border-violet/20' },
  cyan: { bg: 'bg-cyan/10', text: 'text-cyan', border: 'border-cyan/20' },
  amber: { bg: 'bg-amber/10', text: 'text-amber', border: 'border-amber/20' },
  green: { bg: 'bg-green/10', text: 'text-green', border: 'border-green/20' },
};

const TYPE_BADGE_COLORS = {
  'IPv4-Addr':        { bg: '#FF3B5C25', text: '#FF3B5C' },
  'IPv6-Addr':        { bg: '#FF3B5C25', text: '#FF3B5C' },
  'Domain-Name':      { bg: '#00E5FF25', text: '#00E5FF' },
  'Url':              { bg: '#7A44E425', text: '#7A44E4' },
  'Email-Addr':       { bg: '#FFB02025', text: '#FFB020' },
  'StixFile':         { bg: '#00C48C25', text: '#00C48C' },
  'Hostname':         { bg: '#9B6BF725', text: '#9B6BF7' },
  'X509-Certificate': { bg: '#FFB02025', text: '#FFB020' },
};

function formatRelativeTime(dateStr) {
  if (!dateStr) return '';
  const now = new Date();
  const date = new Date(dateStr);
  const diffMs = now - date;
  const diffMin = Math.floor(diffMs / 60000);
  if (diffMin < 1) return 'Just now';
  if (diffMin < 60) return `${diffMin}m ago`;
  const diffHr = Math.floor(diffMin / 60);
  if (diffHr < 24) return `${diffHr}h ago`;
  const diffDay = Math.floor(diffHr / 24);
  return `${diffDay}d ago`;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared dashboard config module</name>
  <files>frontend/src/data/dashboard-config.js</files>
  <read_first>
    - frontend/src/pages/DashboardPage.jsx (source of constants to extract, lines 12-57)
    - frontend/src/data/mock-data.js (see naming conventions in data/ directory)
  </read_first>
  <action>
Create `frontend/src/data/dashboard-config.js` with the following named exports, copied verbatim from DashboardPage.jsx lines 12-57:

```js
export const STAT_CARD_CONFIG = [
  { entity_type: 'IPv4-Addr', label: 'IP Addresses', color: 'red' },
  { entity_type: 'Domain-Name', label: 'Domains', color: 'violet' },
  { entity_type: 'Hostname', label: 'Hostnames', color: 'cyan' },
  { entity_type: 'X509-Certificate', label: 'Certificates', color: 'amber' },
  { entity_type: 'Email-Addr', label: 'Email', color: 'amber' },
  { entity_type: 'Cryptocurrency-Wallet', label: 'Crypto Wallet', color: 'green' },
  { entity_type: 'Url', label: 'URL', color: 'violet' },
];

export const STAT_COLOR_MAP = {
  red: { bg: 'bg-red/10', text: 'text-red', border: 'border-red/20' },
  violet: { bg: 'bg-violet/10', text: 'text-violet', border: 'border-violet/20' },
  cyan: { bg: 'bg-cyan/10', text: 'text-cyan', border: 'border-cyan/20' },
  amber: { bg: 'bg-amber/10', text: 'text-amber', border: 'border-amber/20' },
  green: { bg: 'bg-green/10', text: 'text-green', border: 'border-green/20' },
};

export const TYPE_BADGE_COLORS = {
  'IPv4-Addr':        { bg: '#FF3B5C25', text: '#FF3B5C' },
  'IPv6-Addr':        { bg: '#FF3B5C25', text: '#FF3B5C' },
  'Domain-Name':      { bg: '#00E5FF25', text: '#00E5FF' },
  'Url':              { bg: '#7A44E425', text: '#7A44E4' },
  'Email-Addr':       { bg: '#FFB02025', text: '#FFB020' },
  'StixFile':         { bg: '#00C48C25', text: '#00C48C' },
  'Hostname':         { bg: '#9B6BF725', text: '#9B6BF7' },
  'X509-Certificate': { bg: '#FFB02025', text: '#FFB020' },
};

export function formatRelativeTime(dateStr) {
  if (!dateStr) return '';
  const now = new Date();
  const date = new Date(dateStr);
  const diffMs = now - date;
  const diffMin = Math.floor(diffMs / 60000);
  if (diffMin < 1) return 'Just now';
  if (diffMin < 60) return `${diffMin}m ago`;
  const diffHr = Math.floor(diffMin / 60);
  if (diffHr < 24) return `${diffHr}h ago`;
  const diffDay = Math.floor(diffHr / 24);
  return `${diffDay}d ago`;
}
```

Do NOT include `CATEGORY_COLORS` -- that is only used by DashboardPage's chart and should stay there.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && node -e "const m = await import('./src/data/dashboard-config.js'); console.log(Object.keys(m).join(',')); if(!m.STAT_CARD_CONFIG || !m.STAT_COLOR_MAP || !m.TYPE_BADGE_COLORS || !m.formatRelativeTime) throw new Error('missing exports'); if(m.STAT_CARD_CONFIG.length !== 7) throw new Error('wrong count'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/data/dashboard-config.js` exists
    - File contains `export const STAT_CARD_CONFIG` with exactly 7 items
    - File contains `export const STAT_COLOR_MAP` with keys: red, violet, cyan, amber, green
    - File contains `export const TYPE_BADGE_COLORS` with keys: IPv4-Addr, IPv6-Addr, Domain-Name, Url, Email-Addr, StixFile, Hostname, X509-Certificate
    - File contains `export function formatRelativeTime(dateStr)`
    - File does NOT contain `CATEGORY_COLORS`
  </acceptance_criteria>
  <done>Shared config module exports all 4 items, importable by both DashboardPage and new overlay components</done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardPage to import from shared module</name>
  <files>frontend/src/pages/DashboardPage.jsx</files>
  <read_first>
    - frontend/src/pages/DashboardPage.jsx (full file -- need to see all usages of the 4 constants)
    - frontend/src/data/dashboard-config.js (just created -- verify exports match)
  </read_first>
  <action>
In `frontend/src/pages/DashboardPage.jsx`:

1. Add import at the top (after existing imports):
   ```js
   import { STAT_CARD_CONFIG, STAT_COLOR_MAP, TYPE_BADGE_COLORS, formatRelativeTime } from '../data/dashboard-config';
   ```

2. Delete the local declarations (lines 12-57 in current file):
   - Delete `const STAT_CARD_CONFIG = [...]` (lines 12-20)
   - Delete `const STAT_COLOR_MAP = {...]` (lines 22-28)
   - Delete `const TYPE_BADGE_COLORS = {...]` (lines 30-39)
   - Delete `function formatRelativeTime(dateStr) {...}` (lines 45-57)
   - Keep `const CATEGORY_COLORS = [...]` (line 41-43) -- this stays local

3. Do NOT change any component code, JSX, or other logic. Only the import source changes.

Verify that `CATEGORY_COLORS` remains defined locally in DashboardPage.jsx (it is only used there for the chart).
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.jsx contains `import { STAT_CARD_CONFIG, STAT_COLOR_MAP, TYPE_BADGE_COLORS, formatRelativeTime } from '../data/dashboard-config'`
    - DashboardPage.jsx does NOT contain `const STAT_CARD_CONFIG =` (local declaration removed)
    - DashboardPage.jsx does NOT contain `const STAT_COLOR_MAP =` (local declaration removed)
    - DashboardPage.jsx does NOT contain `const TYPE_BADGE_COLORS =` (local declaration removed)
    - DashboardPage.jsx does NOT contain `function formatRelativeTime` (local declaration removed)
    - DashboardPage.jsx STILL contains `const CATEGORY_COLORS =` (kept local)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>DashboardPage imports shared config from dashboard-config.js, no local duplicates, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `frontend/src/data/dashboard-config.js` exists with 4 named exports
- `frontend/src/pages/DashboardPage.jsx` imports from `../data/dashboard-config`
- No duplicate constant definitions across files
</verification>

<success_criteria>
- Shared config module created and importable
- DashboardPage refactored to use shared imports
- Build succeeds with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-overlay-panel-components/38-01-SUMMARY.md`
</output>
