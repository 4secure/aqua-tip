---
phase: 40-cleanup-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/DashboardPage.jsx
  - frontend/src/pages/ComponentsPage.jsx
  - frontend/src/pages/CtiSearchPage.jsx
  - frontend/src/pages/CtiReportPage.jsx
  - frontend/src/pages/CveDetailPage.jsx
  - frontend/src/pages/DomainReportPage.jsx
  - frontend/src/pages/FeedsPage.jsx
  - frontend/src/pages/VulnScannerPage.jsx
  - frontend/src/hooks/useKeyboardShortcut.js
  - frontend/src/components/shared/BreachCard.jsx
  - frontend/src/components/layout/Topbar.jsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
must_haves:
  truths:
    - "DashboardPage.jsx does not exist on disk"
    - "No orphaned page/hook/component files remain in frontend/src/"
    - "Topbar PAGE_NAMES has no /threat-map entry"
    - "Sidebar navigation shows single Dashboard link at /dashboard"
    - "Vite production build succeeds with zero broken imports"
  artifacts:
    - path: "frontend/src/components/layout/Topbar.jsx"
      provides: "Clean PAGE_NAMES without stale /threat-map entry"
      contains: "'/dashboard': 'Dashboard'"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "frontend/src/pages/ThreatMapPage.jsx"
      via: "route /dashboard"
      pattern: "path.*dashboard"
    - from: "frontend/src/data/dashboard-config.js"
      to: "frontend/src/components/threat-map/LeftOverlayPanel.jsx"
      via: "import"
      pattern: "from.*dashboard-config"
---

<objective>
Delete all dead code from the old dashboard and orphaned files, clean stale route references, and verify the codebase has zero broken imports or stale references.

Purpose: Complete the v3.3 milestone by removing dead code left behind after the dashboard-to-threat-map migration (Phases 37-39). Ensures no orphaned files inflate the bundle or confuse future development.
Output: 10 dead files deleted, Topbar.jsx cleaned, verified clean build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-cleanup-verification/40-CONTEXT.md
@.planning/phases/40-cleanup-verification/40-RESEARCH.md

@frontend/src/components/layout/Topbar.jsx
@frontend/src/App.jsx
@frontend/src/data/mock-data.js

<interfaces>
<!-- Protected files that MUST NOT be deleted (per D-03) -->
From frontend/src/data/dashboard-config.js:
  - Exports: STAT_CARD_CONFIG, STAT_COLOR_MAP, TYPE_BADGE_COLORS, formatRelativeTime
  - Imported by: LeftOverlayPanel.jsx, RightOverlayPanel.jsx

<!-- Topbar PAGE_NAMES object (line 7-15) to edit -->
From frontend/src/components/layout/Topbar.jsx:
```javascript
const PAGE_NAMES = {
  '/dashboard': 'Dashboard',
  '/threat-search': 'Threat Search',
  '/threat-map': 'Threat Map',    // <-- DELETE this line
  '/dark-web': 'Dark Web',
  '/threat-actors': 'Threat Actors',
  '/threat-news': 'Threat News',
  '/settings': 'Settings',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned files and clean Topbar stale entry</name>
  <read_first>
    - frontend/src/components/layout/Topbar.jsx (current PAGE_NAMES to edit)
    - frontend/src/App.jsx (confirm no imports of files being deleted)
    - frontend/src/data/dashboard-config.js (confirm this is NOT being deleted -- D-03)
  </read_first>
  <files>
    frontend/src/pages/DashboardPage.jsx,
    frontend/src/pages/ComponentsPage.jsx,
    frontend/src/pages/CtiSearchPage.jsx,
    frontend/src/pages/CtiReportPage.jsx,
    frontend/src/pages/CveDetailPage.jsx,
    frontend/src/pages/DomainReportPage.jsx,
    frontend/src/pages/FeedsPage.jsx,
    frontend/src/pages/VulnScannerPage.jsx,
    frontend/src/hooks/useKeyboardShortcut.js,
    frontend/src/components/shared/BreachCard.jsx,
    frontend/src/components/layout/Topbar.jsx
  </files>
  <action>
    **Per D-01:** Delete `frontend/src/pages/DashboardPage.jsx`.

    **Per D-02:** Delete 7 additional orphaned page files (confirmed dead in research -- zero imports found):
    - `frontend/src/pages/ComponentsPage.jsx`
    - `frontend/src/pages/CtiSearchPage.jsx`
    - `frontend/src/pages/CtiReportPage.jsx`
    - `frontend/src/pages/CveDetailPage.jsx`
    - `frontend/src/pages/DomainReportPage.jsx`
    - `frontend/src/pages/FeedsPage.jsx`
    - `frontend/src/pages/VulnScannerPage.jsx`

    **Per D-02:** Delete 1 orphaned hook and 1 orphaned component:
    - `frontend/src/hooks/useKeyboardShortcut.js`
    - `frontend/src/components/shared/BreachCard.jsx`

    **Edit Topbar.jsx** (per D-05 stale reference cleanup): Remove the `'/threat-map': 'Threat Map',` line from the PAGE_NAMES object (line 10). The resulting PAGE_NAMES should be:
    ```javascript
    const PAGE_NAMES = {
      '/dashboard': 'Dashboard',
      '/threat-search': 'Threat Search',
      '/dark-web': 'Dark Web',
      '/threat-actors': 'Threat Actors',
      '/threat-news': 'Threat News',
      '/settings': 'Settings',
    };
    ```

    **Do NOT delete** (per D-03): `frontend/src/data/dashboard-config.js` -- it is actively used by overlay panels.
    **Do NOT remove** the `/threat-map` redirect route in App.jsx -- it is intentional for bookmarked URLs.
  </action>
  <verify>
    <automated>cd frontend && ls src/pages/DashboardPage.jsx 2>/dev/null && echo "FAIL: DashboardPage still exists" || echo "PASS: DashboardPage deleted" && ls src/pages/ComponentsPage.jsx 2>/dev/null && echo "FAIL" || echo "PASS" && ls src/hooks/useKeyboardShortcut.js 2>/dev/null && echo "FAIL" || echo "PASS" && ls src/components/shared/BreachCard.jsx 2>/dev/null && echo "FAIL" || echo "PASS" && grep -c "threat-map" src/components/layout/Topbar.jsx && echo "FAIL: stale threat-map in Topbar" || echo "PASS: no threat-map in Topbar"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/DashboardPage.jsx` does not exist
    - `frontend/src/pages/ComponentsPage.jsx` does not exist
    - `frontend/src/pages/CtiSearchPage.jsx` does not exist
    - `frontend/src/pages/CtiReportPage.jsx` does not exist
    - `frontend/src/pages/CveDetailPage.jsx` does not exist
    - `frontend/src/pages/DomainReportPage.jsx` does not exist
    - `frontend/src/pages/FeedsPage.jsx` does not exist
    - `frontend/src/pages/VulnScannerPage.jsx` does not exist
    - `frontend/src/hooks/useKeyboardShortcut.js` does not exist
    - `frontend/src/components/shared/BreachCard.jsx` does not exist
    - `frontend/src/data/dashboard-config.js` STILL exists (protected per D-03)
    - Topbar.jsx PAGE_NAMES contains `'/dashboard': 'Dashboard'`
    - Topbar.jsx PAGE_NAMES does NOT contain `threat-map`
    - grep for `threat-map` in Topbar.jsx returns 0 matches
  </acceptance_criteria>
  <done>10 dead files deleted, Topbar PAGE_NAMES cleaned of stale /threat-map entry, dashboard-config.js preserved</done>
</task>

<task type="auto">
  <name>Task 2: Grep audit and build verification</name>
  <read_first>
    - frontend/src/App.jsx (confirm /threat-map redirect still present)
    - frontend/src/data/mock-data.js (confirm single Dashboard nav entry per D-04)
  </read_first>
  <files>None (verification only)</files>
  <action>
    **Per D-05:** Run grep audit across `frontend/src/` for all deleted file references. Each grep MUST return zero results:

    1. `grep -r "DashboardPage" frontend/src/` -- must be empty
    2. `grep -r "ComponentsPage" frontend/src/` -- must be empty
    3. `grep -r "CtiSearchPage" frontend/src/` -- must be empty
    4. `grep -r "CtiReportPage" frontend/src/` -- must be empty
    5. `grep -r "CveDetailPage" frontend/src/` -- must be empty
    6. `grep -r "DomainReportPage" frontend/src/` -- must be empty
    7. `grep -r "FeedsPage" frontend/src/` -- must be empty
    8. `grep -r "VulnScannerPage" frontend/src/` -- must be empty
    9. `grep -r "useKeyboardShortcut" frontend/src/` -- must be empty
    10. `grep -r "BreachCard" frontend/src/` -- must be empty

    **Per D-04 / CLEAN-02:** Verify sidebar nav is correct:
    11. `grep -n "Dashboard" frontend/src/data/mock-data.js` -- must show exactly one "Dashboard" entry with path `/dashboard`
    12. `grep -n "Threat Map" frontend/src/data/mock-data.js` -- must be empty (no separate Threat Map nav entry)

    **Build verification:** Run `cd frontend && npm run build` to confirm zero broken imports and successful production build.

    **Per D-06:** Do NOT modify any files in `.planning/`.
    **Per D-07:** Do NOT audit config files (vite.config.js, tailwind.config.js).

    If any grep returns results, fix the reference (remove import, delete reference line) before proceeding. If the build fails, investigate and fix the broken import.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "DashboardPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "ComponentsPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "CtiSearchPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "CtiReportPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "CveDetailPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "DomainReportPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "FeedsPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "VulnScannerPage" frontend/src/` returns empty (exit code 1)
    - `grep -r "useKeyboardShortcut" frontend/src/` returns empty (exit code 1)
    - `grep -r "BreachCard" frontend/src/` returns empty (exit code 1)
    - `grep "Threat Map" frontend/src/data/mock-data.js` returns empty (exit code 1)
    - `npm run build` exits with code 0 and outputs "build" success message
  </acceptance_criteria>
  <done>All 10 grep audits return zero matches, sidebar has single Dashboard entry, Vite build succeeds</done>
</task>

</tasks>

<verification>
1. All 10 orphaned files deleted from disk
2. Topbar.jsx PAGE_NAMES has no /threat-map entry
3. Grep audit of frontend/src/ returns zero matches for all deleted file names
4. mock-data.js NAV_CATEGORIES has single "Dashboard" entry at /dashboard, no "Threat Map"
5. `npm run build` succeeds (zero broken imports)
6. dashboard-config.js is preserved (D-03)
7. /threat-map redirect in App.jsx is preserved
</verification>

<success_criteria>
- CLEAN-01: DashboardPage.jsx deleted, zero references remain in codebase
- CLEAN-02: Sidebar shows single "Dashboard" at /dashboard, no separate "Threat Map" link
- Vite production build succeeds
- Full grep audit passes for all 10 deleted files
</success_criteria>

<output>
After completion, create `.planning/phases/40-cleanup-verification/40-01-SUMMARY.md`
</output>
