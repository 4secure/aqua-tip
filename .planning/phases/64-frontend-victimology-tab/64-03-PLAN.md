---
phase: 64-frontend-victimology-tab
plan: 03
type: execute
wave: 3
depends_on:
  - "64-01"
  - "64-02"
files_modified:
  - .planning/phases/64-frontend-victimology-tab/64-VALIDATION.md
autonomous: true
requirements:
  - VICTM-01
  - VICTM-02
  - VICTM-03
  - VICTM-04
  - VICTM-05
  - VICTM-06
  - VICTM-07
  - VICTM-08
user_setup: []
tags:
  - verification
  - validation
  - manual-qa
  - end-to-end-grep
  - source-audit

must_haves:
  truths:
    - "An end-to-end grep audit of frontend/src/pages/ThreatActorsPage.jsx confirms all 8 VICTM requirements are satisfied at code level"
    - "Vite build (cd frontend && npm run build) exits 0 after both Plan 01 and Plan 02 land — full file is parseable + bundleable"
    - "No CSS file in frontend/src/styles/ was modified across the entire phase (D-15) — `git diff --stat HEAD~5 HEAD -- frontend/src/styles/` returns empty"
    - "No new files were created in frontend/src/components/ or frontend/src/utils/ across the entire phase (D-14) — `git status` shows no untracked .jsx/.js anywhere outside the single edited file"
    - "Zero new dependencies — `git diff HEAD~5 HEAD -- frontend/package.json frontend/package-lock.json` returns empty (D-07)"
    - "`.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md` exists and contains a manual QA checklist mapping each of SC1..SC4 to a reproducible browser test scenario"
    - "SC4 fetch-count probe (Gate 14) confirms exactly ONE network-fetch call site exists in ThreatActorsPage.jsx — proves no extra fetch was wired into the Victimology tab during Plan 01 or Plan 02"
  artifacts:
    - path: ".planning/phases/64-frontend-victimology-tab/64-VALIDATION.md"
      provides: "Manual QA checklist for Phase 64 covering SC1..SC4 + per-requirement grep probes + DevTools Network panel scenario for SC4"
      contains: "Manual QA"
      min_lines: 80
  key_links:
    - from: ".planning/phases/64-frontend-victimology-tab/64-VALIDATION.md"
      to: "ROADMAP.md Phase 64 Success Criteria SC1..SC4"
      via: "checklist items map 1:1 to SC1..SC4"
      pattern: "SC[1-4]"
    - from: "Phase verification commit"
      to: "Plan 01 + Plan 02 outcomes"
      via: "verification-only commit produces zero source diff per Phase 62-02 precedent"
      pattern: "docs\\(64-03\\)"
---

<objective>
Wave 3 verification for Phase 64 Victimology Tab: run a single end-to-end source audit of `frontend/src/pages/ThreatActorsPage.jsx` to prove all 8 VICTM requirements are satisfied at code level, prove Vite still builds cleanly, prove no CSS / new file / new dep regressions, and create `64-VALIDATION.md` as the manual QA scaffold for the human walk-through that verifies SC1..SC4 against the live UI.

Purpose: Mirror the Phase 62-02 / Phase 63-03 precedent — a verification-only plan that produces zero source diff but commits a `64-VALIDATION.md` document the human follows during manual QA. This separates the "code is correct" gate (machine-verifiable greps + Vite build) from the "user experience is correct" gate (eyes-on-screen against a real OpenCTI-backed deployment).

Output: One file created (`.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md`). No edits to `frontend/src/`. The `git diff` for this plan should show only the new VALIDATION.md document.

This plan implements Phase 62-02 / Phase 63-03 verification-plan precedent: machine gates run automatically (single Task 1 grep audit + Vite build); manual gates land as a `64-VALIDATION.md` checklist for the human (Task 2). No actor-facing code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-frontend-victimology-tab/64-CONTEXT.md
@.planning/phases/64-frontend-victimology-tab/64-01-SUMMARY.md
@.planning/phases/64-frontend-victimology-tab/64-02-SUMMARY.md
@CLAUDE.md
@frontend/src/pages/ThreatActorsPage.jsx

<interfaces>
Phase 64 Success Criteria (from ROADMAP.md Phase 64 §Success Criteria — verbatim, the manual QA targets):

1. **SC1**: Opening a Threat Actor modal shows a "Victimology" tab and no "Campaigns" tab — the tab bar has exactly 5 tabs: Overview, Relationships, TTPs, Tools, Victimology.
2. **SC2**: Clicking the Victimology tab on an actor with known targets renders four sections: Targeted Countries (with flag icons), Targeted Regions, Targeted Sectors, and Targeted Organizations — at least one section is non-empty for any major actor (e.g., APT28).
3. **SC3**: A target type with zero results (e.g., no Regions data) renders an empty-state message like "No regions data available" — no JS error, no blank section, no layout breakage.
4. **SC4**: Switching between Overview and Victimology tabs multiple times does not trigger additional network requests — enrichment is fetched once on modal open and cached in component state.

Phase 64 Requirements (from REQUIREMENTS.md §VICTM — the per-requirement grep targets):

- VICTM-01: Campaigns tab removed
- VICTM-02: Victimology tab added
- VICTM-03: Countries with flag icons + counts
- VICTM-04: Regions
- VICTM-05: Sectors
- VICTM-06: Organizations
- VICTM-07: lazy-fetch on tab open (satisfied at SPIRIT level — see CONTEXT.md D-01 + SC4)
- VICTM-08: Per-section friendly empty states

Existing Phase 62-02 / Phase 63-03 verification plan precedent:
- A "Verification" task that produces zero source diff (per STATE.md §Decisions Phase 62-02: "Verification Task 3 produced zero diff — pure quality gate (Vite build + hook-lock diff + dep-drift diff + scope check + end-to-end grep probe + legacy-literal grep + VALIDATION.md existence). No file changes, no commit.")
- A separate task that creates the `{phase}-VALIDATION.md` document for human walk-through

Per Phase 62-02 / 63-03 lesson: verification-only tasks that produce zero diff do NOT generate a task commit. The VALIDATION.md creation is the only task in this plan that produces a commit (it adds a new file).

Existing fetch contract (from Phase 56 ThreatActorsPage.jsx, line ~543):
- The single eager fetch is `fetchThreatActorEnrichment(actor.id)` inside the `useEffect` keyed on `actor.id`
- After Plan 64-01 + 64-02 the file should still contain EXACTLY ONE network call site — Gate 14 below proves this directly via a count probe (any value ≥ 2 means a regression added a second fetch)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: End-to-end source audit + Vite build + scope/CSS/dep regression checks (zero-diff verification gate)</name>
  <files></files>
  <read_first>
    - frontend/src/pages/ThreatActorsPage.jsx (re-read fresh after Plan 01 + Plan 02 commits — the file is the single source of truth for all VICTM-01..08 grep probes below)
    - .planning/phases/64-frontend-victimology-tab/64-01-SUMMARY.md (Plan 01 outcomes)
    - .planning/phases/64-frontend-victimology-tab/64-02-SUMMARY.md (Plan 02 outcomes)
    - .planning/phases/64-frontend-victimology-tab/64-CONTEXT.md (D-01..D-15 — the normative spec)
    - .planning/ROADMAP.md §"Phase 64: Frontend Victimology Tab" (SC1..SC4)
  </read_first>
  <behavior>
    - This task is a pure quality gate — it produces ZERO source-file changes and ZERO commits (per Phase 62-02 / 63-03 precedent for verification-only tasks)
    - All gates must pass; if any fail, the executor pauses and reports back rather than committing
    - Gate 1 — Vite build clean: `cd frontend && npm run build` exits 0 after both Plan 01 and Plan 02 commits are in HEAD
    - Gate 2 — VICTM-01 (Campaigns removed): `grep -c "Campaigns\\|campaigns\\|\\bFlag\\b" frontend/src/pages/ThreatActorsPage.jsx` returns 0
    - Gate 3 — VICTM-02 (Victimology added): `grep -q "label: 'Victimology'"` exits 0; `grep -q "icon: Target"` exits 0; `grep -q "activeTab === 'victimology'"` exits 0
    - Gate 4 — VICTM-03 (Countries section + flags): `grep -q "Targeted Countries ("`, `grep -qF "countryCodeToFlag(country.country_code)"`, `grep -q "0x1F1E6"` all exit 0 (use -F fixed-string mode for the optional-chaining `?.` substring so '?' / '.' are literal characters)
    - Gate 5 — VICTM-04 (Regions section): `grep -q "Targeted Regions ("`, `grep -q "<Map size={16}"`, `grep -qF "enrichment.victimology.regions"` all exit 0
    - Gate 6 — VICTM-05 (Sectors section): `grep -q "Targeted Sectors ("`, `grep -q "<Building2 size={16}"`, `grep -qF "enrichment.victimology.sectors"` all exit 0
    - Gate 7 — VICTM-06 (Organizations section): `grep -q "Targeted Organizations ("`, `grep -q "<Users size={16}"`, `grep -qF "enrichment.victimology.organizations"` all exit 0
    - Gate 8 — VICTM-07 (lazy at spirit level — guard present): `grep -q "activeTab === 'victimology' && !enrichError"` exits 0 (the `&&` short-circuit means React skips the entire JSX subtree until the user clicks the tab — no fetch needed because Phase 59 backend already includes victimology in the eager payload per D-01)
    - Gate 9 — VICTM-08 (per-section empty states): all 4 of `grep -q "No countries data available"`, `grep -q "No regions data available"`, `grep -q "No sectors data available"`, `grep -q "No organizations data available"` exit 0
    - Gate 10 — D-14 single-file scope: `git diff --name-only HEAD~5 HEAD -- frontend/src/` returns exactly `frontend/src/pages/ThreatActorsPage.jsx` (HEAD~5 covers the 5 task commits in Plan 01 + Plan 02: 3 from Plan 01 + 2 from Plan 02)
    - Gate 11 — D-15 no CSS edits: `git diff --stat HEAD~5 HEAD -- frontend/src/styles/` returns EMPTY
    - Gate 12 — D-07 zero new deps: `git diff HEAD~5 HEAD -- frontend/package.json frontend/package-lock.json` returns EMPTY
    - Gate 13 — No new component / utility files (D-14): `git status --porcelain | grep -E "^.. frontend/src/(components|utils|hooks)/"` returns EMPTY for any new .jsx or .js file related to Phase 64 (the only acceptable untracked files are pre-existing ones from outside Phase 64 scope, e.g., `backend/tmp_upgrade_pro.sql` from the working tree at session start)
    - Gate 14 (NEW — strengthens SC4 machine probe): `grep -c "fetchThreatActorEnrichment\\|new EventSource\\|fetch(" frontend/src/pages/ThreatActorsPage.jsx` returns EXACTLY 1 (the existing line-543 eager fetch). Any value ≥ 2 indicates a second fetch was wired in (e.g., a Phase 64 regression like `useEffect(() => fetch(...), [activeTab])` would push this to 2+). This is a direct machine probe of SC4 ("no extra requests on tab switch") that catches the regression Gate 8's guard-only check would miss.
    - This task does NOT commit — verification-only per Phase 62-02 / 63-03 precedent. If all gates pass, proceed to Task 2 (which IS the commit-producing task for this plan).
  </behavior>
  <action>
    Run the verification gate sequence from the repo root. Each gate either passes (proceed) or fails (pause, report back to user with the specific failing gate + diagnostic output).

    ─── Gate 1: Vite build clean ───

    ```
    cd C:/laragon/www/aqua-tip/frontend && npm run build
    ```

    Expected: exit 0, ~25–35s, no warnings. If fails: report the build error verbatim and pause (Plan 01 or Plan 02 left the file unbuildable — investigate before proceeding).

    ─── Gate 2: VICTM-01 — Campaigns + Flag fully purged ───

    ```
    cd C:/laragon/www/aqua-tip
    grep -c "campaigns" frontend/src/pages/ThreatActorsPage.jsx
    # Expected: 0
    grep -c "Campaigns" frontend/src/pages/ThreatActorsPage.jsx
    # Expected: 0
    grep -c "\\bFlag\\b" frontend/src/pages/ThreatActorsPage.jsx
    # Expected: 0
    ```

    All three must return 0. If any returns non-zero: report which references remain + their line numbers.

    ─── Gate 3: VICTM-02 — Victimology tab in TABS array + content block ───

    ```
    cd C:/laragon/www/aqua-tip
    grep -q "label: 'Victimology'" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "key: 'victimology'" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "icon: Target" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "activeTab === 'victimology'" frontend/src/pages/ThreatActorsPage.jsx
    grep -c "key: '" frontend/src/pages/ThreatActorsPage.jsx
    # Expected: exactly 5 (overview, relationships, ttps, tools, victimology)
    ```

    All exit 0; tab-key count is exactly 5.

    ─── Gate 4: VICTM-03 — Countries + flag emoji helper consumed ───

    ```
    cd C:/laragon/www/aqua-tip
    grep -q "function countryCodeToFlag" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "0x1F1E6" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "String.fromCodePoint" frontend/src/pages/ThreatActorsPage.jsx
    # Use -F fixed-string mode so the '?' optional-chaining char and '.' member-access char are
    # treated literally, not as regex metachars (zero-or-one quantifier and any-char).
    grep -qF "countryCodeToFlag(country.country_code)" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "Targeted Countries (" frontend/src/pages/ThreatActorsPage.jsx
    grep -qF "enrichment.victimology.countries" frontend/src/pages/ThreatActorsPage.jsx
    ```

    All exit 0.

    ─── Gates 5–7: VICTM-04..06 — Regions / Sectors / Organizations sections ───

    ```
    cd C:/laragon/www/aqua-tip
    # VICTM-04 Regions (use -F for literal '.' in object access)
    grep -q "Targeted Regions (" frontend/src/pages/ThreatActorsPage.jsx
    grep -qF "enrichment.victimology.regions" frontend/src/pages/ThreatActorsPage.jsx
    # VICTM-05 Sectors
    grep -q "Targeted Sectors (" frontend/src/pages/ThreatActorsPage.jsx
    grep -qF "enrichment.victimology.sectors" frontend/src/pages/ThreatActorsPage.jsx
    # VICTM-06 Organizations
    grep -q "Targeted Organizations (" frontend/src/pages/ThreatActorsPage.jsx
    grep -qF "enrichment.victimology.organizations" frontend/src/pages/ThreatActorsPage.jsx
    # All 4 section icons at header (size=16)
    grep -q "<Globe size={16}" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "<Map size={16}" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "<Building2 size={16}" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "<Users size={16}" frontend/src/pages/ThreatActorsPage.jsx
    ```

    All exit 0.

    ─── Gate 8: VICTM-07 spirit — render guard present ───

    ```
    cd C:/laragon/www/aqua-tip
    grep -q "activeTab === 'victimology' && !enrichError" frontend/src/pages/ThreatActorsPage.jsx
    ```

    Exit 0. The `&&` short-circuit guarantees React renders no Victimology JSX subtree until the user clicks the tab. The eager fetch (D-01 + SC4) is preserved because Phase 59 includes victimology in the existing payload — no separate lazy fetch is needed for the spirit of VICTM-07 to hold (no rendering work happens before tab open).

    ─── Gate 9: VICTM-08 — all 4 per-section empty-state captions present ───

    ```
    cd C:/laragon/www/aqua-tip
    grep -q "No countries data available" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "No regions data available" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "No sectors data available" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "No organizations data available" frontend/src/pages/ThreatActorsPage.jsx
    ```

    All exit 0.

    ─── Gates 10–13: D-07 / D-14 / D-15 architectural guarantees (machine-verifiable) ───

    ```
    cd C:/laragon/www/aqua-tip

    # D-14: single-file scope across the whole phase (5 commits = 3 from Plan 01 + 2 from Plan 02)
    git diff --name-only HEAD~5 HEAD -- frontend/src/ | sort -u
    # Expected: exactly one line — "frontend/src/pages/ThreatActorsPage.jsx"

    # D-15: no CSS file edits anywhere in the phase
    git diff --stat HEAD~5 HEAD -- frontend/src/styles/
    # Expected: empty output

    # D-07: zero new deps
    git diff HEAD~5 HEAD -- frontend/package.json frontend/package-lock.json
    # Expected: empty output

    # D-14: no new component / utility files created
    git diff --name-only HEAD~5 HEAD -- 'frontend/src/components/**' 'frontend/src/utils/**' 'frontend/src/hooks/**'
    # Expected: empty output (no new files; no modifications to existing component/util/hook files)
    ```

    All four checks return empty / single-file as specified.

    ─── Gate 14 (NEW): SC4 fetch-count probe — exactly ONE network call site exists ───

    The previous Gate 8 only verifies the `{activeTab === 'victimology' && ...}` guard exists, which doesn't prove no extra fetch was added. A regression like `useEffect(() => fetch(...), [activeTab])` would still pass Gate 8 (the guard is unchanged) but would silently break SC4 ("no additional network requests on tab switch"). Gate 14 catches this directly:

    ```
    cd C:/laragon/www/aqua-tip
    grep -c "fetchThreatActorEnrichment\\|new EventSource\\|fetch(" frontend/src/pages/ThreatActorsPage.jsx
    # Expected: EXACTLY 1 (the existing line-543 eager fetch — `fetchThreatActorEnrichment(actor.id)`)
    # FAIL if ≥ 2 — a second fetch was wired in (e.g., a regression like `useEffect(() => fetch(...), [activeTab])`)
    # FAIL if 0 — the existing line-543 fetch was accidentally removed
    ```

    If this returns anything other than `1`, pause and investigate via:

    ```
    cd C:/laragon/www/aqua-tip
    grep -n "fetchThreatActorEnrichment\\|new EventSource\\|fetch(" frontend/src/pages/ThreatActorsPage.jsx
    # Show line number(s) of all matches
    ```

    Expected line: 1 line in the 540-555 region (the eager fetch in the actor.id-keyed useEffect).

    ─── If all 14 gates pass ───

    Report verbatim to the executor log:

    ```
    Phase 64 verification gates: 14/14 PASS
    - Vite build: PASS
    - VICTM-01..08: all 8 requirements satisfied at code level
    - D-07 zero new deps: PASS (package.json + package-lock.json byte-identical)
    - D-14 single-file scope: PASS (only ThreatActorsPage.jsx modified)
    - D-15 no CSS edits: PASS (frontend/src/styles/ untouched)
    - No new component/util/hook files: PASS
    - SC4 fetch-count: PASS (exactly 1 network call site — the existing eager fetch)

    Proceed to Task 2 (create 64-VALIDATION.md).
    ```

    ─── If any gate fails ───

    Pause execution. Report verbatim:

    ```
    Phase 64 verification gate FAILED: Gate {N}
    {diagnostic output}

    Investigate via:
    - cd C:/laragon/www/aqua-tip && git log --oneline HEAD~5..HEAD
    - cd C:/laragon/www/aqua-tip && git diff HEAD~5 HEAD -- frontend/src/pages/ThreatActorsPage.jsx | head -200

    DO NOT commit Task 2 until the failure is resolved.
    ```

    ─── No commit ───

    This task produces zero source diff and creates no commits — per Phase 62-02 / 63-03 precedent for pure quality-gate tasks.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip && grep -c "campaigns\|Campaigns\|\bFlag\b" frontend/src/pages/ThreatActorsPage.jsx | grep -q "^0$" && grep -q "label: 'Victimology'" frontend/src/pages/ThreatActorsPage.jsx && grep -q "function countryCodeToFlag" frontend/src/pages/ThreatActorsPage.jsx && grep -q "0x1F1E6" frontend/src/pages/ThreatActorsPage.jsx && grep -q "Targeted Countries (" frontend/src/pages/ThreatActorsPage.jsx && grep -q "Targeted Regions (" frontend/src/pages/ThreatActorsPage.jsx && grep -q "Targeted Sectors (" frontend/src/pages/ThreatActorsPage.jsx && grep -q "Targeted Organizations (" frontend/src/pages/ThreatActorsPage.jsx && grep -q "No countries data available" frontend/src/pages/ThreatActorsPage.jsx && grep -q "No regions data available" frontend/src/pages/ThreatActorsPage.jsx && grep -q "No sectors data available" frontend/src/pages/ThreatActorsPage.jsx && grep -q "No organizations data available" frontend/src/pages/ThreatActorsPage.jsx && grep -c "fetchThreatActorEnrichment\|new EventSource\|fetch(" frontend/src/pages/ThreatActorsPage.jsx | grep -q "^1$" && cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 14 gates listed in the action section pass with the expected outputs (Gate 14 specifically returns exactly `1` for the fetch-count probe)
    - `cd frontend && npm run build` exits 0
    - This task produces ZERO source-file changes and ZERO commits (verification-only — per Phase 62-02 / 63-03 precedent)
    - `git status --porcelain` after this task is identical to before (no new untracked files attributable to this task)
    - If any gate fails, the task pauses and reports back rather than continuing to Task 2
  </acceptance_criteria>
  <done>
    All 8 VICTM requirements verified at code level via grep probes; Vite build green; D-07/D-14/D-15 architectural guarantees verified via git diff; SC4 fetch-count probe (Gate 14) confirms exactly one network-fetch call site exists. No source changes, no commits. Ready for Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 64-VALIDATION.md (manual QA scaffold for SC1..SC4 + per-requirement grep probes)</name>
  <files>.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 64: Frontend Victimology Tab" (SC1..SC4 — the manual QA targets)
    - .planning/REQUIREMENTS.md §VICTM (VICTM-01..08 — the per-requirement grep targets)
    - .planning/phases/63-frontend-marker-clustering/63-VALIDATION.md (precedent for VALIDATION.md structure if it exists; otherwise use the Phase 62-02 SUMMARY's verification-section structure as the template)
  </read_first>
  <behavior>
    - A new file `.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md` is created with the structure: front-matter / overview / Manual QA section (SC1..SC4 with reproducible browser steps) / Machine probes section (per-requirement grep + build + Gate 14 fetch-count) / Sign-off section
    - The file is ~120 lines; the manual QA section is the load-bearing content (the human walks through it against a deployed instance — `https://tip.aquasecure.ai/threat-actors` per CLAUDE.md)
    - The file references specific actor identifiers (e.g., APT28) that the human can search for in the live UI to trigger non-empty victimology data
    - For SC4 (no extra network requests on tab switch), the manual QA includes a DevTools Network panel scenario: open the modal, click Victimology, click Overview, click Victimology again — confirm only ONE `/api/threat-actors/{id}/enrichment` request fires
    - The Machine probes section is a verbatim copy of Task 1's gate sequence (including Gate 14 fetch-count probe) — the human can re-run it locally without reading the plan
    - The Sign-off section has 4 unchecked checkboxes (one per SC) for the human to tick as they verify
    - This file is committed in a single commit at the end of this task
    - No `frontend/src/` files are touched
  </behavior>
  <action>
    Create `.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md` with the content below (use the Write tool — this is a new file, no Read needed first because the file does not yet exist).

    File content (use as the exact body of the Write call):

    ```markdown
    # Phase 64: Frontend Victimology Tab — VALIDATION

    **Created:** Plan 64-03 (Wave 3 verification)
    **Phase:** 64-frontend-victimology-tab
    **Status:** Pending manual QA against live deploy
    **Live URL:** https://tip.aquasecure.ai/threat-actors

    ## Overview

    This document is the manual QA checklist for Phase 64. Plan 64-03 Task 1 already proved the 8 VICTM requirements are satisfied at code level (grep probes + Vite build + fetch-count probe). This file is what the human follows in a real browser to confirm SC1..SC4 hold against a real OpenCTI-backed deployment.

    The phase is considered SHIPPED when all 4 SC checkboxes in §Sign-off are ticked.

    ## Manual QA

    ### SC1 — Tab bar shows exactly 5 tabs (Overview, Relationships, TTPs, Tools, Victimology)

    1. Visit https://tip.aquasecure.ai/threat-actors (login as a Pro plan user — feature-gated)
    2. Click any actor card to open the detail modal
    3. Visually confirm the tab bar:
       - Tab 1: "Overview" with Info icon
       - Tab 2: "Relationships" with GitBranch icon
       - Tab 3: "TTPs" with Swords icon
       - Tab 4: "Tools" with Bug icon
       - Tab 5: "Victimology" with Target icon
    4. Confirm NO "Campaigns" tab appears anywhere
    5. Default active tab is Overview (D-03 verbatim — `setActiveTab('overview')` on actor.id change)

    ### SC2 — Victimology tab on a major actor (e.g., APT28) renders 4 non-empty sections

    1. With the modal still open from SC1, search for "APT28" in the Threat Actors page (or another well-known major actor with broad victimology data)
    2. Open the APT28 detail modal
    3. Click the "Victimology" tab
    4. Confirm the 2x2 grid renders 4 section cards:
       - **Top-Left:** Targeted Countries — chips with flag emoji + name (e.g., 🇺🇸 United States, 🇩🇪 Germany)
       - **Top-Right:** Targeted Regions — chips with name only (e.g., Eastern Europe, North America)
       - **Bottom-Left:** Targeted Sectors — chips with name only (e.g., Government, Defense, Energy)
       - **Bottom-Right:** Targeted Organizations — chips with name only
    5. Confirm at least one section has ≥ 1 chip (APT28 should have ≥ 5 countries, ≥ 1 region, ≥ 3 sectors typically)
    6. Confirm chip styling is consistent: rounded-pill, dark theme, font-sans, same border across all 4 sections
    7. Spot-check a country chip with a known ISO-2 alias (e.g., United States → 🇺🇸 emoji visible). Spot-check a country with `country_code: null` if any exists — chip renders name-only with no leading emoji and no whitespace gap

    ### SC3 — Empty section renders friendly empty state (no crash, no blank, no layout breakage)

    1. Find an actor with intentionally sparse victimology — try a less-documented actor or an actor where you know one section is likely empty (e.g., search for an obscure intrusion set)
    2. Open the modal, click Victimology
    3. Confirm: the empty section renders its own empty state INSIDE its card:
       - Section card still present (border, padding, header)
       - Header still shows "Targeted {Label} (0)" with the section icon
       - Body shows centered: section icon at opacity 40 + caption "No {label} data available"
    4. Confirm: other 3 sections in the same modal render normally (a single empty section does NOT break the 2x2 layout for the other 3)
    5. Open browser DevTools Console — confirm NO red errors during the render (no React key warning, no `Cannot read property 'length' of undefined`, no other JS error)
    6. Resize the modal/window to mobile width — confirm the 2x2 collapses to single-column without breaking (Tailwind `grid-cols-1 md:grid-cols-2`)

    ### SC4 — Tab switching does NOT trigger additional network requests

    1. Open browser DevTools → Network panel → filter by `enrichment` (or `XHR`)
    2. Clear the Network panel
    3. Open any threat actor modal
    4. **Confirm:** exactly ONE `GET /api/threat-actors/{id}/enrichment` request fires (the eager fetch on modal open)
    5. Click "Victimology" tab
    6. **Confirm:** NO additional network request fires (Network panel still shows 1 enrichment call)
    7. Click "Overview" tab
    8. **Confirm:** still 1 enrichment call total
    9. Click "Victimology" tab again
    10. **Confirm:** still 1 enrichment call total
    11. Repeat 3 more times rapidly — Network panel should remain at 1 enrichment call

    Rationale: D-01 + SC4 — the existing eager fetch on modal open returns the entire enrichment payload (including `victimology` from Phase 59). The Plan 01 `{activeTab === 'victimology' && ...}` guard means React simply renders different cached subtrees on tab switches; no fetch is triggered. VICTM-07 (lazy-fetch on tab open) is therefore satisfied at the spirit level — no rendering work happens until the user clicks the tab — without an extra network round-trip.

    The machine probe Gate 14 (below) backs this up: `grep -c "fetchThreatActorEnrichment\\|new EventSource\\|fetch(" frontend/src/pages/ThreatActorsPage.jsx` MUST return exactly 1.

    ## Machine probes (re-run anytime)

    Run from repo root. All must pass for the phase to be considered code-complete (Plan 03 Task 1 already runs these once; this section is for re-validation after follow-up changes).

    ```
    cd C:/laragon/www/aqua-tip

    # Vite build clean
    cd frontend && npm run build && cd ..

    # VICTM-01: Campaigns + Flag fully purged
    grep -c "campaigns" frontend/src/pages/ThreatActorsPage.jsx          # Expected: 0
    grep -c "Campaigns" frontend/src/pages/ThreatActorsPage.jsx          # Expected: 0
    grep -c "\\bFlag\\b" frontend/src/pages/ThreatActorsPage.jsx         # Expected: 0

    # VICTM-02: Victimology tab in TABS + content block
    grep -q "label: 'Victimology'" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "icon: Target" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "activeTab === 'victimology'" frontend/src/pages/ThreatActorsPage.jsx

    # VICTM-03: Countries section + flag helper
    # Use -F fixed-string mode so '?' / '.' in optional-chaining / member-access are literal
    grep -q "function countryCodeToFlag" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "0x1F1E6" frontend/src/pages/ThreatActorsPage.jsx
    grep -qF "countryCodeToFlag(country.country_code)" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "Targeted Countries (" frontend/src/pages/ThreatActorsPage.jsx

    # VICTM-04..06: Regions / Sectors / Organizations sections
    grep -q "Targeted Regions (" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "Targeted Sectors (" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "Targeted Organizations (" frontend/src/pages/ThreatActorsPage.jsx

    # VICTM-07 spirit: render guard
    grep -q "activeTab === 'victimology' && !enrichError" frontend/src/pages/ThreatActorsPage.jsx

    # VICTM-08: per-section empty-state captions
    grep -q "No countries data available" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "No regions data available" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "No sectors data available" frontend/src/pages/ThreatActorsPage.jsx
    grep -q "No organizations data available" frontend/src/pages/ThreatActorsPage.jsx

    # SC4 fetch-count probe (Gate 14): exactly ONE network-fetch call site
    grep -c "fetchThreatActorEnrichment\\|new EventSource\\|fetch(" frontend/src/pages/ThreatActorsPage.jsx
    # Expected: exactly 1 — the existing line-543 eager fetch.
    # ≥ 2 indicates a regression added a second fetch (e.g., useEffect on activeTab).
    # 0 indicates the existing eager fetch was accidentally removed.

    # D-14 single-file scope (5 task commits in Plan 01 + Plan 02)
    git diff --name-only HEAD~5 HEAD -- frontend/src/ | sort -u
    # Expected: exactly "frontend/src/pages/ThreatActorsPage.jsx"

    # D-15 no CSS edits
    git diff --stat HEAD~5 HEAD -- frontend/src/styles/
    # Expected: empty

    # D-07 zero new deps
    git diff HEAD~5 HEAD -- frontend/package.json frontend/package-lock.json
    # Expected: empty
    ```

    ## Sign-off

    Tick each checkbox after the corresponding manual QA scenario passes against the live deployment. The phase is SHIPPED when all 4 are ticked + the human commits the updated VALIDATION.md with the ticks.

    - [ ] **SC1**: Tab bar shows exactly 5 tabs (Overview, Relationships, TTPs, Tools, Victimology) with no Campaigns tab — verified against `https://tip.aquasecure.ai/threat-actors`
    - [ ] **SC2**: Victimology tab renders 4 non-empty sections for a major actor (e.g., APT28) with country flag emojis on countries and chip-only rendering on regions/sectors/organizations
    - [ ] **SC3**: Empty section renders friendly empty state inside its card without crashing or breaking the 2x2 layout — verified by selecting an actor with a sparse victimology
    - [ ] **SC4**: Switching between Overview and Victimology tabs multiple times triggers EXACTLY ONE `/api/threat-actors/{id}/enrichment` request total — verified via DevTools Network panel
    ```

    Stage + commit:

    ```
    cd C:/laragon/www/aqua-tip
    git add .planning/phases/64-frontend-victimology-tab/64-VALIDATION.md
    git commit -m "docs(64-03): add 64-VALIDATION.md (manual QA scaffold for SC1..SC4 + machine probes including SC4 fetch-count)"
    ```

    Notes: Per Phase 62-02 / 63-03 precedent, the verification-only Task 1 produced no commit — only this Task 2 produces a commit (the new VALIDATION.md file). The total Plan 03 commit count is therefore 1, which matches Phase 62-02 Plan 02's pattern (verification task = 0 commits, VALIDATION-doc task = 1 commit).
  </action>
  <verify>
    <automated>test -f C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && grep -q "SC1" C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && grep -q "SC2" C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && grep -q "SC3" C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && grep -q "SC4" C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && grep -q "Manual QA" C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && grep -q "fetchThreatActorEnrichment" C:/laragon/www/aqua-tip/.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/64-frontend-victimology-tab/64-VALIDATION.md` exits 0
    - File contains all 4 SC labels: each of `grep -q "SC1"`, `grep -q "SC2"`, `grep -q "SC3"`, `grep -q "SC4"` exits 0 against the file
    - File contains a "Manual QA" section header: `grep -q "Manual QA" 64-VALIDATION.md` exits 0
    - File contains a "Sign-off" section with 4 unchecked checkboxes: `grep -c "^- \\[ \\] \\*\\*SC" 64-VALIDATION.md` returns 4
    - File contains the `https://tip.aquasecure.ai/threat-actors` reference (per CLAUDE.md deployment URL)
    - File contains a Network-panel scenario for SC4 (the no-extra-fetch verification): `grep -q "Network panel" 64-VALIDATION.md` exits 0
    - File contains the SC4 fetch-count probe: `grep -q "fetchThreatActorEnrichment" 64-VALIDATION.md` exits 0
    - File contains the machine-probes section with the full grep probe sequence verbatim: `grep -q "function countryCodeToFlag" 64-VALIDATION.md` exits 0 (the probe quote is embedded in the doc)
    - File is committed in a single commit: `git log -1 --name-only` shows exactly `.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md`
    - `git diff --stat HEAD~1 HEAD -- frontend/src/` returns EMPTY (this task did not touch any frontend source)
    - Commit message starts with `docs(64-03):` and mentions VALIDATION.md
  </acceptance_criteria>
  <done>
    `.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md` exists with the structure: overview / Manual QA (SC1..SC4 with reproducible browser steps including a DevTools Network panel scenario for SC4) / Machine probes section (per-requirement grep probes for VICTM-01..08 + Gate 14 fetch-count probe + D-07/D-14/D-15 git checks) / Sign-off section with 4 unchecked SC checkboxes. File committed. No frontend source touched.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Verification (all must pass before marking Plan 03 complete)

```
cd C:/laragon/www/aqua-tip

# 1. All 14 verification gates from Task 1 pass (re-run sample)
grep -c "campaigns\|Campaigns\|\bFlag\b" frontend/src/pages/ThreatActorsPage.jsx | grep -q "^0$" && echo "PASS: VICTM-01"
grep -q "label: 'Victimology'" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-02"
grep -q "0x1F1E6" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-03 helper"
grep -q "Targeted Regions (" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-04"
grep -q "Targeted Sectors (" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-05"
grep -q "Targeted Organizations (" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-06"
grep -q "activeTab === 'victimology' && !enrichError" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-07 spirit"
grep -q "No regions data available" frontend/src/pages/ThreatActorsPage.jsx && echo "PASS: VICTM-08"
grep -c "fetchThreatActorEnrichment\|new EventSource\|fetch(" frontend/src/pages/ThreatActorsPage.jsx | grep -q "^1$" && echo "PASS: SC4 fetch-count (Gate 14)"

# 2. VALIDATION.md exists and complete
test -f .planning/phases/64-frontend-victimology-tab/64-VALIDATION.md && echo "PASS: VALIDATION.md present"
grep -c "^- \[ \] \*\*SC" .planning/phases/64-frontend-victimology-tab/64-VALIDATION.md
# Expected: 4 (4 unchecked SC sign-off boxes)

# 3. D-14 single-file scope across whole phase
git diff --name-only HEAD~6 HEAD -- frontend/src/ | sort -u
# Expected: exactly "frontend/src/pages/ThreatActorsPage.jsx"
# (HEAD~6 covers: Plan 01 = 3 commits, Plan 02 = 2 commits, Plan 03 = 1 commit = 6 total)

# 4. D-15 no CSS edits
git diff --stat HEAD~6 HEAD -- frontend/src/styles/
# Expected: empty

# 5. Vite build clean
cd frontend && npm run build
# Expected: exit 0
```

## VALIDATION.md Mapping

This plan is the verification gate for Phase 64. It does NOT light up new requirements (Plan 01 + Plan 02 already did). It PROVES the requirements are satisfied:

- VICTM-01..06 + VICTM-08: each has a dedicated grep probe in Task 1's gate sequence (Gates 2–7, 9)
- VICTM-07: satisfied at the spirit level via Gate 8 (the activeTab guard) + SC4 manual QA in 64-VALIDATION.md
- D-07 / D-14 / D-15: machine-verified via Gates 11–13 (zero new deps, single-file scope, no CSS edits)
- SC4: machine-probed via Gate 14 (fetch-count = 1) + manual QA in 64-VALIDATION.md (DevTools Network panel)
- SC1..SC3: deferred to manual QA per 64-VALIDATION.md (a human verifies against the live deploy)

## Requirement Coverage (this plan)

This plan re-asserts coverage on ALL 8 phase requirements (VICTM-01..08) via Task 1's grep gate sequence. New requirement satisfaction is not produced — the requirements were satisfied in Plans 01 + 02 and this plan is the audit.
</verification>

<success_criteria>
- All 14 verification gates from Task 1 pass with their expected outputs (Vite build clean + 8 VICTM grep probes + 4 architectural-guarantee git checks + Gate 14 SC4 fetch-count = 1)
- `.planning/phases/64-frontend-victimology-tab/64-VALIDATION.md` exists with the documented structure and is committed
- Task 1 produces ZERO source diff (verification-only — Phase 62-02 / 63-03 precedent)
- Task 2 produces exactly ONE commit touching only the new VALIDATION.md file
- `git diff --name-only HEAD~6 HEAD -- frontend/src/ | sort -u` returns exactly `frontend/src/pages/ThreatActorsPage.jsx` (the only frontend source file touched across the entire phase)
- `git diff --stat HEAD~6 HEAD -- frontend/src/styles/` returns EMPTY (D-15 — no CSS edits across the whole phase)
- `git diff HEAD~6 HEAD -- frontend/package.json frontend/package-lock.json` returns EMPTY (D-07 — no new deps across the whole phase)
- One commit created: `docs(64-03): add 64-VALIDATION.md (manual QA scaffold for SC1..SC4 + machine probes including SC4 fetch-count)`
- Phase 64 is now CODE-COMPLETE pending manual QA — exactly the state Phases 61, 62, and 63 ended in before their respective sign-off events
</success_criteria>

<output>
After completion, create `.planning/phases/64-frontend-victimology-tab/64-03-SUMMARY.md` documenting:
- All 14 Task 1 gate results (the 8 VICTM probes + Vite build + 4 architectural-guarantee diffs + Gate 14 SC4 fetch-count probe)
- Final line count of `64-VALIDATION.md`
- Confirmation that Task 1 produced zero diff (no commit) and Task 2 produced exactly one commit (VALIDATION.md only)
- Vite build timing
- Git scope summary for the entire phase (single-file scope confirmed across all 6 commits)
- Explicit note that the phase is code-complete pending manual QA per 64-VALIDATION.md (Phase 61/62/63 precedent — manual QA happens against live deploy, signs off via the SC1..SC4 checkboxes)
- Cross-reference to STATE.md Phase 62-02 entry on verification-only zero-diff tasks (the rationale for Task 1 not committing)
</output>
