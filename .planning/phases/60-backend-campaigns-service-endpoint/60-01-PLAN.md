---
phase: 60-backend-campaigns-service-endpoint
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/Feature/ThreatCampaign/IndexTest.php
autonomous: true
requirements: [CAMP-07]
tags: [laravel, pest, scaffold, test-infra]
must_haves:
  truths:
    - "Test directory backend/tests/Feature/ThreatCampaign/ exists"
    - "IndexTest.php exists with RefreshDatabase + beforeEach(Cache::flush())"
    - "fakeCampaignsResponse() helper emits no IntrusionSet-only fields"
    - "mockOpenCtiForCampaigns() and createPlan() helpers available to later waves"
  artifacts:
    - path: "backend/tests/Feature/ThreatCampaign/IndexTest.php"
      provides: "Scaffolded Pest test file with fixture + mock + plan helpers"
      contains: "fakeCampaignsResponse"
    - path: "backend/tests/Feature/ThreatCampaign"
      provides: "New feature test directory"
  key_links:
    - from: "backend/tests/Feature/ThreatCampaign/IndexTest.php"
      to: "backend/tests/Feature/ThreatActor/EnrichmentTest.php:11"
      via: "beforeEach(fn () => Cache::flush()) copied pattern"
      pattern: "beforeEach\\(fn \\(\\) => Cache::flush\\(\\)\\)"
    - from: "backend/tests/Feature/ThreatCampaign/IndexTest.php"
      to: "backend/tests/Feature/FeatureGate/FeatureGateMiddlewareTest.php:31-44"
      via: "createPlan() helper copied verbatim"
      pattern: "function createPlan\\(string \\$slug\\): Plan"
---

<objective>
Scaffold the Phase 60 Pest test infrastructure: create the new test directory, the IndexTest.php skeleton, and the three helper functions (`fakeCampaignsResponse`, `mockOpenCtiForCampaigns`, `createPlan`) that every Wave 1+ test in this phase will consume.

Purpose: Wave 0 contract — Wave 1 service tests and Wave 2 HTTP tests both bind to helpers defined here. Creating them first eliminates the "write test infra then write tests then write code" three-hop context churn that would otherwise degrade later waves.
Output: `backend/tests/Feature/ThreatCampaign/IndexTest.php` with no test bodies yet — only imports, `uses(RefreshDatabase::class)`, `beforeEach(fn () => Cache::flush())`, and the three helpers. Running `php artisan test --filter=ThreatCampaign` prints "No tests executed" (or green 0 tests), NOT a syntax error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-backend-campaigns-service-endpoint/60-CONTEXT.md
@.planning/phases/60-backend-campaigns-service-endpoint/60-RESEARCH.md
@.planning/phases/60-backend-campaigns-service-endpoint/60-PATTERNS.md
@.planning/phases/60-backend-campaigns-service-endpoint/60-VALIDATION.md

<interfaces>
<!-- Helper signatures Wave 1+ consumes. Defined here; do not rename without updating callers. -->

```php
// Fixture builder — mirrors fakeIntrusionSetsResponse() structurally but with Campaign fields only.
function fakeCampaignsResponse(int $count = 2): array;

// Service-binding Mockery helper — replaces OpenCtiService in the container.
function mockOpenCtiForCampaigns(?array $response = null): void;

// Plan seeder — verbatim copy from FeatureGateMiddlewareTest.php:31-44.
function createPlan(string $slug): Plan;
```

Top-level key in the fixture is `campaigns` (NOT `intrusionSets`).
Each node contains: id, name, description, first_seen, last_seen, objective, aliases, modified, created, objectLabel, externalReferences, attributed_to.
Each node MUST NOT contain: primary_motivation, resource_level, goals, motivation, targetedCountries, targetedSectors.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → application container | Tests bind mock services via `app()->bind()`; no external network crosses this in Phase 60 test runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `fakeCampaignsResponse()` fixture | mitigate | Fixture omits IntrusionSet-only keys (`primary_motivation`, `resource_level`, `goals`) so SC4 absence tests in Wave 1 cannot false-positive against a dirty fixture. |
| T-60-02 | Information Disclosure | Test DB seeding | accept | SQLite `:memory:` driver in `phpunit.xml`; no real credentials or PII. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create ThreatCampaign test directory and scaffold IndexTest.php with helpers</name>
  <files>backend/tests/Feature/ThreatCampaign/IndexTest.php</files>
  <read_first>
    - backend/tests/Feature/ThreatActor/ThreatActorIndexTest.php (lines 1-76 — imports, `fakeIntrusionSetsResponse`, `mockOpenCtiForActors`, `uses(RefreshDatabase::class)`; mirror structure with Campaign field swaps)
    - backend/tests/Feature/ThreatActor/EnrichmentTest.php (lines 1-11 — `beforeEach(fn () => Cache::flush())` convention; Pitfall 6 in 60-RESEARCH.md)
    - backend/tests/Feature/FeatureGate/FeatureGateMiddlewareTest.php (lines 31-44 — `createPlan($slug)` helper, ALL 10 columns required by the `plans` table in `:memory:` sqlite)
    - .planning/phases/60-backend-campaigns-service-endpoint/60-PATTERNS.md (Pattern C1, C2, C3, C4)
    - .planning/phases/60-backend-campaigns-service-endpoint/60-CONTEXT.md (D-06 field list, D-19 item shape, D-21 test file location)
  </read_first>
  <action>
Create the directory `backend/tests/Feature/ThreatCampaign/` and the file `backend/tests/Feature/ThreatCampaign/IndexTest.php` with:

1. PHP opening tag and `use` statements (preserve order):
   - `use App\Models\Plan;`
   - `use App\Models\User;`
   - `use App\Services\OpenCtiService;`
   - **Immediately above the next `use` line**, emit the single-line comment `// class created in Plan 03; import here for scaffold parity` so the dangling import is self-documenting.
   - `use App\Services\ThreatCampaignService;` (this class will be created in Wave 1 Plan 03 — the import resolving is fine even before the class exists, because no tests reference the class yet)
   - `use Illuminate\Foundation\Testing\RefreshDatabase;`
   - `use Illuminate\Support\Facades\Cache;`

2. `uses(RefreshDatabase::class);` (per D-22 — SC3 tests need real DB seeding).

3. `beforeEach(fn () => Cache::flush());` — CRITICAL per Pitfall 6 (60-RESEARCH.md). Without this, SC2 tests pass in isolation but fail in full-file runs because the `array` cache driver carries state across tests.

4. `function fakeCampaignsResponse(int $count = 2): array` — mirror 60-PATTERNS.md Pattern C2. Loop `$i = 1..$count` building node array with EXACTLY these keys (per D-06, D-19):
   - `id` = `"campaign-{$i}"`
   - `name` = `"Operation {$i}"`
   - `description` = `"Description for Operation {$i}"`
   - `first_seen` = `'2024-01-15T10:30:00.000Z'`
   - `last_seen` = `'2025-06-20T14:00:00.000Z'`
   - `objective` = `"Steal data for Operation {$i}"`
   - `aliases` = `["AliasA{$i}", "AliasB{$i}"]`
   - `modified` = `'2025-06-20T14:00:00.000Z'`
   - `created` = `'2024-01-15T10:30:00.000Z'`
   - `objectLabel.edges[0].node` = `{id: "label-{$i}", value: "espionage", color: "#cc0000"}`
   - `externalReferences.edges[0].node` = `{source_name: "mitre-attack", url: "https://attack.mitre.org/campaigns/C{$i}", description: "MITRE ref for Operation {$i}"}`
   - `attributed_to.edges[0].node.to` = `{id: "intrusion-set-apt{$i}", name: "APT{$i}"}` — NOTE `node.to` (not `node.from`) per D-10 inverse direction.
   - **NEGATIVE:** Fixture nodes MUST NOT contain `primary_motivation`, `resource_level`, `goals`, `motivation`, `targetedCountries`, or `targetedSectors`. Omitting these keys keeps the SC4 absence test structurally honest.
   Top-level return: `['campaigns' => ['edges' => $edges, 'pageInfo' => [hasNextPage=>true, hasPreviousPage=>false, startCursor=>'cursor-start', endCursor=>'cursor-end', globalCount=>50]]]`. Top-level key is `campaigns` — NOT `intrusionSets`.

5. `function mockOpenCtiForCampaigns(?array $response = null): void` — mirror 60-PATTERNS.md Pattern C3. `$response ??= fakeCampaignsResponse();` then `app()->bind(OpenCtiService::class, fn () => Mockery::mock(OpenCtiService::class)->shouldReceive('query')->andReturn($response)->getMock());` — or the equivalent multi-line form used in the threat-actors analog.

6. `function createPlan(string $slug): Plan` — copy verbatim from `FeatureGateMiddlewareTest.php:31-44` per D-22. All 10 columns required: `slug`, `name`, `daily_credit_limit`, `price_cents`, `is_popular`, `sort_order`, `is_active`, `features`, `description`. `price_cents = $slug === 'free' ? 0 : 1000`. Omitting any column triggers an SQL NOT NULL error in the `:memory:` SQLite test DB.

7. **Do NOT add any `test(...)` blocks.** Wave 1 Plan 03 (service) and Wave 2 Plan 04 (HTTP) add the test bodies. This task ONLY defines the helpers and file skeleton so later waves can bind to them.

8. Sanity comment at top of file: `// Phase 60 test scaffold — tests added in Plans 03 (service) and 04 (HTTP).`
  </action>
  <verify>
    <automated>test -f backend/tests/Feature/ThreatCampaign/IndexTest.php && cd backend && php artisan test --filter=ThreatCampaign 2>&1 | grep -E 'No tests executed|Tests:\s*0'</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` exists
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `uses(RefreshDatabase::class)`
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `beforeEach(fn () => Cache::flush())`
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `function fakeCampaignsResponse(int $count = 2): array`
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `function mockOpenCtiForCampaigns`
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `function createPlan(string $slug): Plan`
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `'campaigns' =>` (top-level fixture key)
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `'attributed_to' =>` and `'to' =>` (D-10 inverse direction)
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `'first_seen'`, `'last_seen'`, `'objective'`
    - `backend/tests/Feature/ThreatCampaign/IndexTest.php` contains `// class created in Plan 03; import here for scaffold parity` (scaffold-parity note above the ThreatCampaignService import)
    - `grep -c 'primary_motivation\|resource_level\|goals\|targetedCountries\|targetedSectors\|motivation' backend/tests/Feature/ThreatCampaign/IndexTest.php` returns 0
    - `grep -c 'intrusionSets' backend/tests/Feature/ThreatCampaign/IndexTest.php` returns 0
    - `cd backend && php artisan test --filter=ThreatCampaign` exits 0 (no tests yet but file parses cleanly)
  </acceptance_criteria>
  <done>
    Test directory and IndexTest.php scaffold exist. File contains helpers but no test bodies. `php artisan test --filter=ThreatCampaign` runs without parse errors (reports zero tests). No IntrusionSet-only keys present anywhere in the file.
  </done>
</task>

</tasks>

<verification>
- `test -d backend/tests/Feature/ThreatCampaign` → directory exists.
- `test -f backend/tests/Feature/ThreatCampaign/IndexTest.php` → file exists.
- `cd backend && php artisan test --filter=ThreatCampaign` → exits 0 with zero tests (or "No tests executed").
- `grep -c 'intrusionSets\|primary_motivation\|resource_level\|goals' backend/tests/Feature/ThreatCampaign/IndexTest.php` → 0.
- `grep -c 'fakeCampaignsResponse\|mockOpenCtiForCampaigns\|createPlan' backend/tests/Feature/ThreatCampaign/IndexTest.php` → 3.
</verification>

<success_criteria>
Scaffold is ready: Wave 1 Plan 03 can add service tests that call `mockOpenCtiForCampaigns()` and `app(ThreatCampaignService::class)`; Wave 2 Plan 04 can add HTTP tests that call `createPlan('free')` and `$this->actingAs($user)->getJson('/api/threat-campaigns')`. The scaffold file parses without error in Pest.
</success_criteria>

<output>
After completion, create `.planning/phases/60-backend-campaigns-service-endpoint/60-01-SUMMARY.md` using the summary template. Record:
- Files created: the test directory + IndexTest.php.
- Helpers defined: `fakeCampaignsResponse`, `mockOpenCtiForCampaigns`, `createPlan`.
- Negative-field audit: grep counts for IntrusionSet-only keys (expect 0).
- Pest smoke check: output of `php artisan test --filter=ThreatCampaign`.
</output>
