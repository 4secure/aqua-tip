---
phase: 02-oauth-email-verification
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/tests/Feature/Auth/OAuthTest.php
  - backend/tests/Feature/Auth/EmailVerificationTest.php
  - backend/tests/Feature/Auth/PasswordResetTest.php
autonomous: true
requirements: [AUTH-03, AUTH-04, AUTH-05, AUTH-09]

must_haves:
  truths:
    - "Google OAuth creates new user with email_verified_at set and redirects to /dashboard"
    - "GitHub OAuth creates new user with email_verified_at set and redirects to /dashboard"
    - "OAuth merges with existing email/password account when emails match"
    - "Unverified user gets 403 from /api/user"
    - "Verified user gets 200 from /api/user"
    - "Verification email resend works for unverified users"
    - "Password reset email is sent for email/password users"
    - "Password reset fails for OAuth-only users with provider message"
    - "Valid reset token + new password updates the password"
    - "Expired or invalid reset token returns 422"
  artifacts:
    - path: "backend/tests/Feature/Auth/OAuthTest.php"
      provides: "OAuth integration tests for Google + GitHub"
      min_lines: 80
    - path: "backend/tests/Feature/Auth/EmailVerificationTest.php"
      provides: "Email verification enforcement + resend tests"
      min_lines: 60
    - path: "backend/tests/Feature/Auth/PasswordResetTest.php"
      provides: "Password reset flow + OAuth guard tests"
      min_lines: 80
  key_links:
    - from: "backend/tests/Feature/Auth/OAuthTest.php"
      to: "SocialAuthController"
      via: "Mocked Socialite driver"
      pattern: "Socialite::shouldReceive"
    - from: "backend/tests/Feature/Auth/EmailVerificationTest.php"
      to: "/api/email/verify"
      via: "Signed URL verification"
      pattern: "email/verify"
    - from: "backend/tests/Feature/Auth/PasswordResetTest.php"
      to: "/api/forgot-password"
      via: "Password broker integration"
      pattern: "forgot-password"
---

<objective>
Write comprehensive Pest integration tests for OAuth sign-in, email verification, and password reset flows.

Purpose: Prove that all Phase 2 auth features work correctly -- Google/GitHub OAuth creates users and sessions, email verification is enforced on protected routes, and password reset works for email/password users while blocking OAuth users. These tests validate AUTH-03, AUTH-04, AUTH-05, and AUTH-09 requirements.

Output: 3 test files with full coverage of all Phase 2 auth behaviors.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-oauth-email-verification/02-RESEARCH.md
@.planning/phases/02-oauth-email-verification/02-01-SUMMARY.md

<interfaces>
<!-- Test patterns established in Phase 1 -->

From backend/tests/Feature/Auth/RegistrationTest.php (pattern reference):
```php
// Tests use Origin header for Sanctum stateful API
test('registers user with valid data', function () {
    $response = $this->withHeaders(['Origin' => 'http://localhost:5173'])
        ->postJson('/api/register', [...]);
    $response->assertStatus(201);
});
```

From 02-RESEARCH.md (Socialite mock pattern):
```php
function mockSocialiteUser(string $provider, array $overrides = []): void
{
    $defaults = [
        'id' => '12345',
        'name' => 'Test User',
        'email' => 'oauth@example.com',
        'avatar' => 'https://example.com/avatar.jpg',
    ];
    $data = array_merge($defaults, $overrides);

    $socialiteUser = Mockery::mock(SocialiteUser::class, function (MockInterface $mock) use ($data) {
        $mock->shouldReceive('getId')->andReturn($data['id']);
        $mock->shouldReceive('getName')->andReturn($data['name']);
        $mock->shouldReceive('getNickname')->andReturn($data['name']);
        $mock->shouldReceive('getEmail')->andReturn($data['email']);
        $mock->shouldReceive('getAvatar')->andReturn($data['avatar']);
    });

    $socialiteProvider = Mockery::mock(Provider::class, function (MockInterface $mock) use ($socialiteUser) {
        $mock->shouldReceive('stateless->user')->andReturn($socialiteUser);
    });

    Socialite::shouldReceive('driver')->with($provider)->andReturn($socialiteProvider);
}
```

Expected routes after Plan 01:
- POST /api/forgot-password (guest, throttle:auth)
- POST /api/reset-password (guest, throttle:auth)
- GET /api/email/verify/{id}/{hash} (auth:sanctum, signed)
- POST /api/email/verification-notification (auth:sanctum, throttle:6,1)
- GET /api/user (auth:sanctum, verified)
- POST /api/logout (auth:sanctum, verified)
- GET /api/auth/{provider}/callback (web middleware, in web.php)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OAuth integration tests for Google and GitHub sign-in</name>
  <files>backend/tests/Feature/Auth/OAuthTest.php</files>
  <behavior>
    - Test: Google OAuth callback creates new user with correct fields (email, name, oauth_provider=google, oauth_id, avatar_url, email_verified_at set)
    - Test: Google OAuth callback authenticates user and redirects to frontend /dashboard
    - Test: GitHub OAuth callback creates new user with correct fields (oauth_provider=github)
    - Test: GitHub OAuth callback authenticates user and redirects to frontend /dashboard
    - Test: OAuth callback with existing email/password user merges accounts (sets oauth_provider + oauth_id on existing user)
    - Test: OAuth callback with existing OAuth user updates avatar and logs in (no duplicate user created)
    - Test: OAuth callback with unsupported provider returns error redirect
    - Test: OAuth redirect endpoint returns JSON with provider URL for Google
    - Test: OAuth redirect endpoint returns JSON with provider URL for GitHub
    - Test: OAuth redirect endpoint rejects unsupported provider with 422
  </behavior>
  <action>
    Create backend/tests/Feature/Auth/OAuthTest.php using Pest syntax with RefreshDatabase.

    Define a `mockSocialiteUser()` helper function at the top of the file (as shown in the interfaces section). This function mocks the Socialite driver to return a fake user without hitting real OAuth providers.

    Key testing patterns:
    - OAuth callback route is GET /api/auth/{provider}/callback in web.php -- use `$this->get()` (not `getJson`) since it returns a redirect
    - Assert redirect to `config('services.frontend_url') . '/dashboard'`
    - Assert `$this->assertAuthenticated()` after callback
    - Assert database has correct user record with `assertDatabaseHas('users', [...])`
    - For account merging test: create a user with email but no oauth_provider first, then call OAuth callback with same email
    - For existing OAuth user test: create user with oauth_provider=google first, then call callback again
    - OAuth redirect is GET /api/auth/{provider}/redirect -- returns JSON `{ url: "..." }`
    - Use `withHeaders(['Origin' => 'http://localhost:5173'])` for API routes (redirect endpoint)

    IMPORTANT: The callback route is in web.php and uses session middleware. The redirect route is in api.php. Test both appropriately.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=OAuthTest</automated>
  </verify>
  <done>OAuthTest.php has 10 tests covering: Google + GitHub new user creation, authentication + redirect, account merging, existing user login, unsupported provider rejection, redirect URL generation. All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Email verification and password reset integration tests</name>
  <files>
    backend/tests/Feature/Auth/EmailVerificationTest.php,
    backend/tests/Feature/Auth/PasswordResetTest.php
  </files>
  <behavior>
    EmailVerificationTest:
    - Test: Unverified user gets 403 from GET /api/user
    - Test: Verified user gets 200 from GET /api/user
    - Test: Registration triggers verification email (Registered event dispatched)
    - Test: Verification endpoint marks user as verified (email_verified_at set)
    - Test: Resend endpoint sends verification notification for unverified user
    - Test: Resend endpoint returns "already verified" for verified user
    - Test: Unverified user can still access verification routes (not blocked by verified middleware)

    PasswordResetTest:
    - Test: POST /api/forgot-password sends reset link for email/password user
    - Test: POST /api/forgot-password returns 422 for OAuth-only user with provider-specific message
    - Test: POST /api/forgot-password returns 422 for non-existent email (standard Laravel behavior)
    - Test: POST /api/reset-password resets password with valid token
    - Test: POST /api/reset-password fails with invalid/expired token (422)
    - Test: POST /api/reset-password enforces password strength rules (min 8, mixed case, numbers)
    - Test: Password reset does NOT auto-login user (per user decision: security-first)
    - Test: POST /api/forgot-password is throttled (cannot request twice within 60s for same email)
  </behavior>
  <action>
    Create both test files using Pest syntax with RefreshDatabase.

    **EmailVerificationTest.php:**
    - Use `withHeaders(['Origin' => 'http://localhost:5173'])` for all API requests
    - Create unverified user: `User::factory()->create(['email_verified_at' => null])`
    - Create verified user: `User::factory()->create(['email_verified_at' => now()])`
    - For "registration triggers email" test: use `Event::fake([Registered::class])` then POST /api/register, then `Event::assertDispatched(Registered::class)`
    - For verification endpoint test: generate a signed verification URL using `URL::temporarySignedRoute('verification.verify', now()->addMinutes(60), ['id' => $user->id, 'hash' => sha1($user->email)])`, then GET that URL as the authenticated user
    - For resend test: act as unverified user, POST /api/email/verification-notification, use `Notification::fake()` to assert `VerifyEmail` notification was sent

    **PasswordResetTest.php:**
    - Use `Notification::fake()` to capture reset notifications
    - For forgot-password test: POST /api/forgot-password with email, assert 200, assert notification sent
    - For OAuth guard test: create user with `oauth_provider => 'google'`, POST /api/forgot-password, assert 422 with message containing "google"
    - For reset test: use `Password::createToken($user)` to get a valid token, then POST /api/reset-password with token + email + new password + confirmation
    - After reset, verify user can login with new password but is NOT auto-logged-in (assert `$this->assertGuest()` after reset)
    - For password strength test: POST /api/reset-password with weak password (e.g., "short"), assert 422 validation error
    - For throttle test: POST /api/forgot-password twice for same email, second should return 422 with throttle message

    NOTE: Check if User factory exists. If not, create one or use `User::create()` directly in tests with required fields.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test --filter="EmailVerificationTest|PasswordResetTest"</automated>
  </verify>
  <done>EmailVerificationTest.php has 7 tests covering verification enforcement, event dispatch, resend flow. PasswordResetTest.php has 8 tests covering forgot/reset flow, OAuth guard, password strength, throttling. All pass. Full auth suite green: `php artisan test --filter=Auth`.</done>
</task>

</tasks>

<verification>
1. `cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=OAuthTest` -- all OAuth tests pass
2. `cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=EmailVerificationTest` -- all verification tests pass
3. `cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=PasswordResetTest` -- all reset tests pass
4. `cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=Auth` -- full auth suite green (including Phase 1 tests)
</verification>

<success_criteria>
- 3 new test files with 25+ tests total
- Google OAuth: new user creation, authentication, redirect to /dashboard
- GitHub OAuth: same behaviors as Google
- OAuth account merging with existing email/password accounts
- Unverified users blocked from protected routes (403)
- Verified users access protected routes (200)
- Registration dispatches Registered event
- Password reset sends email for email/password users
- Password reset blocked for OAuth-only users with helpful message
- Password strength enforced on reset
- No auto-login after password reset
- Full auth test suite passes including Phase 1 tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-oauth-email-verification/02-02-SUMMARY.md`
</output>
