---
phase: 04-frontend-auth-integration
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/auth/CodeInput.jsx
  - frontend/src/pages/VerifyEmailPage.jsx
  - frontend/src/pages/GetStartedPage.jsx
  - frontend/src/pages/ForgotPasswordPage.jsx
  - frontend/src/pages/ResetPasswordPage.jsx
  - frontend/package.json
autonomous: false
requirements:
  - FEND-07
  - FEND-08

must_haves:
  truths:
    - "User sees 6-digit code input on /verify-email with envelope icon and their email address"
    - "User can paste a full 6-digit code and it auto-fills all boxes"
    - "User can resend verification code from /verify-email page"
    - "User can enter name + phone with country code picker on /get-started page"
    - "User can request password reset email from /forgot-password"
    - "User can set new password on /reset-password with token from email"
    - "All new pages use ParticleBackground + glassmorphism card dark theme layout"
  artifacts:
    - path: "frontend/src/components/auth/CodeInput.jsx"
      provides: "6-digit verification code input with auto-advance and paste support"
    - path: "frontend/src/pages/VerifyEmailPage.jsx"
      provides: "Email verification page with code entry"
    - path: "frontend/src/pages/GetStartedPage.jsx"
      provides: "Onboarding page with name + phone fields"
    - path: "frontend/src/pages/ForgotPasswordPage.jsx"
      provides: "Password reset request page"
    - path: "frontend/src/pages/ResetPasswordPage.jsx"
      provides: "New password entry page"
  key_links:
    - from: "frontend/src/pages/VerifyEmailPage.jsx"
      to: "frontend/src/api/auth.js"
      via: "verifyEmailCode() and resendVerification() API calls"
      pattern: "verifyEmailCode|resendVerification"
    - from: "frontend/src/pages/GetStartedPage.jsx"
      to: "frontend/src/api/auth.js"
      via: "completeOnboarding() API call"
      pattern: "completeOnboarding"
    - from: "frontend/src/pages/ResetPasswordPage.jsx"
      to: "frontend/src/pages/LoginPage.jsx"
      via: "navigate('/login', { state: { success: '...' } }) after reset"
      pattern: "navigate.*login.*success"
---

<objective>
Create all new frontend auth pages: VerifyEmailPage (6-digit code input), GetStartedPage (onboarding with name + phone), ForgotPasswordPage, and ResetPasswordPage.

Purpose: These pages complete the auth flow: Register -> Verify Email (code or link) -> Get Started (name + phone) -> Dashboard. Also handles forgot/reset password flow. All pages match the existing dark theme with ParticleBackground and glassmorphism cards.

Output: 4 new page components, 1 new CodeInput component, react-phone-number-input installed for GetStartedPage.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-auth-integration/04-CONTEXT.md
@.planning/phases/04-frontend-auth-integration/04-RESEARCH.md
@.planning/phases/04-frontend-auth-integration/04-01-SUMMARY.md

@frontend/src/pages/LoginPage.jsx
@frontend/src/api/auth.js
@frontend/src/contexts/AuthContext.jsx
@frontend/src/components/ui/ParticleBackground.jsx
@frontend/src/components/ui/GradientButton.jsx

<interfaces>
<!-- Auth page layout pattern (from LoginPage.jsx) -->
<div className="min-h-screen bg-primary flex items-center justify-center px-4 relative"
  style={{ background: 'radial-gradient(ellipse at 30% 20%, rgba(122, 68, 228, 0.12) 0%, transparent 50%), radial-gradient(ellipse at 70% 80%, rgba(90, 45, 184, 0.08) 0%, transparent 50%), #0A0B10' }}>
  <ParticleBackground />
  <div className="w-full max-w-md relative z-10">
    <div className="bg-surface border border-border rounded-xl p-8">
      {/* Logo header */}
      <div className="text-center mb-8">
        <Link to="/" className="inline-flex items-center gap-2 mb-4">
          <img src="/logo.png" alt="Aqua Tip" className="w-6 h-6" />
          <span className="font-display text-xl font-bold text-text-primary tracking-tight">AQUA TIP</span>
        </Link>
        <h1 className="font-display text-2xl font-bold text-text-primary">{title}</h1>
        <p className="font-mono text-sm text-text-secondary mt-1">{subtitle}</p>
      </div>
      {/* Page content */}
    </div>
  </div>
</div>

<!-- API functions available from api/auth.js (after Plan 02 Task 1) -->
verifyEmailCode(code) -> POST /api/email/verify-code
resendVerification() -> POST /api/email/verification-notification
forgotPassword(email) -> POST /api/forgot-password
resetPassword({ token, email, password, password_confirmation }) -> POST /api/reset-password
completeOnboarding({ name, phone }) -> POST /api/onboarding

<!-- AuthContext value (after Plan 02 Task 1) -->
{ user, loading, error, isAuthenticated, emailVerified, onboardingCompleted, userInitials, login, register, logout, refreshUser }

<!-- Error handling pattern -->
Field errors: err.errors -> { field: ['message'] }
General errors: err.message -> string
Display: bg-red/10 border border-red/20 rounded-lg text-red text-sm font-mono
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CodeInput component, VerifyEmailPage, and GetStartedPage</name>
  <files>
    frontend/src/components/auth/CodeInput.jsx,
    frontend/src/pages/VerifyEmailPage.jsx,
    frontend/src/pages/GetStartedPage.jsx,
    frontend/package.json
  </files>
  <action>
    1. **Install react-phone-number-input**: Run `cd frontend && npm install react-phone-number-input`.

    2. **CodeInput.jsx** (`frontend/src/components/auth/CodeInput.jsx`): Hand-rolled 6-digit input component. Props: `length` (default 6), `onComplete(code)` callback, `disabled` boolean. Implementation:
       - Use `useState(Array(length).fill(''))` for values, `useRef([])` for input refs
       - `handleChange(index, value)`: Accept digits only (`/^\d*$/.test(value)`). Take last char. Set value. Auto-advance focus to next input if value entered and not last box. If all boxes filled, call `onComplete(values.join(''))`.
       - `handleKeyDown(index, e)`: On Backspace with empty current box, focus previous box.
       - `handlePaste(e)`: Prevent default. Extract digits from clipboard, fill boxes left-to-right, focus appropriate box. If all 6 digits pasted, call `onComplete`.
       - Each input: `type="text"` `inputMode="numeric"` `maxLength={1}` `autoComplete="one-time-code"` (first input only). Styling: `w-12 h-14 text-center text-xl font-mono bg-surface-2 border border-border rounded-lg text-text-primary focus:border-violet focus:outline-none transition-colors`. When disabled, add `opacity-50 cursor-not-allowed`.
       - Wrap in `<div className="flex gap-3 justify-center" onPaste={handlePaste}>`.

    3. **VerifyEmailPage.jsx**: Full-page standalone auth layout (copy LoginPage pattern exactly). This page requires the user to be logged in (check `isAuthenticated` from useAuth; if not, Navigate to /login). If already verified (`emailVerified`), Navigate to /get-started or /dashboard based on `onboardingCompleted`.
       - Header: Envelope icon (use `Mail` from lucide-react, sized large like `w-12 h-12` in a violet/cyan gradient circle), "Verify your email" heading, show user's email: `<p className="font-mono text-sm text-cyan mt-2">{user?.email}</p>`
       - CodeInput component for 6-digit entry. On complete, call `verifyEmailCode(code)`. Show submitting spinner while verifying.
       - Success: call `refreshUser()` to update AuthContext, then navigate to `/get-started`.
       - Error: show error message in red banner, clear code inputs.
       - Below code input: `<p className="text-xs text-text-muted font-mono mt-4">Or click the link in your email</p>`
       - Resend button: "Resend Code" with loading state. Calls `resendVerification()`. After success, show "Code sent!" confirmation text for 3 seconds. Rate limit hint: `<p className="text-xs text-text-muted font-mono mt-2">Check your spam folder</p>`
       - Bottom: `<Link to="/login">Back to Login</Link>` (clicking this should log the user out first, or simply link back -- since they're authenticated, GuestRoute will redirect. Better: call `logout()` then navigate to `/login`)

    4. **GetStartedPage.jsx**: Full-page standalone auth layout. Requires authenticated user. If not authenticated, Navigate to /login. If not verified, Navigate to /verify-email. If already onboarded (`onboardingCompleted`), Navigate to /dashboard.
       - Header: "Complete your profile" heading, "Just a few more details to get started" subtitle
       - Name field: Text input with User icon, pre-filled with `user?.name` if it's not the email-prefix placeholder (check if name contains '@' or equals email prefix). Label: "Full Name". Required.
       - Phone field: Use `react-phone-number-input` with `PhoneInput` component. Import: `import PhoneInput from 'react-phone-number-input'` and `import 'react-phone-number-input/style.css'`. Use `defaultCountry="US"`. State: `const [phone, setPhone] = useState('')`. Apply dark theme styling -- the library accepts `className` prop. Override the default CSS with custom styles to match dark theme: set `--PhoneInputCountryFlag-height` and override `.PhoneInputInput` background, border, text color via a small `<style>` tag or by wrapping in a styled container with Tailwind.
       - Submit: "Get Started" GradientButton. On submit, call `completeOnboarding({ name, phone })`. On success, call `refreshUser()` then navigate to `/dashboard`.
       - Error handling: standard field-level + general error pattern.
  </action>
  <verify>
    <automated>cd frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CodeInput renders 6 digit boxes with auto-advance, backspace, and paste support
    - VerifyEmailPage shows user's email, code input, resend button, and "or click link" helper
    - GetStartedPage shows name (pre-filled for OAuth) and phone with country picker
    - react-phone-number-input installed and functional
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ForgotPasswordPage and ResetPasswordPage</name>
  <files>
    frontend/src/pages/ForgotPasswordPage.jsx,
    frontend/src/pages/ResetPasswordPage.jsx
  </files>
  <action>
    1. **ForgotPasswordPage.jsx**: Full-page standalone auth layout (same ParticleBackground + glassmorphism pattern).
       - Header: "Forgot password?" heading, "Enter your email to receive a reset link" subtitle
       - Email input field with Mail icon (same styling as LoginPage email field)
       - "Send Reset Link" GradientButton. On submit, call `forgotPassword(email)` from api/auth.js.
       - On success (200): show green success banner "If an account exists with that email, you'll receive a reset link shortly." Clear the email field. Do NOT navigate away (user stays on page).
       - On error (422): show field-level errors. On general error: show red banner.
       - Loading/disabled state while submitting.
       - Bottom: `<Link to="/login" className="text-violet-light hover:text-violet transition-colors font-mono text-sm">Back to Login</Link>`

    2. **ResetPasswordPage.jsx**: Full-page standalone auth layout.
       - Read `token` and `email` from URL search params: `const [searchParams] = useSearchParams()`. Extract `token = searchParams.get('token')` and `email = searchParams.get('email')`.
       - If no token or email in URL, show error: "Invalid or missing reset link. Please request a new one." with link to /forgot-password.
       - Header: "Reset your password" heading, show email address in subtitle
       - Two password fields: "New Password" and "Confirm Password", both with Lock icon and show/hide toggle (same pattern as LoginPage password field). Use separate `showPassword` and `showConfirm` states.
       - Password hint text: "Min 8 characters, mixed case, at least one number" (same as RegisterPage)
       - Client-side check: if passwords don't match, set field error on password_confirmation.
       - "Reset Password" GradientButton. On submit, call `resetPassword({ token, email, password, password_confirmation })`.
       - On success: navigate to `/login` with state: `navigate('/login', { state: { success: 'Password reset successfully! Please sign in.' }, replace: true })`.
       - On error: show field-level or general error messages.
       - Loading/disabled state while submitting.
       - Bottom: `<Link to="/login">Back to Login</Link>`
  </action>
  <verify>
    <automated>cd frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ForgotPasswordPage renders email input with "Send Reset Link" button and success/error handling
    - ResetPasswordPage reads token/email from URL, shows new password + confirm fields, redirects to /login on success with success banner
    - Both pages use dark theme ParticleBackground + glassmorphism layout
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <action>
    Human verifies the complete auth flow works by walking through registration, verification, onboarding, password reset, and route protection in the browser.
  </action>
  <what-built>Complete auth flow: simplified registration, email verification with 6-digit code, onboarding (name + phone), forgot/reset password, sidebar avatar dropdown, route protection with multi-step guards</what-built>
  <how-to-verify>
    1. Start both servers: `cd backend && php artisan serve` and `cd frontend && npm run dev`
    2. Visit http://localhost:5173/ -- landing page should load without auth
    3. Click "Get Started" or navigate to /register -- should see simplified signup (email + password + legal checkbox only)
    4. Register with a new email. Should redirect to /verify-email page with 6-digit code input
    5. Check Laravel log (`backend/storage/logs/laravel.log`) for the verification email with 6-digit code
    6. Enter the code on /verify-email page -- should verify and redirect to /get-started
    7. On /get-started, enter name and phone number with country code picker, click "Get Started"
    8. Should redirect to /dashboard. Sidebar should show avatar dropdown (click user area for Account Settings + Logout)
    9. Log out. Try /forgot-password -- enter your email, check log for reset link
    10. Use the reset link to set a new password on /reset-password -- should redirect to /login with success banner
    11. Try accessing /dashboard while logged out -- should redirect to /login
    12. Verify /eula and /privacy-policy pages show placeholder content
  </how-to-verify>
  <verify>Manual browser walkthrough</verify>
  <done>All 12 verification steps pass. Complete auth flow works end-to-end.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd frontend && npx vite build 2>&1 | tail -10
cd backend && php artisan test 2>&1 | tail -10
```
Frontend build must succeed. All backend tests must pass. Manual browser walkthrough confirms full auth flow.
</verification>

<success_criteria>
- VerifyEmailPage shows 6-digit code input with auto-advance and paste, displays user email, has resend button
- GetStartedPage shows name + phone with country code picker, pre-fills name for OAuth users
- ForgotPasswordPage sends reset email and shows confirmation
- ResetPasswordPage accepts new password and redirects to login with success banner
- All pages use ParticleBackground + glassmorphism dark theme layout matching existing auth pages
- Complete flow works: Register -> Verify -> Onboard -> Dashboard
- OAuth flow works: OAuth -> Get Started -> Dashboard
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-auth-integration/04-03-SUMMARY.md`
</output>
