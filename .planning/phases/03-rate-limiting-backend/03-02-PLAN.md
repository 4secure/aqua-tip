---
phase: 03-rate-limiting-backend
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/tests/Feature/Credit/GuestCreditLimitTest.php
  - backend/tests/Feature/Credit/AuthCreditLimitTest.php
  - backend/tests/Feature/Credit/CreditResetTest.php
  - backend/tests/Feature/Credit/CreditStatusTest.php
  - backend/tests/Feature/Ioc/IocSearchTest.php
autonomous: true
requirements: [RATE-01, RATE-02, RATE-03]

must_haves:
  truths:
    - "Tests prove guest is limited to 1 IOC lookup per day by IP"
    - "Tests prove authenticated user is limited to 10 lookups per day by user ID"
    - "Tests prove credits reset after midnight UTC via lazy reset"
    - "Tests prove IOC search returns correct mock data structure with credit info"
    - "Tests prove GET /api/credits returns status without deducting"
  artifacts:
    - path: "backend/tests/Feature/Credit/GuestCreditLimitTest.php"
      provides: "RATE-01 guest limit tests"
      min_lines: 30
    - path: "backend/tests/Feature/Credit/AuthCreditLimitTest.php"
      provides: "RATE-02 auth user limit tests"
      min_lines: 40
    - path: "backend/tests/Feature/Credit/CreditResetTest.php"
      provides: "RATE-03 midnight UTC reset tests"
      min_lines: 30
  key_links:
    - from: "backend/tests/Feature/Credit/GuestCreditLimitTest.php"
      to: "POST /api/ioc/search"
      via: "HTTP test requests without auth cookies"
      pattern: "postJson.*ioc/search"
    - from: "backend/tests/Feature/Credit/AuthCreditLimitTest.php"
      to: "POST /api/ioc/search"
      via: "HTTP test requests with actingAs user"
      pattern: "actingAs.*postJson.*ioc/search"
---

<objective>
Comprehensive Pest test suite covering all three rate limiting requirements (RATE-01, RATE-02, RATE-03) plus IOC search endpoint behavior and credit status endpoint.

Purpose: Prove the credit system enforces limits correctly for guests and authenticated users, resets at midnight UTC, and returns proper response structures.
Output: 5 test files with full coverage of rate limiting behavior.
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-rate-limiting-backend/03-CONTEXT.md
@.planning/phases/03-rate-limiting-backend/03-RESEARCH.md
@.planning/phases/03-rate-limiting-backend/03-01-SUMMARY.md
@backend/tests/Feature/Auth/RegisterTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guest and authenticated credit limit tests (RATE-01, RATE-02)</name>
  <files>
    backend/tests/Feature/Credit/GuestCreditLimitTest.php,
    backend/tests/Feature/Credit/AuthCreditLimitTest.php,
    backend/tests/Feature/Ioc/IocSearchTest.php
  </files>
  <action>
    All tests use Pest syntax with RefreshDatabase trait. Use Origin header ('http://localhost:5173') for Sanctum stateful API requests, matching existing test patterns from Phase 1.

    1. Create GuestCreditLimitTest.php (RATE-01):
       - Test: guest first IOC search returns 200 with data and credits object
       - Test: guest second IOC search same day returns 429 with is_guest=true, remaining=0, limit=1, resets_at, message="Sign in for more lookups"
       - Test: guest 429 response includes correct JSON structure
       - Test: different IP addresses get independent credit pools (use REMOTE_ADDR server variable override)

    2. Create AuthCreditLimitTest.php (RATE-02):
       - Test: authenticated user can make 10 searches (loop 10 POSTs, all return 200)
       - Test: 11th search returns 429 with is_guest=false, remaining=0, limit=10, message="Daily limit reached"
       - Test: credits.remaining decrements correctly (check response after 1st call shows remaining=9, after 5th shows remaining=5)
       - Test: two different authenticated users have independent credit pools
       - Use actingAs($user) with User::factory()->create() for auth

    3. Create IocSearchTest.php (IOC search behavior):
       - Test: POST /api/ioc/search with valid IP query returns 200 with data.type='ipv4'
       - Test: POST /api/ioc/search with domain returns data.type='domain'
       - Test: POST /api/ioc/search with SHA256 hash returns data.type='sha256'
       - Test: POST /api/ioc/search without query returns 422 validation error
       - Test: POST /api/ioc/search with empty string returns 422
       - Test: response includes credits object with remaining, limit, resets_at keys
       - Test: search is logged in search_logs table (assert DB has record with module='ioc_search')
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test --filter="GuestCreditLimit|AuthCreditLimit|IocSearch"</automated>
  </verify>
  <done>Guest limited to 1/day (429 on 2nd), authenticated limited to 10/day (429 on 11th), IOC search returns correct mock data structure, search logs are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Credit reset and status endpoint tests (RATE-03)</name>
  <files>
    backend/tests/Feature/Credit/CreditResetTest.php,
    backend/tests/Feature/Credit/CreditStatusTest.php
  </files>
  <action>
    1. Create CreditResetTest.php (RATE-03):
       - Test: guest who used their credit yesterday can search again today (use Carbon::setTestNow to simulate day change)
         - Make a search (200), then Carbon::setTestNow(now()->addDay()), make another search (200)
       - Test: authenticated user who used all 10 credits yesterday has 10 again today
         - Make 10 searches (all 200), Carbon::setTestNow(now()->addDay()), make 1 search (200, remaining=9)
       - Test: credit remaining resets to full limit after midnight UTC
         - Create a Credit record with remaining=0, last_reset_at=yesterday. Make a request. Should succeed (lazy reset fires).
       - Test: credit does NOT reset if same day (remaining=0, last_reset_at=today should still return 429)
       - Use Carbon::setTestNow() for time travel and Carbon::setTestNow(null) in tearDown/afterEach

    2. Create CreditStatusTest.php:
       - Test: GET /api/credits for guest returns remaining=1, limit=1, resets_at when no prior searches
       - Test: GET /api/credits for guest after 1 search returns remaining=0
       - Test: GET /api/credits for authenticated user returns remaining=10, limit=10 when fresh
       - Test: GET /api/credits does NOT deduct a credit (call twice, remaining stays same)
       - Test: GET /api/credits response includes resets_at as ISO 8601 string
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test --filter="CreditReset|CreditStatus"</automated>
  </verify>
  <done>Credits proven to reset after midnight UTC via lazy reset. Credit status endpoint returns correct data without deducting. Full test suite passes: `cd backend && php artisan test` all green.</done>
</task>

</tasks>

<verification>
Full test suite:
```bash
cd C:/laragon/www/aqua-tip/backend && php artisan test
```

All existing Phase 1/2 tests plus new Phase 3 tests must pass.
</verification>

<success_criteria>
- All 5 test files exist and pass
- RATE-01 proven: guest gets 429 on 2nd search same day
- RATE-02 proven: auth user gets 429 on 11th search same day
- RATE-03 proven: credits reset after simulated day change via Carbon::setTestNow
- Credit status endpoint tested for both guest and auth without deduction
- Full backend test suite green (no regressions from Phase 1/2)
</success_criteria>

<output>
After completion, create `.planning/phases/03-rate-limiting-backend/03-02-SUMMARY.md`
</output>
