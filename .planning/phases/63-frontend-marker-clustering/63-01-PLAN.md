---
phase: 63-frontend-marker-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/styles/components.css
autonomous: true
requirements:
  - MAPCLU-02
user_setup: []
tags:
  - leaflet
  - markercluster
  - css
  - glassmorphism
  - dependency-install

must_haves:
  truths:
    - "leaflet.markercluster@1.5.3 is installed and resolvable by Vite"
    - "frontend/package.json contains exact entry \"leaflet.markercluster\": \"^1.5.3\""
    - "frontend/package-lock.json contains leaflet.markercluster subtree at 1.5.3"
    - "frontend/src/styles/components.css contains a .map-cluster-icon CSS block with the normative dark-glassmorphism spec from UI-SPEC §Component Contract"
    - "vite build is green after install and CSS append"
    - "v6.1 hook-lock is preserved byte-identical (useThreatStream.js + useThreatMapBuffer.js not touched)"
    - "No MarkerCluster.Default.css import appears anywhere in frontend/src/"
  artifacts:
    - path: "frontend/package.json"
      provides: "leaflet.markercluster dependency entry"
      contains: "\"leaflet.markercluster\""
    - path: "frontend/package-lock.json"
      provides: "locked dep subtree for leaflet.markercluster@1.5.3"
      contains: "leaflet.markercluster"
    - path: "frontend/src/styles/components.css"
      provides: ".map-cluster-icon dark-glassmorphism CSS block"
      contains: "map-cluster-icon"
      min_lines: 275
  key_links:
    - from: "frontend/src/pages/ThreatMapPage.jsx (Plan 02)"
      to: ".map-cluster-icon CSS (this plan)"
      via: "L.divIcon html string referencing class name"
      pattern: "map-cluster-icon"
---

<objective>
Wave 0 scaffold for Phase 63 marker clustering: install `leaflet.markercluster@1.5.3` as the only new v6.1 dependency (roadmap-locked) and append the `.map-cluster-icon` dark-glassmorphism CSS block to `components.css`.

Purpose: Unblock every subsequent phase-63 task. Without the install, `import 'leaflet.markercluster'` fails at Vite build time (P-04). Without the CSS class, the custom `iconCreateFunction` in Plan 02 would render an unstyled `<div>` on the map.

Output: Two tracked files modified (`frontend/package.json`, `frontend/src/styles/components.css`), one tracked file regenerated by npm (`frontend/package-lock.json`). No runtime behaviour change yet — clustering is not wired until Plan 02.

This plan implements D-01, D-02, D-15, D-27 from 63-CONTEXT.md and UI-SPEC §Component Contract `.map-cluster-icon` normative spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-frontend-marker-clustering/63-CONTEXT.md
@.planning/phases/63-frontend-marker-clustering/63-RESEARCH.md
@.planning/phases/63-frontend-marker-clustering/63-UI-SPEC.md
@.planning/phases/63-frontend-marker-clustering/63-VALIDATION.md
@CLAUDE.md
@frontend/package.json
@frontend/src/styles/components.css

<interfaces>
<!-- No new code interfaces in this plan. CSS-only + dependency-install. -->
<!-- The .map-cluster-icon class name is the contract consumed by Plan 02's buildClusterIcon() -->
<!-- html template: `<div class="map-cluster-icon">${count}</div>` -->

Existing components.css neighbour block (lines 241-257) for placement reference:
```css
/* Map marker pulse */
.map-marker {
  width: 10px;
  height: 10px;
  background: #FF3B5C;
  border-radius: 50%;
  position: relative;
}

.map-marker::before {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  border: 2px solid rgba(255, 59, 92, 0.4);
  animation: threatPulse 2s ease-in-out infinite;
}
```

Existing frontend/package.json dependencies block (lines 11-28) for alpha-order placement of the new entry between `leaflet` (line 19) and `lucide-react` (line 20):
```json
"leaflet": "^1.9.4",
"lucide-react": "^0.577.0",
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install leaflet.markercluster@1.5.3 in frontend/</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (read to confirm `leaflet: ^1.9.4` present at line 19 and no existing `leaflet.markercluster` entry — D-04 zero-transitive-deps verification baseline)
    - .planning/phases/63-frontend-marker-clustering/63-RESEARCH.md §Common Pitfalls P-02 (lockfile confusion) + P-04 (skipping install) + P-10 (wrong directory) — MUST read these three pitfalls before running npm install
    - .planning/phases/63-frontend-marker-clustering/63-CONTEXT.md D-01, D-04, D-27, D-28 — install spec + commit rules
  </read_first>
  <behavior>
    - After install: `frontend/node_modules/leaflet.markercluster/` directory exists and contains `dist/leaflet.markercluster.js` + `dist/MarkerCluster.css` + `dist/MarkerCluster.Default.css` (the Default.css file must exist in node_modules but must NEVER be imported by app code — that rule is enforced in Plan 02)
    - `frontend/package.json` gains exactly one new line in the `dependencies` block: `"leaflet.markercluster": "^1.5.3",` inserted alphabetically between `"leaflet"` (line 19) and `"lucide-react"` (line 20)
    - `frontend/package-lock.json` is regenerated by npm; the existing file (tracked since commit 37a7c06 per RESEARCH.md P-02) is modified in place — NOT a new file
    - `npm ls leaflet.markercluster` reports exactly `leaflet.markercluster@1.5.3` with the peer dep `leaflet` present and zero transitive runtime deps (D-04)
    - The ROOT `./package-lock.json` (untracked at session start per `git status`) is UNTOUCHED by this task — the install runs inside `frontend/`, not at the repo root
  </behavior>
  <action>
    Run the dependency install in the frontend workspace and verify the lockfile + package.json land exactly where expected.

    Step 1 — Install (MUST use explicit cwd; do NOT bare `npm install` from repo root — P-10):

    ```bash
    cd C:/laragon/www/aqua-tip/frontend && npm install leaflet.markercluster@1.5.3 --save
    ```

    Step 2 — Verify the install landed correctly (each of these MUST exit 0):

    ```bash
    grep -q '"leaflet.markercluster": "\^1\.5\.' C:/laragon/www/aqua-tip/frontend/package.json
    test -d C:/laragon/www/aqua-tip/frontend/node_modules/leaflet.markercluster
    test -f C:/laragon/www/aqua-tip/frontend/node_modules/leaflet.markercluster/dist/leaflet.markercluster.js
    test -f C:/laragon/www/aqua-tip/frontend/node_modules/leaflet.markercluster/dist/MarkerCluster.css
    cd C:/laragon/www/aqua-tip/frontend && npm ls leaflet.markercluster 2>&1 | grep -q "leaflet.markercluster@1.5.3"
    ```

    Step 3 — Verify root lockfile untouched (P-02 guard):

    ```bash
    # The root ./package-lock.json MUST still be untracked and unchanged by this task.
    # This is belt-and-braces: our install ran in frontend/, so root files cannot have changed.
    # But verify staging cannot accidentally pick up the root lockfile:
    cd C:/laragon/www/aqua-tip && git diff --name-only -- 'package-lock.json' | head -1
    # Expected: empty output (root lockfile is untracked, so it won't appear in diff)
    ```

    Step 4 — Verify v6.1 hook-lock preserved (D-25 architectural guarantee):

    ```bash
    cd C:/laragon/www/aqua-tip && git diff --stat frontend/src/hooks/useThreatStream.js frontend/src/hooks/useThreatMapBuffer.js
    # Expected: empty output (this task touched neither file)
    ```

    Step 5 — Stage ONLY the two intended files; NEVER use `git add -A` or `git add .` (P-02):

    ```bash
    cd C:/laragon/www/aqua-tip && git add frontend/package.json frontend/package-lock.json
    ```

    Step 6 — Pre-commit staging audit (P-02):

    ```bash
    cd C:/laragon/www/aqua-tip && git diff --cached --name-only
    # Expected: exactly these two lines:
    #   frontend/package-lock.json
    #   frontend/package.json
    # If root package-lock.json appears, UNSTAGE IT: `git reset HEAD package-lock.json`
    ```

    Step 7 — Commit:

    ```bash
    cd C:/laragon/www/aqua-tip && git commit -m "chore(63-01): install leaflet.markercluster@1.5.3 (v6.1 roadmap-locked)"
    ```

    Notes: Per D-01 this is the ONLY new dependency for the entire v6.1 milestone (Phases 64/65/66 resume zero-new-deps). The `^1.5.3` caret is roadmap-locked to 1.5.x patches only — no 1.6+ drift; the library was last published 2021-10-18 and is stable.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/frontend && grep -q '"leaflet.markercluster": "\^1\.5\.' package.json && test -d node_modules/leaflet.markercluster && test -f node_modules/leaflet.markercluster/dist/MarkerCluster.css && npm ls leaflet.markercluster 2>&1 | grep -q "leaflet.markercluster@1.5.3"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"leaflet.markercluster": "\^1\.5\.' frontend/package.json` exits 0
    - `test -d frontend/node_modules/leaflet.markercluster` exits 0
    - `test -f frontend/node_modules/leaflet.markercluster/dist/MarkerCluster.css` exits 0
    - `cd frontend && npm ls leaflet.markercluster` output contains the exact string `leaflet.markercluster@1.5.3`
    - `git diff --stat frontend/src/hooks/useThreatStream.js frontend/src/hooks/useThreatMapBuffer.js` returns EMPTY (v6.1 hook-lock preserved)
    - `git log -1 --name-only` shows exactly two files changed: `frontend/package.json` and `frontend/package-lock.json`
    - Root `./package-lock.json` (if present) is NOT in the commit (`git log -1 --name-only | grep -v '^frontend/' | grep -c 'package-lock.json'` returns 0)
    - Commit message starts with `chore(63-01):` and mentions `leaflet.markercluster@1.5.3`
  </acceptance_criteria>
  <done>
    leaflet.markercluster@1.5.3 is installed in frontend/, package.json + package-lock.json committed together, v6.1 hook-lock still byte-identical, root lockfile untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append .map-cluster-icon CSS block to components.css</name>
  <files>frontend/src/styles/components.css</files>
  <read_first>
    - frontend/src/styles/components.css (current 257 lines; read last 30 lines to locate the `.map-marker::before` block that ends at line 257 — the new block appends AFTER line 257)
    - .planning/phases/63-frontend-marker-clustering/63-UI-SPEC.md §Component Contract `.map-cluster-icon` (normative CSS spec — MUST be transcribed verbatim, byte-for-byte)
    - .planning/phases/63-frontend-marker-clustering/63-CONTEXT.md D-15 (file placement + no transitions/animations rule)
    - .planning/phases/63-frontend-marker-clustering/63-RESEARCH.md §Common Pitfalls P-08 (CSS specificity loss — no ancestor chains, no `!important`)
  </read_first>
  <behavior>
    - `frontend/src/styles/components.css` grows from 257 lines to ~276 lines (one blank separator line + one comment line + 18 CSS lines per the UI-SPEC spec)
    - Single new CSS rule appears: `.map-cluster-icon { ... }` — no `:hover` state, no `@media` block, no transitions, no animations (D-15 "NO transitions, NO animations — static visual")
    - Rule selector is single-class only (`.map-cluster-icon`) — no ancestor chain, no descendant combinators (P-08)
    - All color values are rgba literals matching `tailwind.config.js` tokens: `rgba(15, 17, 23, 0.8)` = `surface #0F1117` at 80% α; `rgba(30, 32, 48, 0.8)` = `border #1E2030` at 80% α; `#E8EAED` = `text-primary`
    - Font stack is `'JetBrains Mono', monospace` (NOT Outfit/sans — numeric content per CLAUDE.md + UI-SPEC §Typography)
    - Box dimensions are exactly 32×32px to match Plan 02's `iconSize: [32, 32]` — mismatch would render the bubble off-centre
    - WebKit prefix (`-webkit-backdrop-filter: blur(8px);`) is present for Safari parity
    - Vite build (`cd frontend && npm run build`) is green post-edit (no CSS parse errors)
    - The existing `.map-marker` block (lines 241-257) is UNTOUCHED (byte-identical before/after on lines 241-257)
  </behavior>
  <action>
    Append the `.map-cluster-icon` CSS block to the end of `frontend/src/styles/components.css`, immediately after the existing `.map-marker::before` closing brace at line 257.

    Exact content to append (MUST be byte-identical to this spec — UI-SPEC §Component Contract is the normative source):

    ```css

    /* Map cluster icon (Phase 63 — MAPCLU-01/02/05) */
    /* Static glassmorphism bubble rendered by buildClusterIcon() in ThreatMapPage.jsx */
    .map-cluster-icon {
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(15, 17, 23, 0.8);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      border: 1px solid rgba(30, 32, 48, 0.8);
      border-radius: 50%;
      font-family: 'JetBrains Mono', monospace;
      font-size: 12px;
      font-weight: 600;
      color: #E8EAED;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
      cursor: pointer;
      /* NO transitions, NO animations — static visual. */
    }
    ```

    The leading blank line above the `/* Map cluster icon ... */` comment is intentional — it separates the new block from the existing `.map-marker::before` closing brace. Use the Edit tool with an `old_string` anchored to the final `}` of `.map-marker::before` so the append is unambiguous; alternatively use the Write tool with the full file content (read first, concatenate).

    Post-edit, run Vite build to confirm no CSS parse errors:

    ```bash
    cd C:/laragon/www/aqua-tip/frontend && npm run build
    ```

    Expected: exit 0, "build completed" output, no red warnings. The build should take ~25s (per 63-VALIDATION.md sampling rate). Compare build-output css chunk count to pre-edit — should be unchanged (Vite merges all imported CSS into a single chunk per route).

    Then verify v6.1 hook-lock:

    ```bash
    cd C:/laragon/www/aqua-tip && git diff --stat frontend/src/hooks/useThreatStream.js frontend/src/hooks/useThreatMapBuffer.js
    # Expected: empty
    ```

    And verify no MarkerCluster.Default.css references (D-02 / P-01 belt-and-braces, even though no JS file imports it yet — establishes the prohibition from Wave 0):

    ```bash
    cd C:/laragon/www/aqua-tip && grep -r "MarkerCluster.Default.css" frontend/src/ 2>/dev/null
    # Expected: no output, exit 1
    ```

    Stage + commit:

    ```bash
    cd C:/laragon/www/aqua-tip && git add frontend/src/styles/components.css
    git commit -m "style(63-01): add .map-cluster-icon glassmorphism bubble CSS (MAPCLU-02 visual contract)"
    ```

    Notes: Per D-15 this CSS lives in `components.css`, NOT in `animations.css` (which is Phase 61 buffer-marker lifecycle only). Phase 66's integration polish may evaluate variant extensions; do not add any here. The bubble has NO hover state by design (cursor-pointer is the sole hover affordance — adding a transition would conflict with Leaflet's `.leaflet-cluster-anim` internals even though we skip the default CSS; belt-and-braces).
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip && grep -c 'map-cluster-icon' frontend/src/styles/components.css | grep -q '^[1-9]' && grep -q 'rgba(15, 17, 23, 0.8)' frontend/src/styles/components.css && grep -q "font-family: 'JetBrains Mono', monospace" frontend/src/styles/components.css && ! grep -rq "MarkerCluster.Default.css" frontend/src/ && cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'map-cluster-icon' frontend/src/styles/components.css` returns a value ≥ 1 (the selector appears at least once)
    - `grep -q 'rgba(15, 17, 23, 0.8)' frontend/src/styles/components.css` exits 0 (surface token present at 80% α)
    - `grep -q 'rgba(30, 32, 48, 0.8)' frontend/src/styles/components.css` exits 0 (border token present at 80% α)
    - `grep -q '#E8EAED' frontend/src/styles/components.css` exits 0 (text-primary token present)
    - `grep -q "font-family: 'JetBrains Mono', monospace" frontend/src/styles/components.css` exits 0
    - `grep -q 'backdrop-filter: blur(8px)' frontend/src/styles/components.css` exits 0 (standard property present)
    - `grep -q '\-webkit\-backdrop-filter: blur(8px)' frontend/src/styles/components.css` exits 0 (WebKit prefix present)
    - `grep -q 'border-radius: 50%' frontend/src/styles/components.css` exits 0
    - `grep -q '0 2px 8px rgba(0, 0, 0, 0.3)' frontend/src/styles/components.css` exits 0 (shadow spec matches)
    - `grep -q 'cursor: pointer' frontend/src/styles/components.css` exits 0
    - `grep -c 'transition:' frontend/src/styles/components.css` returns the SAME count as pre-edit (no new transitions added — `diff` against git HEAD should show only additions)
    - `grep -rq "MarkerCluster.Default.css" frontend/src/` returns exit 1 (prohibition enforced from Wave 0)
    - `cd frontend && npm run build` exits 0 with no warnings
    - `git diff --stat frontend/src/hooks/useThreatStream.js frontend/src/hooks/useThreatMapBuffer.js` returns EMPTY (v6.1 hook-lock preserved byte-identical)
    - Exactly one file changed in this commit (`git log -1 --name-only` shows only `frontend/src/styles/components.css`)
  </acceptance_criteria>
  <done>
    `.map-cluster-icon` CSS block is appended to `components.css` per the UI-SPEC normative spec byte-for-byte, Vite build is green, no Default.css imports anywhere in frontend/src/, v6.1 hook-lock still byte-identical.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Verification (all must pass before marking Plan 01 complete)

Run from repo root:

```bash
cd C:/laragon/www/aqua-tip

# 1. Dep install verified
grep -q '"leaflet.markercluster": "\^1\.5\.' frontend/package.json && echo "PASS: dep entry present" || echo "FAIL: dep entry missing"
test -d frontend/node_modules/leaflet.markercluster && echo "PASS: node_modules present" || echo "FAIL: node_modules missing"

# 2. CSS block verified
grep -c 'map-cluster-icon' frontend/src/styles/components.css
grep -q 'rgba(15, 17, 23, 0.8)' frontend/src/styles/components.css && echo "PASS: surface token" || echo "FAIL: surface missing"

# 3. Prohibited import NOT present (D-02 / P-01)
grep -rq "MarkerCluster.Default.css" frontend/src/ && echo "FAIL: Default.css reference leaked" || echo "PASS: no Default.css references"

# 4. v6.1 hook-lock byte-identical (D-25 architectural guarantee)
git diff --stat frontend/src/hooks/useThreatStream.js frontend/src/hooks/useThreatMapBuffer.js
# Expected: empty output

# 5. Vite build clean
cd frontend && npm run build
# Expected: exit 0, ~25s, no warnings
```

## VALIDATION.md Mapping

This plan's two tasks advance the Wave 0 checklist in `63-VALIDATION.md`:
- [x] `leaflet.markercluster@1.5.3` installed (D-01) — Task 1
- [x] `63-VALIDATION.md` scaffold exists (pre-created) — no plan task needed
- [x] `.map-cluster-icon` CSS present (referenced by SC1 Probe 1 + Probe 2) — Task 2

Plan 01 does NOT execute any SC1..SC5 probe — those run in Plan 03 after Plan 02 wires the behaviour.
</verification>

<success_criteria>
- leaflet.markercluster@1.5.3 installed in `frontend/` with zero transitive deps (verified via `npm ls`)
- `frontend/package.json` has `"leaflet.markercluster": "^1.5.3"` entry
- `frontend/package-lock.json` regenerated + committed alongside `package.json`
- Root `./package-lock.json` untouched (still untracked, not staged, not committed)
- `.map-cluster-icon` CSS block appended to `components.css` with byte-identical values from UI-SPEC §Component Contract
- `grep -rq "MarkerCluster.Default.css" frontend/src/` returns exit 1 (D-02 prohibition established)
- `cd frontend && npm run build` exits 0 with no warnings
- `git diff --stat frontend/src/hooks/useThreatStream.js frontend/src/hooks/useThreatMapBuffer.js` returns empty (v6.1 hook-lock byte-identical)
- Two commits created: `chore(63-01): install leaflet.markercluster@1.5.3 ...` and `style(63-01): add .map-cluster-icon ...`
</success_criteria>

<output>
After completion, create `.planning/phases/63-frontend-marker-clustering/63-01-SUMMARY.md` documenting:
- Exact install command + output (npm ls leaflet.markercluster)
- Final line count of `frontend/src/styles/components.css` before and after
- Confirmation that root `./package-lock.json` was NOT staged
- Confirmation that hook-lock diff-stat returned empty
- Vite build timing and clean exit
- Any deviations from the UI-SPEC normative CSS (should be zero)
</output>
