---
phase: 41-plan-data-restructure
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - backend/tests/Feature/Plan/PlanSeederTest.php
  - backend/tests/Feature/Credit/CreditResolverTest.php
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03

must_haves:
  truths:
    - "PlanSeederTest asserts Free=5/day, Basic=30/day/$10, Pro=50/day/$29, Enterprise=200/day/null price"
    - "PlanSeederTest verifies unified feature lists with tier-specific search count"
    - "CreditResolverTest asserts expired-trial users get 5 credits (Free tier)"
    - "CreditResolverTest asserts Basic plan users get 30 credits"
    - "CreditResolverTest asserts trial limit is 10 (unchanged, per D-11)"
    - "All plan-related tests pass with php artisan test --filter=Plan"
  artifacts:
    - path: "backend/tests/Feature/Plan/PlanSeederTest.php"
      provides: "Updated seeder test assertions"
      contains: "toBe(5)"
    - path: "backend/tests/Feature/Credit/CreditResolverTest.php"
      provides: "Updated credit resolver test assertions"
      contains: "toBe(30)"
  key_links:
    - from: "backend/tests/Feature/Plan/PlanSeederTest.php"
      to: "backend/database/seeders/PlanSeeder.php"
      via: "seeds and asserts plan values"
      pattern: "seed.*PlanSeeder"
    - from: "backend/tests/Feature/Credit/CreditResolverTest.php"
      to: "backend/app/Services/CreditResolver.php"
      via: "resolveLimit and lazyReset assertions"
      pattern: "resolveLimit\\|lazyReset"
---

<objective>
Update all test files to assert the new plan values (Free=5, Basic=30, price_cents=1000 for Basic, null for Enterprise) and verify the entire test suite passes.

Purpose: Tests currently assert stale values (Free=3, Basic=15, Basic price=900). This plan fixes all assertions to match the new seeder/migration values, confirming PLAN-01, PLAN-02 (trial=10 unchanged), and PLAN-03 behavior.

Output: Updated test files with all assertions passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-plan-data-restructure/41-CONTEXT.md
@.planning/phases/41-plan-data-restructure/41-01-SUMMARY.md

<interfaces>
<!-- Values from Plan 01 that tests must assert against -->

Plan tier values (from migration + seeder):
- Free:       daily_credit_limit=5,   price_cents=0,    features=["5 searches per day", "All threat lookups", "Full indicator data", "Search history", "Priority data access", "Dark web monitoring"]
- Basic:      daily_credit_limit=30,  price_cents=1000, features=["30 searches per day", ...]
- Pro:        daily_credit_limit=50,  price_cents=2900, features=["50 searches per day", ...], is_popular=true
- Enterprise: daily_credit_limit=200, price_cents=null,  features=[..., "API access"]

CreditResolver constants (unchanged):
- TRIAL_DAILY_LIMIT = 10
- GUEST_DAILY_LIMIT = 1

CreditResolver behavior:
- Guest (null user) -> 1
- Active trial (no plan, trial_ends_at future) -> 10
- Expired trial (no plan, trial_ends_at past) -> Free plan limit (5)
- User with plan -> plan->daily_credit_limit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PlanSeederTest assertions</name>
  <files>backend/tests/Feature/Plan/PlanSeederTest.php</files>
  <read_first>
    - backend/tests/Feature/Plan/PlanSeederTest.php
    - backend/database/seeders/PlanSeeder.php
    - .planning/phases/41-plan-data-restructure/41-CONTEXT.md
  </read_first>
  <action>
Update `backend/tests/Feature/Plan/PlanSeederTest.php` with the following changes:

**Test: 'plan seeder creates correct tiers'**
Change these assertions:
- `$free->daily_credit_limit` from `toBe(3)` to `toBe(5)` (per D-01)
- `$basic->daily_credit_limit` from `toBe(15)` to `toBe(30)` (per D-02)
- `$basic->price_cents` from `toBe(900)` to `toBe(1000)` (per D-02)
- Add assertion for Enterprise: `expect($enterprise->price_cents)->toBeNull()` (per D-04)

**Test: 'plan features are arrays'**
Keep the existing assertion `->toContain('50 searches per day')` (Pro is unchanged).
Add new assertions to verify unified feature list (per D-05):
```php
expect($pro->features)->toContain('All threat lookups');
expect($pro->features)->toContain('Dark web monitoring');
```

**Add new test: 'enterprise has API access feature'** (per D-06):
```php
test('enterprise has API access feature', function () {
    $this->seed(\Database\Seeders\PlanSeeder::class);
    $enterprise = Plan::where('slug', 'enterprise')->first();
    expect($enterprise->features)->toContain('API access');
    expect($enterprise->features)->toHaveCount(7);
});
```

**Add new test: 'free plan does not have API access'**:
```php
test('free plan does not have API access', function () {
    $this->seed(\Database\Seeders\PlanSeeder::class);
    $free = Plan::where('slug', 'free')->first();
    expect($free->features)->not->toContain('API access');
    expect($free->features)->toHaveCount(6);
});
```

Do NOT change the 'plan seeder creates 4 plans' or 'plan seeder is idempotent' tests — they should pass as-is.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=PlanSeederTest 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `PlanSeederTest.php` contains `expect($free->daily_credit_limit)->toBe(5)`
    - `PlanSeederTest.php` contains `expect($basic->daily_credit_limit)->toBe(30)`
    - `PlanSeederTest.php` contains `expect($basic->price_cents)->toBe(1000)`
    - `PlanSeederTest.php` contains `expect($enterprise->price_cents)->toBeNull()`
    - `PlanSeederTest.php` contains test named `enterprise has API access feature`
    - `PlanSeederTest.php` contains `toContain('API access')`
    - `php artisan test --filter=PlanSeederTest` exits 0
  </acceptance_criteria>
  <done>PlanSeederTest assertions match new plan values. All seeder tests pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Update CreditResolverTest assertions and run full suite</name>
  <files>backend/tests/Feature/Credit/CreditResolverTest.php</files>
  <read_first>
    - backend/tests/Feature/Credit/CreditResolverTest.php
    - backend/app/Services/CreditResolver.php
    - .planning/phases/41-plan-data-restructure/41-CONTEXT.md
  </read_first>
  <action>
Update `backend/tests/Feature/Credit/CreditResolverTest.php` with the following changes:

**Test: 'returns Free plan limit for user with no plan and expired trial'** (line ~41-48):
Change `toBe(3)` to `toBe(5)` — Free tier is now 5 credits/day (per D-01).

**Test: 'creates Credit record with plan-derived limit for authenticated user'** (line ~52-66):
Change both `->and($credit->remaining)->toBe(15)` and `->and($credit->limit)->toBe(15)` to `toBe(30)` — Basic tier is now 30/day (per D-02).

**Test: 'assigns Free plan to user with expired trial during lazy reset'** (line ~117-137):
Change both `->and($credit->remaining)->toBe(3)` and `->and($credit->limit)->toBe(3)` to `toBe(5)` — Free tier is now 5 (per D-01).

**Test: 'applies pending downgrade when plan_change_at is today or past'** (line ~139-163):
Change both `->and($credit->remaining)->toBe(3)` and `->and($credit->limit)->toBe(3)` to `toBe(5)` — downgrade target is Free tier at 5 (per D-01).

Do NOT change these tests (they should pass as-is per D-11):
- 'returns 1 for null user (guest)' — GUEST_DAILY_LIMIT stays 1
- 'returns 10 for user with no plan and active trial' — TRIAL_DAILY_LIMIT stays 10
- 'returns plan daily_credit_limit for user with plan' — Pro is still 50
- 'creates Credit record with 1/1 for guest' — stays 1
- 'does nothing if last_reset_at is today' — unchanged
- 'resets remaining and limit to plan-derived value...' — Pro is still 50
- 'does not apply pending downgrade if plan_change_at is in the future' — Pro is still 50

After updating, run the full plan-related test suite: `cd backend && php artisan test --filter=Plan` and then `cd backend && php artisan test --filter=Credit`.
  </action>
  <verify>
    <automated>cd C:/laragon/www/aqua-tip/backend && php artisan test --filter=PlanSeederTest && php artisan test --filter=CreditResolverTest && php artisan test --filter=PlanIndexTest 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `CreditResolverTest.php` line with 'expired trial' contains `toBe(5)` not `toBe(3)`
    - `CreditResolverTest.php` line with 'plan-derived limit for authenticated user' contains `toBe(30)` not `toBe(15)`
    - `CreditResolverTest.php` line with 'assigns Free plan to user with expired trial' contains `toBe(5)` not `toBe(3)`
    - `CreditResolverTest.php` line with 'applies pending downgrade' contains `toBe(5)` not `toBe(3)`
    - `CreditResolverTest.php` still contains `toBe(10)` for trial user test (unchanged per D-11)
    - `CreditResolverTest.php` still contains `toBe(1)` for guest test (unchanged)
    - `php artisan test --filter=PlanSeederTest` exits 0
    - `php artisan test --filter=CreditResolverTest` exits 0
    - `php artisan test --filter=PlanIndexTest` exits 0
  </acceptance_criteria>
  <done>All CreditResolverTest assertions match new plan values. Full plan and credit test suites pass green. PLAN-01 (correct tiers), PLAN-02 (trial=10 unchanged), and PLAN-03 (credit sync) are verified by tests.</done>
</task>

</tasks>

<verification>
- `cd backend && php artisan test --filter=Plan` — all plan tests pass
- `cd backend && php artisan test --filter=Credit` — all credit tests pass
- No test asserts old values (3, 15, 900 for Free/Basic)
</verification>

<success_criteria>
- PlanSeederTest asserts: Free=5, Basic=30/$10, Pro=50/$29/is_popular, Enterprise=200/null price
- PlanSeederTest verifies unified features and Enterprise-only API access
- CreditResolverTest asserts: expired-trial=5, Basic plan=30, trial=10, guest=1
- All 3 test files (PlanSeederTest, CreditResolverTest, PlanIndexTest) pass green
</success_criteria>

<output>
After completion, create `.planning/phases/41-plan-data-restructure/41-02-SUMMARY.md`
</output>
