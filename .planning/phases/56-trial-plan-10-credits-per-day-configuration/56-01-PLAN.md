---
phase: 56-observable-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/Services/DashboardService.php
  - backend/tests/Unit/Services/DashboardServiceTest.php
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
must_haves:
  truths:
    - "Dashboard counts API returns 7 entity types instead of 4"
    - "Email-Addr count is non-zero when OpenCTI has email observables"
    - "Url count is non-zero when OpenCTI has URL observables"
    - "Cryptocurrency-Wallet count is non-zero when OpenCTI has crypto observables"
    - "Threat Database widget on Threat Map page displays all 7 type rows"
  artifacts:
    - path: "backend/app/Services/DashboardService.php"
      provides: "7 entity types in fetchCounts()"
      contains: "'Email-Addr' => 'Email'"
    - path: "backend/tests/Unit/Services/DashboardServiceTest.php"
      provides: "Updated test asserting 7 entity types"
      contains: "toHaveCount(7)"
  key_links:
    - from: "backend/app/Services/DashboardService.php"
      to: "OpenCTI GraphQL API"
      via: "stixCyberObservables query with entity_type filter"
      pattern: "Email-Addr.*Email"
    - from: "frontend/src/data/dashboard-config.js"
      to: "backend/app/Services/DashboardService.php"
      via: "STAT_CARD_CONFIG entity_type keys matching API response"
      pattern: "Email-Addr"
---

<objective>
Add the 3 missing STIX observable entity types (Email-Addr, Url, Cryptocurrency-Wallet) to the DashboardService fetchCounts() method so the Threat Database widget on the Threat Map page displays real counts for all 7 observable types instead of showing 0 for the missing three.

Purpose: The frontend STAT_CARD_CONFIG already defines all 7 types with labels and colors, but the backend only queries 4 types. This is a backend data gap causing 3 widget rows to show 0.

Output: Updated DashboardService.php with 7 entity types, updated PHPDoc, and updated unit test asserting 7 types.
</objective>

<execution_context>
@C:\Users\ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-trial-plan-10-credits-per-day-configuration/56-CONTEXT.md
@.planning/phases/56-trial-plan-10-credits-per-day-configuration/56-RESEARCH.md

@backend/app/Services/DashboardService.php
@backend/tests/Unit/Services/DashboardServiceTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 missing entity types to DashboardService::fetchCounts()</name>
  <files>backend/app/Services/DashboardService.php</files>
  <read_first>
    - backend/app/Services/DashboardService.php (current $entityTypes array at lines 105-110, PHPDoc at lines 14-15 and 98-99)
    - frontend/src/data/dashboard-config.js (verify STAT_CARD_CONFIG entity_type keys match exactly)
  </read_first>
  <behavior>
    - After change: fetchCounts() iterates over 7 entity types instead of 4
    - The 3 new entries use exact STIX type names: Email-Addr, Url, Cryptocurrency-Wallet
    - The 3 new labels are: Email, URL, Crypto Wallet
    - GraphQL query structure remains unchanged (per D-02)
    - Cache key remains dashboard_counts (per Claude's Discretion)
  </behavior>
  <action>
Edit backend/app/Services/DashboardService.php to make these exact changes:

1. **Line 15** (PHPDoc on getCounts): Change
   `Get observable counts by entity type (IPv4-Addr, Domain-Name, Hostname, X509-Certificate).`
   to
   `Get observable counts by entity type (IPv4-Addr, Domain-Name, Hostname, X509-Certificate, Email-Addr, Cryptocurrency-Wallet, Url).`

2. **Line 99** (PHPDoc on fetchCounts): Change
   `Fetch entity type counts from OpenCTI via 4 sequential GraphQL queries.`
   to
   `Fetch entity type counts from OpenCTI via 7 sequential GraphQL queries.`

3. **Lines 105-110** ($entityTypes array): Replace the array with:
```php
$entityTypes = [
    'IPv4-Addr' => 'IP Addresses',
    'Domain-Name' => 'Domains',
    'Hostname' => 'Hostnames',
    'X509-Certificate' => 'Certificates',
    'Email-Addr' => 'Email',
    'Cryptocurrency-Wallet' => 'Crypto Wallet',
    'Url' => 'URL',
];
```

Per D-01: exact STIX type names and labels as specified. Per D-02: no changes to the GraphQL query or loop structure. Per D-06: ordering matches STAT_CARD_CONFIG (existing 4 first, then Email, Crypto, URL).

Do NOT change anything else in the file -- no changes to the GraphQL query, cache logic, or any other method.
  </action>
  <verify>
    <automated>cd backend && grep -c "Email-Addr" app/Services/DashboardService.php && grep -c "Cryptocurrency-Wallet" app/Services/DashboardService.php && grep -c "Url.*URL" app/Services/DashboardService.php && grep "via 7 sequential" app/Services/DashboardService.php</automated>
  </verify>
  <acceptance_criteria>
    - DashboardService.php line ~105-113 contains all 7 entries: IPv4-Addr, Domain-Name, Hostname, X509-Certificate, Email-Addr, Cryptocurrency-Wallet, Url
    - DashboardService.php contains the string "'Email-Addr' => 'Email'"
    - DashboardService.php contains the string "'Cryptocurrency-Wallet' => 'Crypto Wallet'"
    - DashboardService.php contains the string "'Url' => 'URL'"
    - PHPDoc contains "via 7 sequential GraphQL queries"
    - PHPDoc contains "Email-Addr, Cryptocurrency-Wallet, Url"
    - No other methods in the file are modified
  </acceptance_criteria>
  <done>DashboardService::fetchCounts() iterates over 7 entity types with correct STIX names and labels. PHPDoc updated to reflect 7 types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update DashboardServiceTest to assert 7 entity types</name>
  <files>backend/tests/Unit/Services/DashboardServiceTest.php</files>
  <read_first>
    - backend/tests/Unit/Services/DashboardServiceTest.php (current test at lines 32-52 asserts 4 types, cache test at line 190-200 mocks 4 queries)
    - backend/app/Services/DashboardService.php (verify the 7 entity types and their order after Task 1)
  </read_first>
  <behavior>
    - Test "getCounts returns 7 entity type counts from OpenCTI" passes with 7 mock responses
    - Test asserts all 7 entity types with correct labels and counts
    - Cache test mocks 7 queries instead of 4
    - All other tests remain unchanged
  </behavior>
  <action>
Edit backend/tests/Unit/Services/DashboardServiceTest.php to make these exact changes:

1. **Line 32** (test name): Change `'getCounts returns 4 entity type counts from OpenCTI'` to `'getCounts returns 7 entity type counts from OpenCTI'`

2. **Line 35** (mock times): Change `->times(4)` to `->times(7)`

3. **Lines 36-41** (mock return values): Replace with 7 responses:
```php
    ->andReturn(
        makeCountsResponse(100),
        makeCountsResponse(200),
        makeCountsResponse(300),
        makeCountsResponse(400),
        makeCountsResponse(500),
        makeCountsResponse(600),
        makeCountsResponse(700),
    );
```

4. **Line 46** (count assertion): Change `->toHaveCount(4)` to `->toHaveCount(7)`

5. **After line 51** (after X509-Certificate assertion): Add 3 new assertions:
```php
    expect($result[4])->toMatchArray(['entity_type' => 'Email-Addr', 'label' => 'Email', 'count' => 500]);
    expect($result[5])->toMatchArray(['entity_type' => 'Cryptocurrency-Wallet', 'label' => 'Crypto Wallet', 'count' => 600]);
    expect($result[6])->toMatchArray(['entity_type' => 'Url', 'label' => 'URL', 'count' => 700]);
```

6. **Line ~193** (cache test mock): Change `->times(4)` to `->times(7)` in the "getCounts caches result for 5 minutes" test.

Do NOT change any other tests (stale cache, exception, getIndicators, getCategories).
  </action>
  <verify>
    <automated>cd backend && php artisan test --filter=DashboardServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - DashboardServiceTest.php contains the string "getCounts returns 7 entity type counts from OpenCTI"
    - DashboardServiceTest.php contains "->times(7)" (appears twice: main test and cache test)
    - DashboardServiceTest.php contains "->toHaveCount(7)"
    - DashboardServiceTest.php contains "entity_type' => 'Email-Addr', 'label' => 'Email', 'count' => 500"
    - DashboardServiceTest.php contains "entity_type' => 'Cryptocurrency-Wallet', 'label' => 'Crypto Wallet', 'count' => 600"
    - DashboardServiceTest.php contains "entity_type' => 'Url', 'label' => 'URL', 'count' => 700"
    - `php artisan test --filter=DashboardServiceTest` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All DashboardServiceTest tests pass. The getCounts test verifies all 7 entity types with correct STIX names, labels, and counts. Cache test mocks 7 queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This change adds hardcoded strings to a server-side PHP array. No user input is involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|

No threats identified. The change adds 3 constant string entries to an existing array in server-side code. No user input is processed, no new endpoints are created, and the existing authentication/caching on the /api/dashboard/counts endpoint remains unchanged.
</threat_model>

<verification>
1. `cd backend && php artisan test --filter=DashboardServiceTest` -- all tests pass (7 entity types verified)
2. `cd backend && php artisan test` -- full test suite passes (no regressions)
3. `grep -c "Email-Addr\|Cryptocurrency-Wallet\|'Url'" backend/app/Services/DashboardService.php` returns 3
</verification>

<success_criteria>
- DashboardService::fetchCounts() queries 7 STIX observable types from OpenCTI
- Unit test asserts all 7 types with correct names, labels, and counts
- Full test suite passes with no regressions
- No frontend changes made (frontend already handles all 7 types)
- No GraphQL query structure changes (per D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/56-trial-plan-10-credits-per-day-configuration/56-01-SUMMARY.md`
</output>
