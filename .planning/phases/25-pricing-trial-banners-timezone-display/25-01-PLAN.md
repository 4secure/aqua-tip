---
phase: 25-pricing-trial-banners-timezone-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/PricingPage.jsx
  - frontend/src/components/pricing/PlanCard.jsx
  - frontend/src/components/pricing/PlanConfirmModal.jsx
  - frontend/src/App.jsx
  - frontend/src/data/mock-data.js
  - frontend/src/data/icons.jsx
autonomous: true
requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04
  - PRICE-05
  - PRICE-08

must_haves:
  truths:
    - "Navigating to /pricing renders 4 plan cards fetched from GET /api/plans"
    - "Pro card has violet glow border and 'Most Popular' badge"
    - "Enterprise card shows 'Contact Us' mailto link instead of selection button"
    - "User's current plan shows 'Current Plan' badge instead of action button"
    - "Selecting a plan opens confirmation modal with before/after credit and price comparison"
    - "Confirming plan selection calls POST /api/plan and refreshes user state"
    - "Pricing appears in sidebar navigation"
  artifacts:
    - path: "frontend/src/pages/PricingPage.jsx"
      provides: "Pricing page with 4-card layout, plan selection flow"
      min_lines: 80
    - path: "frontend/src/components/pricing/PlanCard.jsx"
      provides: "Individual plan card component"
      min_lines: 40
    - path: "frontend/src/components/pricing/PlanConfirmModal.jsx"
      provides: "Plan selection confirmation modal"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/PricingPage.jsx"
      to: "/api/plans"
      via: "apiClient.get on mount"
      pattern: "apiClient\\.get.*api/plans"
    - from: "frontend/src/pages/PricingPage.jsx"
      to: "/api/plan"
      via: "apiClient.post on confirm"
      pattern: "apiClient\\.post.*api/plan"
    - from: "frontend/src/App.jsx"
      to: "frontend/src/pages/PricingPage.jsx"
      via: "Route path=/pricing"
      pattern: "path.*pricing"
    - from: "frontend/src/data/mock-data.js"
      to: "/pricing"
      via: "NAV_ITEMS entry"
      pattern: "Pricing.*pricing"
---

<objective>
Create the pricing page with 4-card plan comparison layout, plan selection with confirmation modal, route, and sidebar navigation entry.

Purpose: Users can view and compare plan tiers, see their current plan, and switch plans with a confirmation step showing before/after credit limits and pricing.
Output: PricingPage.jsx, PlanCard.jsx, PlanConfirmModal.jsx, updated App.jsx route, NAV_ITEMS entry, pricing icon.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pricing-trial-banners-timezone-display/25-CONTEXT.md
@.planning/phases/25-pricing-trial-banners-timezone-display/25-RESEARCH.md

<interfaces>
<!-- Backend API contracts (already built in Phase 23) -->

GET /api/plans (Public, no auth required):
```json
[
  { "id": 1, "slug": "free", "name": "Free", "daily_credit_limit": 3, "price_cents": 0, "features": ["Basic IOC lookups", "3 searches per day"], "description": "...", "is_popular": false },
  { "id": 2, "slug": "basic", "name": "Basic", "daily_credit_limit": 15, "price_cents": 900, "features": ["All IOC types", "15 searches per day", "Search history"], "description": "...", "is_popular": false },
  { "id": 3, "slug": "pro", "name": "Pro", "daily_credit_limit": 50, "price_cents": 2900, "features": ["All IOC types", "50 searches per day", "Search history", "Dark web monitoring", "Priority support"], "description": "...", "is_popular": true },
  { "id": 4, "slug": "enterprise", "name": "Enterprise", "daily_credit_limit": 200, "price_cents": 0, "features": ["Unlimited IOC types", "200 searches per day", "Full API access", "Dedicated support", "Custom integrations"], "description": "...", "is_popular": false }
]
```

POST /api/plan (Authenticated, validates slug in:free,basic,pro -- enterprise excluded):
Request: { "plan": "pro" }
Response (upgrade): { "message": "Plan upgraded", "plan": { "id": 3, "slug": "pro", "name": "Pro", "daily_credit_limit": 50 } }
Response (downgrade): { "message": "Downgrade scheduled", "pending_plan": { ... }, "plan_change_at": "2026-04-23T..." }

UserResource shape (from AuthContext user):
```json
{
  "plan": { "id": 3, "slug": "pro", "name": "Pro", "daily_credit_limit": 50, "features": [...] },
  "trial_active": false,
  "trial_days_left": 0,
  "pending_plan": null,
  "plan_change_at": null
}
```

From frontend/src/api/client.js:
```js
export const apiClient = { get: (url) => request('GET', url), post: (url, data) => request('POST', url, data) };
```

From frontend/src/contexts/AuthContext.jsx:
```js
// useAuth() returns: { user, isAuthenticated, refreshUser, timezone, ... }
// refreshUser() re-fetches current user from backend
```

From frontend/src/data/icons.jsx:
```js
export const ICONS = Object.freeze({ dashboard: `<svg...`, ... });
export function Icon({ name, className = '' }) { ... }
```

From frontend/src/data/mock-data.js:
```js
export const NAV_ITEMS = Object.freeze([
  { label: 'Dashboard', icon: 'dashboard', href: '/dashboard', public: false },
  { label: 'Threat Search', icon: 'search', href: '/threat-search', public: true },
  // ... more items
]);
```

Existing patterns:
- Glassmorphism card: `bg-surface/60 border border-border backdrop-blur-sm rounded-xl`
- GradientButton: violet gradient used across auth pages
- Lazy-loaded pages: `const Page = lazy(() => import('./pages/Page'))`
- Font classes: font-display for headings, font-mono for data, font-body for general
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlanCard, PlanConfirmModal, and PricingPage components</name>
  <files>
    frontend/src/components/pricing/PlanCard.jsx
    frontend/src/components/pricing/PlanConfirmModal.jsx
    frontend/src/pages/PricingPage.jsx
  </files>
  <read_first>
    frontend/src/contexts/AuthContext.jsx
    frontend/src/api/client.js
    frontend/src/components/ui/GradientButton.jsx
    frontend/src/pages/SettingsPage.jsx (reference for page structure pattern)
    frontend/src/styles/glassmorphism.css
    frontend/src/styles/components.css (chip classes)
  </read_first>
  <action>
    Create `frontend/src/components/pricing/PlanCard.jsx`:
    - Props: `plan` (object), `currentPlanSlug` (string|null), `onSelect` (function), `isTrialUser` (boolean)
    - Card uses glassmorphism pattern: `bg-surface/60 border backdrop-blur-sm rounded-xl p-6`
    - Per D-02: If `plan.is_popular === true`, apply `border-violet shadow-lg shadow-violet/20` and render a "Most Popular" badge at top using `chip chip-violet` or a small absolute-positioned pill: `<span className="absolute -top-3 left-1/2 -translate-x-1/2 px-3 py-1 bg-violet text-white text-xs font-display rounded-full">Most Popular</span>`
    - Per D-02: Non-popular cards use `border-border`
    - Per D-07: Card content layout (top to bottom):
      1. Plan name: `<h3 className="font-display text-lg font-bold text-white">{plan.name}</h3>`
      2. Price: `plan.price_cents === 0 ? (plan.slug === 'enterprise' ? 'Custom' : 'Free') : '$' + (plan.price_cents / 100) + '/mo'` in large text
      3. Daily credit limit: `<p className="font-mono text-sm text-text-secondary">{plan.daily_credit_limit} searches/day</p>`
      4. Feature list: `plan.features.map(f => <li>)` with check icon (use lucide-react Check icon, green color) before each
      5. Action button area (bottom of card, mt-auto with flex-1 spacer)
    - Per D-03: If `plan.slug === currentPlanSlug`, show `<span className="chip chip-cyan">Current Plan</span>` centered, no button
    - Per D-04: If `plan.slug === 'enterprise'`, show `<a href="mailto:sales@aquasecure.ai" className="...">Contact Us</a>` styled as outlined button (border-violet text-violet hover:bg-violet/10)
    - Per D-03: Otherwise show "Select Plan" button (or "Upgrade" if user has a plan) using violet gradient: `bg-gradient-to-r from-violet to-cyan text-white font-display font-semibold py-2.5 px-6 rounded-lg`
    - Card has `relative overflow-hidden` for the badge positioning
    - Card should have equal height with siblings: use `flex flex-col h-full`

    Create `frontend/src/components/pricing/PlanConfirmModal.jsx`:
    - Props: `isOpen` (boolean), `onClose` (function), `onConfirm` (function), `selectedPlan` (object), `currentPlan` (object|null), `isLoading` (boolean)
    - Per D-05: Modal overlay with backdrop: `fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm`
    - Modal content: `bg-surface border border-border rounded-xl p-6 max-w-md w-full mx-4 shadow-2xl`
    - Title: `Switch to {selectedPlan.name}?`
    - Comparison section showing:
      - Daily limit: `{currentPlan?.daily_credit_limit || (isTrialUser ? 10 : 3)} -> {selectedPlan.daily_credit_limit}`
      - Price: `${currentPlan?.price_cents ? currentPlan.price_cents/100 : 0}/mo -> ${selectedPlan.price_cents/100}/mo`
    - If downgrade (selectedPlan.daily_credit_limit < currentPlan.daily_credit_limit): show amber warning text "Downgrade will take effect at end of billing period"
    - Buttons: "Cancel" (text button) and "Confirm" (violet gradient button, shows spinner when isLoading)
    - Close on Escape key press and backdrop click
    - Use Framer Motion for enter/exit animation: `AnimatePresence` wrapping `motion.div` with opacity and scale transitions

    Create `frontend/src/pages/PricingPage.jsx`:
    - Fetch plans from `apiClient.get('/api/plans')` in useEffect on mount, store in state
    - Get `{ user, isAuthenticated, refreshUser }` from `useAuth()`
    - Derive `currentPlanSlug = user?.plan?.slug ?? null`
    - Derive `isTrialUser = user?.trial_active === true`
    - Loading state: show 4 skeleton cards (use same pattern as SkeletonCard in project)
    - Error state: show error message with retry button
    - Per D-01: Card grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6` for horizontal row on desktop, stacking on mobile
    - Page header: `<h1 className="font-display text-2xl font-bold text-white mb-2">Choose Your Plan</h1>` with subtitle
    - On card select: open PlanConfirmModal with selected plan data
    - On confirm: call `apiClient.post('/api/plan', { plan: selectedPlan.slug })`, then `await refreshUser()`, close modal, show success state
    - Handle API errors (422 for enterprise should never happen due to D-04, but handle gracefully)
    - After successful plan change, re-fetch plans list too (to update the "Current Plan" state immediately)
    - Sort plans by `sort_order` field if present, otherwise by `id`
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PlanCard.jsx exists and contains `is_popular` check with `border-violet` and `Most Popular` string
    - PlanCard.jsx contains `slug === 'enterprise'` check with `Contact Us` and `mailto:sales@aquasecure.ai`
    - PlanCard.jsx contains `currentPlanSlug` comparison with `Current Plan` string
    - PlanConfirmModal.jsx exists and contains `AnimatePresence` from framer-motion
    - PlanConfirmModal.jsx contains `apiClient` or `onConfirm` prop and displays daily_credit_limit comparison
    - PricingPage.jsx contains `apiClient.get('/api/plans')` fetch call
    - PricingPage.jsx contains `apiClient.post('/api/plan'` for plan selection
    - PricingPage.jsx contains `refreshUser()` call after successful plan selection
    - PricingPage.jsx contains `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4` responsive grid
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>
    PricingPage renders 4 plan cards from API, Pro card highlighted with violet glow and "Most Popular" badge, Enterprise shows "Contact Us" mailto, current plan shows badge, other plans show selection button, confirmation modal opens on select with before/after comparison, plan change calls POST /api/plan and refreshes user.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pricing route, sidebar nav entry, and pricing icon</name>
  <files>
    frontend/src/App.jsx
    frontend/src/data/mock-data.js
    frontend/src/data/icons.jsx
  </files>
  <read_first>
    frontend/src/App.jsx
    frontend/src/data/mock-data.js
    frontend/src/data/icons.jsx
  </read_first>
  <action>
    Per D-06 and PRICE-08:

    1. In `frontend/src/data/icons.jsx`, add a `pricing` key to the ICONS object. Use a CreditCard-style SVG icon (matching existing icon style: width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"):
    ```
    pricing: `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="14" x="2" y="5" rx="2"/><line x1="2" x2="22" y1="10" y2="10"/></svg>`,
    ```

    2. In `frontend/src/data/mock-data.js`, add a Pricing entry to NAV_ITEMS array. Insert it after the last current entry ('Threat News') and before the closing bracket. The NAV_ITEMS is frozen with Object.freeze, so the entry must be inside the array literal:
    ```js
    { label: 'Pricing', icon: 'pricing', href: '/pricing', public: true },
    ```
    Note: `public: true` because the pricing page should be visible to both authenticated and unauthenticated users.

    3. In `frontend/src/App.jsx`:
    - Add lazy import at top with other lazy imports: `const PricingPage = lazy(() => import('./pages/PricingPage'));`
    - Add route inside the `<Route element={<AppLayout />}>` block, as a PUBLIC route (not inside ProtectedRoute), right after the threat-search route:
    ```jsx
    <Route path="/pricing" element={<PricingPage />} />
    ```
    This goes in the same section as `/threat-search` (public route within AppLayout).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - icons.jsx contains `pricing:` key with an SVG string
    - mock-data.js NAV_ITEMS contains entry with `label: 'Pricing'` and `href: '/pricing'` and `public: true`
    - App.jsx contains `const PricingPage = lazy(() => import('./pages/PricingPage'))`
    - App.jsx contains `path="/pricing"` route element
    - App.jsx pricing route is NOT inside ProtectedRoute wrapper (it is a public route within AppLayout)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>
    /pricing route is registered in App.jsx as a public route within AppLayout, sidebar navigation shows Pricing entry with credit card icon, pricing page is lazy-loaded.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` completes without errors
2. Navigate to /pricing in browser -- 4 plan cards visible in horizontal row on desktop
3. Pro card has violet glow and "Most Popular" badge
4. Enterprise card shows "Contact Us" link
5. Logged-in user sees "Current Plan" badge on their plan
6. Clicking another plan opens confirmation modal with credit/price comparison
7. Confirming updates the plan and refreshes the page state
8. "Pricing" appears in sidebar navigation
</verification>

<success_criteria>
- Pricing page renders 4 cards from GET /api/plans with correct layout (PRICE-01, PRICE-02)
- Pro highlighted as Most Popular (PRICE-03)
- Enterprise shows Contact Us (PRICE-04)
- Current plan indicated with badge (PRICE-05)
- Pricing in sidebar nav (PRICE-08)
- Plan selection with confirmation modal works end-to-end
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-pricing-trial-banners-timezone-display/25-01-SUMMARY.md`
</output>
